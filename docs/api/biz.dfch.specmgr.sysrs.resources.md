# `biz.dfch.specmgr.sysrs.resources`

MCP resource registrations for System Requirements Specification (SYSRS) documents (placeholder).

Empty as of Phase 2 (models + parser) of
``.specmgr/feat/feat-32-sysrs/README.md`` -- the 3 planned resources
(``specmgr://sysrs/schema``, ``specmgr://sysrs/example``,
``specmgr://sysrs/template``) are added in Phase 4, mirroring
``sop/resources/``'s/``vcr/resources/``'s shape (no ``/{id}``, no
``/list``).
