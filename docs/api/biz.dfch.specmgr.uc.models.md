# `biz.dfch.specmgr.uc.models`

Use Case models — Pydantic schema for Cockburn-based use cases.

This package contains versioned model definitions. The current version is re-exported
as "current" for convenience.
