{
  "$comment": "v1",
  "$defs": {
    "AcceptanceCriteria": {
      "description": "`### Acceptance Criteria` under Related Artifacts -- bullet list of\ncross-references to acceptance criteria, one per line\n(e.g. \"ACC-1234: <title>\").",
      "properties": {
        "items": {
          "description": "Bullet list of cross-references to acceptance criteria, one per line (e.g. \"ACC-1234: <title>\"); must contain at least one item.",
          "items": {
            "$ref": "#/$defs/MarkdownListItem"
          },
          "minItems": 1,
          "title": "Items",
          "type": "array"
        }
      },
      "required": [
        "items"
      ],
      "title": "AcceptanceCriteria",
      "type": "object"
    },
    "Decisions": {
      "description": "`### Decisions` under Related Artifacts -- bullet list of\ncross-references to decisions, one per line (e.g. \"DEC-2703: <title>\").",
      "properties": {
        "items": {
          "description": "Bullet list of cross-references to decisions, one per line (e.g. \"DEC-2703: <title>\"); must contain at least one item.",
          "items": {
            "$ref": "#/$defs/MarkdownListItem"
          },
          "minItems": 1,
          "title": "Items",
          "type": "array"
        }
      },
      "required": [
        "items"
      ],
      "title": "Decisions",
      "type": "object"
    },
    "Description": {
      "description": "`## Description` -- free-form prose giving context/rationale for the\ngoal statement above it. Optional.",
      "properties": {},
      "title": "Description",
      "type": "object"
    },
    "Goal": {
      "description": "The goal body: a single H1 section with the fields below.\n\nThe H1 heading text is free-form. Mirrors `Requirement` (REQ) minus\n`Characteristics` and minus `Level` -- see the module docstring.\n\nParameters\n----------\nstatement:\n    The lead paragraph right after the H1. Mandatory.\ndescription:\n    `## Description`. Optional.\npriority:\n    `## Priority`. Optional.\ntags:\n    `## Tags`. Optional.\nsource:\n    `## Source`. Mandatory.\nrelated_artifacts:\n    `## Related Artifacts`. Optional.\nmore_information:\n    `## More Information`. Optional.\nnotes:\n    `## Notes`. Optional.",
      "properties": {
        "description": {
          "anyOf": [
            {
              "$ref": "#/$defs/Description"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "`## Description` section. Optional."
        },
        "more_information": {
          "anyOf": [
            {
              "$ref": "#/$defs/MoreInformation"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "`## More Information` section. Optional."
        },
        "notes": {
          "anyOf": [
            {
              "$ref": "#/$defs/Notes"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "`## Notes` section. Optional."
        },
        "priority": {
          "anyOf": [
            {
              "$ref": "#/$defs/Priority"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "`## Priority` section. Optional."
        },
        "related_artifacts": {
          "anyOf": [
            {
              "$ref": "#/$defs/RelatedArtifacts"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "`## Related Artifacts` section. Optional."
        },
        "source": {
          "$ref": "#/$defs/Source",
          "description": "`## Source` section. Mandatory."
        },
        "statement": {
          "$ref": "#/$defs/MarkdownParagraph",
          "description": "The lead paragraph directly under the H1, before any H2 section -- the goal statement itself. Mandatory."
        },
        "tags": {
          "anyOf": [
            {
              "$ref": "#/$defs/Tags"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "`## Tags` section. Optional."
        }
      },
      "required": [
        "statement",
        "source"
      ],
      "title": "Goal",
      "type": "object"
    },
    "Goals": {
      "description": "`### Goals` under Related Artifacts -- bullet list of\ncross-references to goals, one per line (e.g. \"GOL-0007: <title>\").",
      "properties": {
        "items": {
          "description": "Bullet list of cross-references to goals, one per line (e.g. \"GOL-0007: <title>\"); must contain at least one item.",
          "items": {
            "$ref": "#/$defs/MarkdownListItem"
          },
          "minItems": 1,
          "title": "Items",
          "type": "array"
        }
      },
      "required": [
        "items"
      ],
      "title": "Goals",
      "type": "object"
    },
    "GolFrontmatter": {
      "description": "Goal (GOL) frontmatter: `MarkdownFrontmatter` narrowed for the ``gol`` document type.\n\nParameters\n----------\ntype:\n    Fixed discriminator, always ``\"gol\"``. Narrows the base's mandatory,\n    default-less ``str`` field to a ``Literal[\"gol\"] = \"gol\"``, so a document\n    omitting ``type`` entirely still parses as a goal document.\nstatus:\n    One of ``\"draft\"``, ``\"proposed\"``, ``\"accepted\"``, ``\"superseded\"``,\n    ``\"deprecated\"``, ``\"rejected\"``, ``\"implemented\"``. Narrows the base's\n    free-form ``str = \"draft\"`` default to this closed seven-value set\n    (REQ's exact set -- goals are business-level requirements, so\n    requirement-lifecycle semantics apply). Blank/absent still defaults\n    to ``\"draft\"`` (inherited from the base's\n    ``_default_blank_status_to_draft`` validator, which runs before this one).\nversion:\n    The ``models.md`` schema major.minor.patch version this document's\n    frontmatter was written with. DO NOT CHANGE!\n\nAll other fields (``id``, ``created``, ``updated``, ``version``) are inherited\nunchanged from :class:`MarkdownFrontmatter`.",
      "properties": {
        "created": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Created"
        },
        "id": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Id"
        },
        "status": {
          "default": "draft",
          "title": "Status",
          "type": "string"
        },
        "type": {
          "const": "gol",
          "default": "gol",
          "title": "Type",
          "type": "string"
        },
        "updated": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Updated"
        },
        "version": {
          "default": "1.0.0",
          "title": "Version",
          "type": "string"
        }
      },
      "title": "GolFrontmatter",
      "type": "object"
    },
    "MarkdownComment": {
      "description": "A standalone HTML comment block (`\"html_block\"`, e.g. `<!-- some note -->`).\n\nDeliberately leaf-only, like `MarkdownCodeBlock`. Declare an optional\n`comment: MarkdownComment | None` field on any `MarkdownStr` subclass to\nlet a value be preceded by such an explanatory comment without it\nbreaking that class's own structural field matching. See `get_extent`/\n`from_text`/`text` for the full mechanics.",
      "properties": {},
      "title": "MarkdownComment",
      "type": "object"
    },
    "MarkdownListItem": {
      "description": "One item (\"li\") of a markdown bullet or numbered list.\n\nUsed only as `items: list[MarkdownListItem]` (or `list[MarkdownListItem] |\nNone`) on a `MarkdownSection`/`MarkdownParagraph`/`MarkdownListItem` --\nthere is no separate list-container class, and a bare item cannot be\nparsed as a top-level/scalar field on its own; every item is assumed to\nstart with at least one paragraph (its own lead text).\n\nLeaf (no declared nested fields, the default): `_value` holds the item's\ncomplete extent verbatim, marker included. Composite (a subclass declares\nfields): `_value` holds only the item's own leading paragraph, marker\nincluded -- the marker cannot be reconstructed from class metadata alone,\nso it is kept verbatim -- and the remainder is dedented and delegated to\nnested field parsing; `__str__` re-indents the children's rendered output\nby the marker's own width before recombining.\n\nRound-trip note: a tight source list currently round-trips to a\nstructurally-equivalent loose list rather than byte-exact (loose lists\nremain byte-exact) -- an accepted, documented exception; see\n`from_text`/`__str__` docstrings below for the full mechanics.",
      "properties": {},
      "title": "MarkdownListItem",
      "type": "object"
    },
    "MarkdownListItemWithNotes": {
      "description": "One list item that captures continuation paragraphs (loose-list notes) via a declared `notes` field.\n\nMirrors `ExtensionItem`'s docstring: adds `notes` for captured continuation\nparagraphs inside a list item, so they appear in ``model_dump()``/JSON output\ninstead of being lost to Pydantic's private-attribute invisibility. Delegates\n``get_extent()`` / ``from_text()`` / ``__str__()`` to the inherited\n``MarkdownListItem`` (no new extent logic needed; ``MarkdownStr.process_list_field()``\niterates items by ``get_extent()`` and the field-distribution loop picks up\nremaining text).",
      "properties": {
        "notes": {
          "anyOf": [
            {
              "items": {
                "$ref": "#/$defs/MarkdownParagraph"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Notes"
        }
      },
      "title": "MarkdownListItemWithNotes",
      "type": "object"
    },
    "MarkdownParagraph": {
      "description": "A markdown paragraph (\"p\"), the non-heading sibling of `MarkdownSection`.\n\nA paragraph has no level, so there is a single `MarkdownParagraph` class\n(no `MarkdownParagraph1`..`6` spectrum) and no `@alias` check -- a\nparagraph's content is free-form prose, not a title.\n\nLeaf (no declared nested fields): `_value` holds the paragraph's own line\nspan, verbatim. Composite (declares fields): `_value` holds only the\nparagraph's own inline text (e.g. a lead-in sentence); the remaining text\nis delegated to nested field parsing, bounded by the next heading of any\nlevel (a paragraph can never itself contain a heading). See\n`get_extent`/`from_text`/`__str__` docstrings below for the full\nmechanics.",
      "properties": {},
      "title": "MarkdownParagraph",
      "type": "object"
    },
    "MoreInformation": {
      "description": "`## More Information` -- free-form optional supplementary text, no\nfixed format. Optional.",
      "properties": {},
      "title": "MoreInformation",
      "type": "object"
    },
    "Notes": {
      "description": "`## Notes` -- free-form optional remarks (e.g. change history). Optional.",
      "properties": {},
      "title": "Notes",
      "type": "object"
    },
    "Priority": {
      "description": "`## Priority` -- single-line value giving the goal's relative\npriority (e.g. a numeric rank). Optional. May be preceded by an\nexplanatory HTML comment (e.g. describing the numeric range).",
      "properties": {
        "comment": {
          "anyOf": [
            {
              "$ref": "#/$defs/MarkdownComment"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional explanatory HTML comment (`<!-- ... -->`) preceding `value`, e.g. describing the numeric range."
        },
        "value": {
          "$ref": "#/$defs/MarkdownParagraph",
          "description": "Single-line value giving the goal's relative importance (0 to 99, lower number is more important, e.g. 50)."
        }
      },
      "required": [
        "value"
      ],
      "title": "Priority",
      "type": "object"
    },
    "RelatedArtifacts": {
      "description": "`## Related Artifacts` -- container for four independent, all-optional\n`### ` cross-reference lists (requirements/decisions/goals/acceptance\ncriteria). Optional as a whole; no consistency check is enforced between\nthe sub-lists.",
      "properties": {
        "acceptance_criteria": {
          "anyOf": [
            {
              "$ref": "#/$defs/AcceptanceCriteria"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "`### Acceptance Criteria` sub-section. Optional."
        },
        "decisions": {
          "anyOf": [
            {
              "$ref": "#/$defs/Decisions"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "`### Decisions` sub-section. Optional."
        },
        "goals": {
          "anyOf": [
            {
              "$ref": "#/$defs/Goals"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "`### Goals` sub-section. Optional."
        },
        "requirements": {
          "anyOf": [
            {
              "$ref": "#/$defs/Requirements"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "`### Requirements` sub-section. Optional."
        }
      },
      "title": "RelatedArtifacts",
      "type": "object"
    },
    "Requirements": {
      "description": "`### Requirements` under Related Artifacts -- bullet list of\ncross-references to other requirements, one per line\n(e.g. \"REQ-9687: <title>\").",
      "properties": {
        "items": {
          "description": "Bullet list of cross-references to other requirements, one per line (e.g. \"REQ-9687: <title>\"); must contain at least one item.",
          "items": {
            "$ref": "#/$defs/MarkdownListItem"
          },
          "minItems": 1,
          "title": "Items",
          "type": "array"
        }
      },
      "required": [
        "items"
      ],
      "title": "Requirements",
      "type": "object"
    },
    "Source": {
      "description": "`## Source` -- single-line value naming the origin/authority of this\ngoal. Mandatory.",
      "properties": {
        "value": {
          "$ref": "#/$defs/MarkdownParagraph",
          "description": "Single-line value naming the origin/authority of this goal."
        }
      },
      "required": [
        "value"
      ],
      "title": "Source",
      "type": "object"
    },
    "Tags": {
      "description": "`## Tags` -- bullet list of free-form labels for grouping/filtering\ngoals. Optional.",
      "properties": {
        "items": {
          "description": "Bullet list of free-form labels for grouping/filtering goals; must contain at least one item.",
          "items": {
            "$ref": "#/$defs/MarkdownListItemWithNotes"
          },
          "minItems": 1,
          "title": "Items",
          "type": "array"
        }
      },
      "required": [
        "items"
      ],
      "title": "Tags",
      "type": "object"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "A full goal document: YAML frontmatter and body.\n\nAttributes\n----------\nfrontmatter:\n    The YAML frontmatter block. See :class:`GolFrontmatter`.\nbody:\n    The parsed goal sections. See :class:`Goal`.",
  "properties": {
    "body": {
      "$ref": "#/$defs/Goal"
    },
    "frontmatter": {
      "$ref": "#/$defs/GolFrontmatter"
    }
  },
  "required": [
    "frontmatter",
    "body"
  ],
  "title": "GolDocument",
  "type": "object"
}
