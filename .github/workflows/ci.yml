name: Lint and Test

on: [push, pull_request]

jobs:
  build:
    env:
      # Opt-in to Node 24 to avoid Node 20 deprecation warnings (effective June 2026).
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    permissions:
      contents: read
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
    - name: Checkout
      # uses: actions/checkout@v5
      uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8

    - name: Install uv
      # uses: astral-sh/setup-uv@v5 v5.2.1
      uses: astral-sh/setup-uv@b5f58b2abc5763ade55e4e9d0fe52cd1ff7979ca
      with:
        enable-cache: true
        python-version: ${{ matrix.python-version }}

    - name: Verify tools 1
      run: |
        uv --version

    - name: Install dependencies
      run: uv sync --frozen --python "${{ matrix.python-version }}" --all-extras

    - name: Verify tools 2
      run: |
        uv run --frozen python --version
        uv run --frozen ruff --version
        uv run --frozen pylint --version

    - name: Analyzing the code with pylint and ruff
      run: |
        uv run --frozen ruff format --check
        uv run --frozen ruff check
        uv run --frozen pylint $(git ls-files '*.py') || true

    - name: Run unit tests
      run: |
        uv run --frozen python -m unittest discover -v -s tests -t . -p "test_*.py"

    - name: Check docs/ is up to date with the source tree
      run: |
        uv run --frozen specmgr docs
        git diff --exit-code -- docs/ || {
          echo "::error::docs/ is out of date -- run 'uv run --frozen specmgr docs' and commit the result (see AGENTS.md)."
          exit 1
        }
