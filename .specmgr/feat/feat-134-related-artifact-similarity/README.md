---
classification: null
created: '2026-09-17 07:40:37.439+02:00'
id: feat-134-related-artifact-similarity
status: review
type: feat
updated: '2026-09-21 18:41:06.000+02:00'
version: 1.0.0
---

# Feature: Embedding-Based Related-Artifact Similarity Engine

## Plan

### Overview

specmgr currently has no way for an agent to discover artifacts that are "similar" or related to a given (or draft) requirement/decision/risk/etc. -- only advisory, unvalidated `## Related Artifacts` bullets on a few domains (that shape is scheduled for format validation in feat-135, issue #135) and one regex-validated (but never existence-checked) SYSRS cross-reference idiom. This feature adds a generic, cross-domain semantic similarity engine backed by local sentence embeddings (offline at inference time; one-time model download on first use), exposed via two new generic MCP tools (mirroring the existing `update`/`set_status`/`delete` dispatch-tool pattern): `find_related` (by existing document id) and `find_similar_text` (by free-form query text, for pre-creation dedup/discovery checks).

### Requirements

- REQ-001: The default embedding model MUST be runnable CPU-only (no GPU dependency, no torch) -- an ONNX-runtime-backed local model via `fastembed` (e.g. `BAAI/bge-small-en-v1.5`) satisfies this. First use requires a one-time model download from the Hub; inference itself is fully local.
- REQ-002: The embedding backend MUST be pluggable behind an `EmbeddingProvider` protocol exposing `embed` (document-side) and `embed_query` (query-side), with the CPU-only backend above as the shipped default, so a different backend can be substituted later without changing tool code.
- REQ-003: The feature MUST support being made unavailable at runtime, either because the optional dependency isn't installed (or the model fails to load, including first-use download failure), or via an explicit opt-out, and MUST NOT rely on the dependency being absent to hide the tools from clients. specmgr's `@mcp.tool()` decorators register tools via introspection at module-import time, independent of whether any runtime dependency the tool body needs is actually installed -- `find_related`/`find_similar_text` will always appear in the tool list once this module is imported by `server.py`. Therefore the embedding import itself MUST be lazy (deferred to first call, not at module import time, so a missing dependency never breaks tool *registration*), and both tools MUST return the same structured "unavailable" result (not raise) whenever either (a) the embedding backend fails to import or to load, or (b) the env var `SPECMGR_SIMILARITY_DISABLED` is present (presence-based, `os.environ.get(name) is not None` -- the repo's existing env-flag convention, `general/resources/config.py`) -- one code path, two triggers, consistent with the project's existing non-raising structured-result precedent (e.g. `set_status`'s `InvalidStatusResult`).
- REQ-004: Computed embeddings MUST be cached in-memory, per process, content-hash validated (mirroring the existing `DocCache` design from feat-107) -- no on-disk persistence.
- REQ-005: The embedding input text for a document is: its title, plus its frontmatter fields excluding bookkeeping-only keys (`id`, `type`, `version`, `created`, `updated`, `status`), plus its raw frontmatter-stripped body text (the same text `raw=True` reads already expose). Across all 12 whole-body domains the only surviving non-bookkeeping frontmatter field is `classification` (verified against every domain's frontmatter subclass). For a document that fails to parse, the input degrades to the file's full raw text (REQ-009).
- REQ-006: `find_related`/`find_similar_text` MUST default to searching every whole-body domain (req, uc, tsk, qa, prb, gol, rsk, dec, sop, feat, vcr, sysrs) and MUST exclude `adr` -- ADR is being removed as an artifact type entirely (see issue #46, "Remove adr artifact type"), so it is not a useful similarity target/source. The default set MUST be derived from the shared `WHOLE_BODY_DOMAINS` registry (REQ-012), which structurally excludes `adr`.
- REQ-007: A new optional dependency extra (name: `similarity`) MUST hold the default embedding backend's dependency (`fastembed`), keeping the base library and the `mcp`/`cli` extras dependency-light; the `dev` extra MUST include `similarity`, and the committed `uv.lock` MUST be regenerated in the same change.
- REQ-008: The embedding cache MUST be wired into the document lifecycle: the generic `delete` tool MUST invalidate the deleted path's entry, and `set_feat_id` MUST move (or invalidate) the old path's entry to the new one -- mirroring feat-107's `DocCache` integration, which this feature claims to follow.
- REQ-009: Error contract: an invalid `type`/`target_types` value or a `top_k` outside 1..100 MUST be rejected with `ValueError` before any filesystem access (path-safety convention); `find_related` with a missing source id MUST raise the domain's own `XNotFoundError` (like `get_<d>`); unparseable candidate documents MUST still appear in results, embedded from their full raw file text, with the `FAILED_TO_PARSE_MARKER` (`<failed to parse>`) title/status convention and `id = None`.
- REQ-010: Long documents MUST NOT be silently truncated at the model's max sequence length (512 tokens for `bge-small-en-v1.5`). Input exceeding the max length MUST be chunked, each chunk embedded, and the document vector computed as the renormalized mean of the chunk vectors (chunk + mean-pool, inside the default provider -- the protocol still returns one vector per input). Queries MUST go through `embed_query`, which applies the backend's retrieval instruction prefix (`bge-small`'s "Represent this sentence for searching relevant passages:"). A safety cap (`_MAX_CHUNKS_PER_DOC`, e.g. 128, evenly sampled above it) MUST bound worst-case per-document cost.
- REQ-011: The server MUST begin embedding the default corpus into the cache in a background thread at startup (lifespan), gated on the extra being importable and `SPECMGR_SIMILARITY_DISABLED` being absent. Warmup MUST NOT block server startup, MUST NOT raise out of startup, and MUST be a no-op when the feature is unavailable.
- REQ-012: A single shared `WHOLE_BODY_DOMAINS` registry in `general/tools` (the 12-domain tuple plus per-domain adapters: base-dir resolver, path iterator, `load_by_id`) MUST be the one source of the domain set; the five existing generic tools (`update`, `set_status`, `set_classification`, `delete`, `validate`) MUST be re-pointed at it alongside the two new similarity tools, so a future domain (e.g. `ac`) is added in one place.

### Acceptance Criteria

- [x] ACC-001: `find_related(type, id, target_types=None, top_k=10, min_score=None)` returns ranked hits `(type, id, title, status, path, score)` across the default (non-adr) domain set, excluding the source document itself; unparseable candidates appear with `id = None` and marker title/status.
- [x] ACC-002: `find_similar_text(query, target_types=None, top_k=10, min_score=None)` returns the same shape of ranked hits for a free-text query.
- [x] ACC-003: With the `similarity` extra not installed, both tools still appear in the registered tool list and return the structured "unavailable" result when called (never raise, never disappear from the tool list).
- [x] ACC-004: Setting `SPECMGR_SIMILARITY_DISABLED` (any value, presence-based) produces the same structured "unavailable" result even when the extra IS installed.
- [x] ACC-005: Repeated calls against unchanged documents reuse cached embeddings (verified via a cache-hit counter/spy in tests) rather than recomputing.
- [x] ACC-006: Changing a document's content invalidates its cached embedding (content-hash mismatch triggers recompute).
- [x] ACC-007: `adr` never appears as a candidate and is rejected as a `type`/`target_types` value.
- [x] ACC-008: A dedicated, real (non-mocked) test against the actual `fastembed` backend exists, gated behind its own pytest marker, and is excluded from the CI matrix; it asserts end-to-end ranking including retrieval of a >512-token document via mid-document content.
- [x] ACC-009: With the extra installed but the model failing to load (e.g. no network for the first-use download), both tools return the same structured "unavailable" result (never raise).
- [x] ACC-010: The `FastEmbedProvider` wrapper is unit-tested in CI with `fastembed.TextEmbedding` monkeypatched at the import boundary -- the gated real-backend test is the only similarity test CI excludes.
- [x] ACC-011: The generic `delete` tool invalidates the deleted document's embedding-cache entry and `set_feat_id` moves/invalidates the renamed document's entry (no stale vector served, no leaked entry).
- [x] ACC-012: Chunker + mean-pool unit tests cover: short input takes the single-embed fast path; long input splits at whitespace boundaries with each chunk within the model max length; the pooled vector is the renormalized mean (verified against a deterministic fake); the `_MAX_CHUNKS_PER_DOC` cap engages by even sampling.
- [x] ACC-013: All five existing generic tools plus both similarity tools derive their domain set from the shared `WHOLE_BODY_DOMAINS` registry; the existing full test suite stays green after the re-pointing.
- [x] ACC-014: At server startup, warmup populates the embedding cache in a background daemon thread when enabled -- startup never blocks on the embedding backend (no model load/download on the startup path) and warmup never raises out of startup (REQ-011); with `SPECMGR_SIMILARITY_DISABLED` set, no thread is started; with the extra missing or the model failing to load, the thread starts and exits immediately without cache writes (reworded after the 2026-09-21 review; the probe-into-thread fix lands in Phase 5, Task 5.2).
- [x] ACC-015: `top_k` outside 1..100, an unknown `type`/`target_types` value, and a missing `find_related` source id are each rejected per REQ-009 (`ValueError` before filesystem access; the domain's `XNotFoundError` for the missing id).

### Scope

#### Included

- `EmbeddingProvider` protocol (`embed` + `embed_query`) plus default `FastEmbedProvider` (lazy import, CPU-only, BGE retrieval instruction prefix for queries, model-load lock for concurrent first calls).
- Chunk + mean-pool long-document strategy inside `FastEmbedProvider` (whitespace-boundary character chunks, single-embed fast path at/below the model max length, `_MAX_CHUNKS_PER_DOC` safety cap).
- Global in-memory, content-hash-validated, process-local embedding cache (DocCache blake2b pattern, innermost-lock ordering rule, provider-native vector storage, `reset()` test-only hook) -- no on-disk persistence.
- Embedding cache lifecycle wiring: generic `delete` invalidation, `set_feat_id` entry move.
- Embedding input extraction (title plus frontmatter minus bookkeeping keys incl. `status` plus raw body; unparseable documents embed their full raw text with marker title/status).
- Candidate enumeration registry: per-domain base-dir resolver + path iterator (the 11 flat domains via `iter_doc_paths`; `feat`'s `<base>/<id>/README.md` folder shape the one bespoke case), plus source resolution via per-domain `load_by_id` and the existing `_path_safety` guards.
- Pure-Python cosine-similarity ranking (dot product on normalized vectors), `top_k` capped at 1..100, optional `min_score`.
- Shared `WHOLE_BODY_DOMAINS` registry in `general/tools/` plus re-pointing of the five existing generic tools (`update`, `set_status`, `set_classification`, `delete`, `validate`).
- `find_related`/`find_similar_text` generic MCP tools in `general/tools/` (one module per tool, matching the `delete.py`/`update.py` convention).
- New `similarity` optional extra in `pyproject.toml` (holding `fastembed`), `dev` extra including it, `uv.lock` regeneration.
- `SPECMGR_SIMILARITY_DISABLED` env var support (presence-based).
- Background embedding warmup in `server.py`'s `_lifespan` (gated, never-raising).
- Unit tests (deterministic fake `EmbeddingProvider`, no real model/no network; `FastEmbedProvider` wrapper test with monkeypatched `fastembed`; warmup tests) plus one gated real-backend test excluded from CI via `pyproject.toml` `addopts`.
- pytest `embedding_model` marker registration + `addopts` exclusion in `[tool.pytest.ini_options]` (covers the CI and pre-commit pytest invocations without editing either).
- `whitelist.py` entries for test-only hooks (cache `reset`, provider seams).
- An ADR documenting this design (pluggable embedding-based semantic similarity, chunk + mean-pool long-document strategy, in-memory-only cache with delete/rename lifecycle wiring, new optional extra, ADR-domain exclusion, registration-vs-availability distinction from REQ-003, background warmup, and the corpus boundary -- the default set includes the dev docs under `.specmgr/feat` via `DEFAULT_FEAT_DIR`, which is intentional) -- this is architecture-level per this repo's own ADR-vs-feature-log guidance.
- Docs: `server.py` docstring, `AGENTS.md` `general/` bullet (plus fixing its stale feat status vocabulary `in-progress` -> `progress` while there), and `specmgr docs` + `specmgr mcp-docs` + `specmgr adr-toc` regeneration.

#### Explicitly Out Of Scope

- Persisting embeddings to disk across process restarts.
- Any structured/validated explicit cross-reference section on req/gol/dec/sop/rsk (a SYSRS-style curated `<TYPE> <uuid>: <title>` list) -- this work is already filed as feat-133-tags-dec-rsk (issue #133, Tags) and feat-135-related-artifacts-risks (issue #135, cross-reference format validation); this feature stays discovery-only (no existence-checking, no link validation).
- Multi-vector retrieval (ColBERT-style per-chunk max-sim) or a longer-context model (e.g. bge-m3, 8192 tokens) -- revisit only if chunk + mean-pool recall proves insufficient in practice.
- Supporting `adr` as a source or target domain.
- Any non-offline (API-based) embedding backend.
- Prompt changes: the `create_*` prompts' advisory near-duplicate `list_<d>` title checks adopting `find_similar_text` is a follow-up issue, not part of this feature.
- The `## Tags` addition to `dec`/`rsk` -- tracked separately in feat-133-tags-dec-rsk (issue #133); this feature's embedding input extraction works generically off raw body text regardless of whether Tags exists on a given domain.

### Dependencies

#### Depends On

- None strictly, though feat-133-tags-dec-rsk (issue #133), if shipped first, gives `dec`/`rsk` an extra structured signal that naturally flows into this feature's generic raw-body embedding input with no extra work required here. feat-135-related-artifacts-risks (issue #135) is adjacent, not a dependency: it format-validates the `## Related Artifacts` sub-lists on req/gol/dec/sop. If it ships first, this feature's Overview wording ("advisory, unvalidated bullets") goes stale and should be reworded.

#### Blocks

- None.

### Design Notes

Provider interface (`general/tools/_embedding.py`): an `EmbeddingProvider` protocol exposing `embed(self, texts: list[str]) -> list[list[float]]` (document-side) and `embed_query(self, texts: list[str]) -> list[list[float]]` (query-side). The default `FastEmbedProvider` wraps `fastembed.TextEmbedding`, constructed lazily via a `get_default_provider()` factory guarded by a load lock (the MCP SDK thread-pools every sync tool call, so concurrent first calls must not double-load the model), never imported at module level, so `server.py`'s unconditional domain-import line never fails even without the `similarity` extra installed.

Long documents (REQ-010): input longer than the model's max sequence (512 tokens for `bge-small`) is split into ~2000-character whitespace-boundary chunks (character-based on purpose -- no private tokenizer API; the backend's own `truncation=True` guards any slight overflow), each chunk is embedded, and the document vector is `normalize(mean(chunk_vectors))` -- renormalizing after the mean keeps cosine == dot-product comparability across documents of different lengths. Input at/below the max length takes a single-embed fast path (every current spec artifact does: 129-246 tokens measured across `docs/req`, `docs/gol`, `docs/sop`, `docs/sysrs`, `docs/tsk`; the documents that chunk are the 5k-40k-token `.specmgr/feat` READMEs). `_MAX_CHUNKS_PER_DOC = 128` with even sampling above it bounds worst-case per-document cost. `embed_query` prepends `bge-small`'s retrieval instruction ("Represent this sentence for searching relevant passages:") and never chunks (queries are short). Note the embedding cost per document is capped at `min(len, 128)` chunks regardless of file size, so first-call latency scales with document count, not total corpus size.

Availability check (REQ-003): a single shared helper, e.g. `_similarity_availability()`, checked first thing inside both tool bodies: if `SPECMGR_SIMILARITY_DISABLED` is present (`os.environ.get(name) is not None`), return the structured unavailable result; else attempt to obtain the default provider (lazy import), and on import/model-load failure (including first-use download failure) return the same structured unavailable result; else proceed with the real ranking logic. The unavailable result carries `{available: false, reason: "disabled" | "backend-unavailable", message: <enablement hint, e.g. install `biz-dfch-specmgr[similarity]` / unset the flag>}`.

Cache (`general/tools/_embedding_cache.py`): a single global instance, keyed by `(domain, resolved path)` to `(content_hash, vector)`, mirroring `DocCache`'s blake2b hash-check-then-recompute pattern but standalone, since a search call spans multiple domains at once, unlike per-domain parsing. `DocCache`'s lock-ordering rule applies: the cache lock guards dict bookkeeping only -- never file reads, never embedding computation -- and is always the innermost lock in any call stack. Vectors are stored in the provider's native arrays (numpy), not converted to Python `list[float]` (~7x memory bloat). `reset()` exists as a test-only hook (needs a `whitelist.py` entry, same precedent as `DocCache.reset`).

Background warmup (REQ-011): `server.py`'s currently no-op `_lifespan` starts a daemon thread at startup that embeds the full default corpus into the cache. Gated on the extra being importable and the flag being absent; every failure is swallowed (logged, and the demand path keeps working). This is safe to background precisely because ONNX Runtime runs its inference in C++ outside the GIL -- unlike the markdown-it/Pydantic parsing that feat-107's ADR (bfd76370) identified as the GIL-holding bottleneck. Rationale: the current default corpus is ~22 short spec docs plus ~52 feat READMEs totaling ~330k tokens (estimate: minutes of CPU, once per process); without warmup, that first call would likely exceed an MCP client's tool timeout even though the server-side work would continue and cache.

Candidate enumeration (REQ-012): the shared `WHOLE_BODY_DOMAINS` registry holds the 12-domain tuple plus, per domain, the base-dir resolver, the path iterator (flat `*.md` via the existing `iter_doc_paths` for the 11 flat domains; `<base>/<id>/README.md` for `feat`), and `load_by_id` -- the same per-domain adapter shape `general/tools/delete.py` already imports at module level. The five existing generic tools' copy-pasted domain sets (`_DELETE_TYPES`, `_VALIDATE_TYPES`, the `update`/`set_status`/`set_classification` `Literal[...]`s) are re-pointed at the registry's tuple.

Embedding input (`general/tools/_similarity_text.py`): `python-frontmatter` (already a base dependency) splits frontmatter from body; title = first H1 (it is also present in the body text -- deliberate double weighting of the title signal, no dedup); frontmatter keys minus `{id, type, version, created, updated, status}` -- across all 12 domains only `classification` survives (verified). Unparseable documents (parse failure of any channel) embed the full raw file text instead, and their result rows carry the `FAILED_TO_PARSE_MARKER` title/status with `id = None` (REQ-009). No per-domain field extraction, keeping the tool domain-agnostic by design.

Ranking: dot product on normalized vectors (== cosine), sorted descending, capped at `top_k` (validated 1..100, the same cap the `list_*` tools apply to `max_results`), optional `min_score` filter (cosine in [-1, 1]; `None` returns up to `top_k` hits regardless of score).

Real-backend test marker: name it after the feature/mechanism, not a generic informal term -- `@pytest.mark.embedding_model` (a real, non-mocked test that loads the actual `fastembed` model and asserts end-to-end ranking behavior, including mid-document retrieval of a long doc). Registered in `pyproject.toml`'s `[tool.pytest.ini_options]` `markers` and excluded from every default pytest invocation via `addopts = '-m "not embedding_model"'` -- one line covering both the CI matrix step and the pre-commit pytest hook without editing either file; developers opt in locally with `-m embedding_model` (first use requires a one-time model download from the Hub, which is why it is excluded from CI's offline-by-default posture).

Open questions/TODOs, not yet decided, to resolve during Phase 3: (2) should the embedding model be configurable (env var to select an alternate `fastembed` model), or is `BAAI/bge-small-en-v1.5` fixed for v1 -- the chunk-size/max-length constants live in the provider and would need updating alongside a model swap, so v1 fixes the model.

(Former open question 1 -- additional/different ranking parameters -- is resolved by the user 2026-09-19: no additional parameters, the ACC-001/ACC-002 signatures are final. See Decisions Made.)

(Former open question 3 -- `status` inclusion in the embedded frontmatter -- is resolved: excluded. See Decisions Made.)

### Related Decisions

- ADR 750842b2-aca4-4649-ba0c-855ec8e1f505 (accepted 2026-09-19): documents the design -- pluggable CPU-only `EmbeddingProvider` protocol, chunk + mean-pool long-document strategy, in-memory-only hash-validated cache with delete/`set_feat_id` lifecycle wiring, new `similarity` extra, `adr`-domain exclusion, registration-vs-availability distinction, background warmup and its GIL rationale, the shared `WHOLE_BODY_DOMAINS` registry, and the corpus boundary (including the dev docs under `.specmgr/feat`).

### Task List

#### Phase 1: Provider + Cache + Availability + Registry

- [x] Task 1.1: Write the ADR for this design (pluggable embedding provider with `embed`/`embed_query`, chunk + mean-pool long-document strategy, in-memory cache with delete/`set_feat_id` lifecycle wiring, new extra, ADR-domain exclusion, registration-vs-availability distinction from REQ-003, background warmup, shared `WHOLE_BODY_DOMAINS` registry, corpus boundary).
- [x] Task 1.2: Add `similarity` extra to `pyproject.toml` with `fastembed`; add it to the `dev` extra; regenerate `uv.lock`.
- [x] Task 1.3: Implement `EmbeddingProvider` protocol (`embed` + `embed_query`) plus `FastEmbedProvider` plus `get_default_provider()` (lazy import, model-load lock).
- [x] Task 1.4: Implement `SPECMGR_SIMILARITY_DISABLED` (presence-based) plus `_similarity_availability()` shared helper and its structured "unavailable" result type (`{available, reason, message}` with enablement hint).
- [x] Task 1.5: Implement the global in-memory embedding cache (blake2b content-hash validation, innermost-lock ordering, native-array vector storage, `reset()` test hook).
- [x] Task 1.6: Extract the shared `WHOLE_BODY_DOMAINS` registry in `general/tools`; re-point `update`/`set_status`/`set_classification`/`delete`/`validate` at it; full suite stays green.

#### Phase 2: Text Extraction + Chunking + Ranking

- [x] Task 2.1: Implement the candidate enumeration registry (per-domain base-dir + path iterator, `feat` folder-shape bespoke) plus source resolution via per-domain `load_by_id` with the existing `_path_safety` guards.
- [x] Task 2.2: Implement embedding-input text extraction (title, frontmatter minus bookkeeping keys incl. `status`, raw body; unparseable -> full raw text + marker fields).
- [x] Task 2.3: Implement the chunker + mean-pool in `FastEmbedProvider` (whitespace-boundary character chunks, single-embed fast path, `_MAX_CHUNKS_PER_DOC` even-sampling cap) plus `embed_query` with the BGE retrieval instruction prefix.
- [x] Task 2.4: Implement pure-Python cosine (dot-on-normalized) ranking with `top_k` 1..100 validation and the `min_score` filter.

#### Phase 3: Tools + Wiring

- [x] Task 3.1: Implement `find_related(type, id, target_types=None, top_k=10, min_score=None)`.
- [x] Task 3.2: Implement `find_similar_text(query, target_types=None, top_k=10, min_score=None)`.
- [x] Task 3.3: Resolve the open question on additional/different ranking parameters (see Design Notes) before finalizing tool signatures.
- [x] Task 3.4: Wire `adr` rejection into both tools' `type`/`target_types` validation (via the shared registry).
- [x] Task 3.5: Implement the error contract (REQ-009): `ValueError` for bad `type`/`target_types`/`top_k` before any filesystem access; the domain's `XNotFoundError` for a missing `find_related` source.
- [x] Task 3.6: Wire the embedding cache into the generic `delete` tool (invalidate) and `set_feat_id` (move/invalidation).
- [x] Task 3.7: Implement the `_lifespan` background warmup (gated on extra present + flag absent, daemon thread, never raising).

#### Phase 4: Tests & Docs

- [x] Task 4.1: Unit tests with a deterministic fake `EmbeddingProvider` (cache hit/miss/invalidation, chunker + mean-pool math, ranking correctness, `target_types` filtering, `top_k`/`min_score`, self-exclusion, `adr` rejection, unavailable-result paths for all three triggers, error contract).
- [x] Task 4.2: `FastEmbedProvider` wrapper unit test with `fastembed.TextEmbedding` monkeypatched at the import boundary (CI-covered).
- [x] Task 4.3: Real-backend test under the `embedding_model` marker (end-to-end ranking incl. mid-document retrieval of a >512-token document); register the marker and the `addopts` exclusion in `pyproject.toml` (must precede the first commit of any marked test).
- [x] Task 4.4: Warmup tests (enabled populates cache in background; flag set / extra missing -> no-op; never raises out of startup).
- [x] Task 4.5: Update `server.py` docstring and `AGENTS.md`'s `general/` bullet (plus the `in-progress` -> `progress` status-vocabulary fix).
- [x] Task 4.6: Regenerate docs via `specmgr docs` + `specmgr mcp-docs` + `specmgr adr-toc`.
- [x] Task 4.7: Add `whitelist.py` entries for the test-only hooks (cache `reset`, provider seams).
- [x] Task 4.8: Final gate: `ruff format --check`, `ruff check`, `vulture src/ whitelist.py --min-confidence 60`, full `pytest -n auto --cov=src`, `specmgr coverage-badge`.

#### Phase 5: Post-review fixes (feat-reviewer pass, 2026-09-21)

- [ ] Task 5.1: Add the missing CHANGELOG `[Unreleased]` `### Added` entry: `find_related`/`find_similar_text` (cross-domain semantic similarity across the whole-body domains, `adr` excluded structurally), the new optional `similarity` extra (`fastembed`/`BAAI/bge-small-en-v1.5`, CPU-only, one-time model download on first use), the presence-based `SPECMGR_SIMILARITY_DISABLED` opt-out, and the structured non-raising `{available, reason, message}` unavailable result -- cite GitHub issue #134, in the style of the feat-29/feat-132/feat-133 entries.
- [ ] Task 5.2: Move the warmup availability probe into the daemon thread (review finding: startup-blocking). `start_similarity_warmup` gates synchronously on the `SPECMGR_SIMILARITY_DISABLED` env flag ONLY (no `_similarity_availability`/model import/load on the startup path -- server startup never blocks on the backend, REQ-011's strict reading); `warmup_similarity_cache` runs `_similarity_availability()` first thing in its thread body (unavailable -> log + return, no cache writes; still never raises). Updates: `tests/general/tools/test__similarity_search.py` (`test_backend_missing_returns_none` becomes: the thread starts, joins clean, and the cache stays empty; the disabled-flag test keeps returning `None`/no thread), the `_similarity_search` module + both functions' docstrings, `server.py`'s `_lifespan` docstring, ADR 750842b2's **Warmup** sub-decision (edited in place), and the ACC-014 test mapping in the Progress section.
- [ ] Task 5.3: Wording fix (review finding): clarify that the Phase 4 entry's "`docs/MCP.md` and `docs/adr/README.md` are unchanged" refers to Phase 4's regeneration being idempotent -- the feature's own diff DOES change both (`docs/MCP.md` gains the two tool sections, 90 -> 92 tools; `docs/adr/README.md` gains the new ADR's TOC entry).
- [ ] Task 5.4: Record the no-change decisions from the review: the `pip install 'biz-dfch-specmgr[similarity]'` enablement hint in `_embedding._similarity_availability` stays (the README documents pip-first installation), and `find_similar_text`'s blank-`query` `AssertionError` stays (the repo's program-invariant convention; MCP clients send strings).
- [ ] Task 5.5: Final gate: `ruff format --check`, `ruff check`, `vulture src/ whitelist.py --min-confidence 60`, full `pytest -n auto --cov=src`, `specmgr docs` + `specmgr mcp-docs` + `specmgr adr-toc` idempotency, `specmgr coverage-badge`. Update the Progress section (Current Status + a new Updates entry with the gate evidence) and bump the frontmatter `updated`. Commit in the phase style: one `fix(general): ...` commit (Task 5.2 code + tests + ADR) and one `docs: ...` commit (Tasks 5.1/5.3/5.4 + this Progress update), both citing `(feat-134 Phase 5, #134)`.

## Progress

### Current Status

**As of 2026-09-21**: **Phases 1-4 complete; Phase 5 (post-review fixes, Tasks 5.1-5.5) in progress; PR #137 open.** The feat-reviewer pass (2026-09-21) found no functional errors -- full suite green at post-merge HEAD, all gates clean, ADR valid, generated docs consistent -- and three actionable findings that Phase 5 fixes: the missing CHANGELOG entry (Task 5.1), the synchronous warmup availability probe that can block server startup on first/air-gapped runs (Task 5.2, the REQ-011 strict-reading gap), and one misleading Phase 4 wording (Task 5.3). The current dev (v0.30.0, feat-133) is merged into the branch (`1510a98`), which also removes the duplicate `.opencode` feat-reviewer permission commits from the PR diff (dev's own `2ec5595`/`ec803f9`/`41cf57b` carry that change); post-merge gates: 3505 passed / 1796 subtests, ruff/vulture clean, docs trio idempotent. Phase 4 (Tests & Docs) shipped the full test suite for the similarity engine: ten new test files plus one shared fixture/helper module under `tests/general/tools/` (`_similarity_helpers.py` -- a deterministic bag-of-words `FakeProvider` over the whole-word `alpha`/`beta`/`gamma` vocabulary so every expected cosine is hand-computable, the `fastembed` import-boundary simulators, and the temp-dir/env-var/cache-reset fixture), mirroring the existing `test_delete.py`/`test__doc_cache.py` conventions. Coverage of the feat-134 `src/` modules went from ~25-46% to **100%** across `_embedding`/`_embedding_cache`/`_similarity_corpus`/`_similarity_ranking`/`_similarity_search`/`_similarity_text`/`_domains`/`find_related`/`find_similar_text` and `server.py` (`_lifespan`). The single real-backend test is gated behind the new `embedding_model` pytest marker and excluded from every default invocation by `pyproject.toml`'s `addopts = '-m "not embedding_model"'` (registered before the first run; the marker/addopts is the only CI-excluded similarity test, ACC-010). **Final gate (Task 4.8) all green:** `ruff format --check` (1716 files) and `ruff check` clean; `vulture src/ whitelist.py --min-confidence 60` clean (the Phase 1 whitelist entries `iter_paths`/`embed_query`/`_similarity_availability` were stale and removed -- they are now called from `src/`; `documents`/`available`/`reason` kept); full suite **3447 passed, 1787 subtests** (up from 3307 passed / 1750 subtests pre-Phase-4: +140 tests, the 2 marked real-backend tests excluded by `addopts`); `pytest -m embedding_model` **2 passed in 3.26s** (the bge-small model is in the local HF cache -- mid-document retrieval of a >512-token doc confirmed on the real backend); `specmgr docs` + `specmgr mcp-docs` + `specmgr adr-toc` regenerated and idempotent (`docs/api/biz.dfch.specmgr.server.md` gained the two tools, `docs/GENERATED.md` test count 356 -> 366; `docs/MCP.md` and `docs/adr/README.md` unchanged); `specmgr coverage-badge` updated **96% -> 99%**. `server.py`'s module docstring and `AGENTS.md`'s `general/` bullet now enumerate `find_related`/`find_similar_text` (Task 4.5), and `AGENTS.md`'s stale feat status vocabulary is fixed (`in-progress` -> `progress`). The feat frontmatter `status` is now `progress` (a valid feat-model value -- the model rejects `in-progress`); it is not `done` because PR #137 is not yet merged.

### Blockers

- None.

### Updates

<!-- Newest entry first -- prepend new entries directly below this comment. -->

#### 2026-09-21 16:41:06.000Z - Review complete (feat-reviewer); Phase 5 planned; current dev merged in

The feat-reviewer pass over the feature work (`d5d4521..cebf830` plus the post-merge tree) found **no functional errors**: the full suite was green at post-merge HEAD (3494 passed / 1796 subtests before the dev merge below), `ruff format`/`ruff check`/`vulture --min-confidence 60` clean, `validate_adr` on ADR 750842b2 passing, generated docs consistent (`docs/GENERATED.md`'s test-file count matched disk, `docs/MCP.md` carried both new tools at 92 total), the 2 `embedding_model` real-backend tests correctly deselected from default runs, and the merge-time adaptation of `_similarity_helpers.py`'s DEC template to feat-29's now-mandatory `## Roles and Responsibilities`/`## Source` sections verified. Three actionable findings, now planned as Phase 5: (1) **missing CHANGELOG entry** -- every user-visible feature in this repo adds `[Unreleased]` entries (feat-29: +42 lines, feat-132: +13, feat-133: +35); feat-134 ships two new MCP tools and a new `similarity` extra and had none (Task 5.1); (2) **synchronous startup probe** -- `start_similarity_warmup` ran `_similarity_availability()` (hence `get_default_provider()`'s eager model load, including the first-use download) on the lifespan's startup path before the `yield`; corpus embedding was backgrounded as designed, but the probe itself could delay server readiness on a first/air-gapped run (Task 5.2, REQ-011's "MUST NOT block server startup" strict reading; ACC-014 reworded accordingly); (3) **misleading wording** -- the Phase 4 entry's "`docs/MCP.md` and `docs/adr/README.md` are unchanged" describes Phase 4's idempotent re-run, not the feature's own diff, which changes both (Task 5.3). No-change decisions recorded (Task 5.4): the `pip install 'biz-dfch-specmgr[similarity]'` enablement hint (the README is pip-first) and `find_similar_text`'s blank-`query` `AssertionError` (program-invariant convention). Also: the two `.opencode` feat-reviewer permission commits on this branch (`e8a9730`/`cdc18be`) proved to be duplicates of commits already on dev (`2ec5595`/`ec803f9`, plus the follow-up `41cf57b`), so the current dev tip (v0.30.0 incl. feat-133) was merged in (`1510a98`); the `.opencode` file resolved to dev's version and those changes no longer appear in the PR diff. Post-merge gates: **3505 passed / 1796 subtests**, `ruff format --check` (1726 files)/`ruff check` clean, `vulture` clean, `specmgr docs` + `specmgr mcp-docs` + `specmgr adr-toc` idempotent on the merged tree.

#### 2026-09-20 08:41:40.538Z - Phase 4 complete (Tasks 4.1-4.8): full test suite, docs, and the green final gate

Phase 4 shipped, per the accepted ADR 750842b2 and this plan. (1) **Task 4.1/4.2** -- ten new test files under `tests/general/tools/` (mirroring the `test_delete.py`/`test__doc_cache.py` layout: one file per source module, `test__<module>.py` for the private `_` modules) plus a shared `_similarity_helpers.py` (the `tests/adr/tools/_helpers.py` precedent). The helper's `FakeProvider` is a deterministic, call-counting bag-of-*whole-words* embedding over the fixed `alpha`/`beta`/`gamma` vocabulary, L2-normalized, so every expected cosine in the suite is hand-computable (no model, no network, no numpy); the seed helpers keep vocabulary words out of every structural section (title/EARS-statement/`## Characteristics`/`## Level`/`## Source`), so a document's vector is determined solely by its free-text section. Covered: the chunker + renormalized mean-pool + even-sampling math against hand-computed values (`test__embedding.py`; e.g. `_mean_pool([[1,0],[0,1]])` -> `[1/sqrt(2), 1/sqrt(2)]`), the `FastEmbedProvider` wrapper with `fastembed.TextEmbedding` monkeypatched at the import boundary (ACC-010), `get_default_provider`'s lazy-import + load-failure seams, `_similarity_availability`'s three triggers, the content-hash cache hit/miss/invalidation/move/reset, `WHOLE_BODY_DOMAINS` equality + `whole_body_domain("adr")`/`("bogus")` `ValueError`s, candidate enumeration/source-resolution/marker rows, the cosine ranking + `top_k`/`min_score` bounds, and the full `find_related`/`find_similar_text` tool contract (ranked hits, self-exclusion, marker rows, the default non-`adr` set, `target_types` filtering, cache reuse/invalidation, the `delete`/`set_feat_id` lifecycle wiring, and the error contract with a nonexistent-`SPECMGR_DOCS_DIR` proof that the `ValueError`s fire before any filesystem access). (2) **Task 4.3** -- the single real-backend test `test_similarity_real_backend.py` under `@pytest.mark.embedding_model`: self-contained temp fixture docs, end-to-end mid-document retrieval of a >512-token requirement (the distinctive phrase sits in chunk 2 of 6, past the 2000-char fast-path boundary) plus the two Phase 3 real-backend assertions (native `numpy` arrays flow through the pure ranking without an `AssertionError`; the hit `score` is a plain Python `float`). The `embedding_model` marker and `addopts = '-m "not embedding_model"'` were registered in `pyproject.toml`'s `[tool.pytest.ini_options]` *before* the marked test's first run (verified: a default run deselects the 2 marked tests; an explicit `-m embedding_model` selects exactly them). (3) **Task 4.4** -- warmup tests: enabled -> a daemon thread named `specmgr-similarity-warmup` populates the cache without blocking; flag set / backend missing -> `start_similarity_warmup()` returns `None` and starts no thread; `warmup_similarity_cache` swallows a mid-corpus provider failure (leaving the cache partially warm) and never raises; `server._lifespan` completes without raising in both cases. (4) **Task 4.5** -- `server.py`'s module docstring and `AGENTS.md`'s `general/` Status bullet now enumerate `find_related`/`find_similar_text` in the existing style; `AGENTS.md`'s stale feat status vocabulary is fixed (`in-progress` -> `progress`, the feat model's actual vocabulary). (5) **Task 4.6** -- `specmgr docs` + `specmgr mcp-docs` + `specmgr adr-toc` regenerated (idempotent on re-run): `docs/api/biz.dfch.specmgr.server.md` gained the two tools, `docs/GENERATED.md`'s test-file count went 356 -> 366, `docs/MCP.md` and `docs/adr/README.md` are unchanged (the tool docstrings did not change). (6) **Task 4.7** -- `whitelist.py` made accurate: the stale Phase 1 entries `iter_paths`/`embed_query`/`_similarity_availability` (now called from `src/`) were removed and the comments refreshed; `documents`/`available`/`reason` are still required (a protocol-stub argument and the two `SimilarityUnavailableResult` fields read only via (de)serialization). No entries were needed for the test-only `reset_embedding_cache`/`reset_default_provider` seams (they are `__all__`-exported, which vulture treats as used). **Test -> ACC map (all 15 ACCs):** ACC-001 `test_find_related.py::TestFindRelatedRanking` (+ `test__similarity_search.py::TestToSimilarityHit`); ACC-002 `test_find_similar_text.py::TestFindSimilarTextRanking`; ACC-003 `test_find_related.py::TestFindRelatedRegistration` + `test_find_similar_text.py::TestFindSimilarTextRegistration` + `test_find_related.py::TestFindRelatedAvailability::test_backend_missing_returns_unavailable_and_never_raises` + `test_find_similar_text.py::TestFindSimilarTextAvailability::test_backend_missing_returns_unavailable_and_never_raises` + `test__embedding.py::TestGetDefaultProvider::test_import_failure_propagates_out_of_get_default_provider` + `test__embedding.py::TestSimilarityAvailability::test_backend_unavailable_when_extra_is_missing`; ACC-004 `test_find_related.py::TestFindRelatedAvailability::test_disabled_flag_returns_unavailable_even_with_the_extra_installed` + `test_find_similar_text.py::TestFindSimilarTextAvailability::test_disabled_flag_returns_unavailable` + `test__embedding.py::TestSimilarityAvailability::test_disabled_flag_is_presence_based_not_truthy`; ACC-005 `test_find_related.py::TestFindRelatedCaching::test_repeated_call_reuses_cached_embeddings` + `test__embedding_cache.py::TestRead::test_miss_then_hit_returns_same_vector_without_reembedding`; ACC-006 `test_find_related.py::TestFindRelatedCaching::test_content_change_re_embeds_exactly_the_changed_document` + `test__embedding_cache.py::TestRead::test_content_change_recomputes`; ACC-007 `test__domains.py` (tuple/lookup) + `test__similarity_corpus.py::TestIterCandidatePaths`/`TestIterCandidatePathsRejection::test_adr_target_rejected_before_filesystem_access`/`TestResolveSource::test_adr_source_rejected` + `test_find_related.py::TestFindRelatedRanking::test_default_target_set_spans_whole_body_domains_and_excludes_adr`/`TestFindRelatedErrorContract::test_adr_rejected_as_source_type`/`test_adr_rejected_as_target_type` + `test_find_similar_text.py::TestFindSimilarTextErrorContract::test_adr_target_rejected_before_filesystem_access`; ACC-008 `test_similarity_real_backend.py::TestRealBackendSimilarity` (both `@pytest.mark.embedding_model` tests); ACC-009 `test_find_related.py::TestFindRelatedAvailability::test_model_load_failure_returns_unavailable_and_never_raises` + `test_find_similar_text.py::TestFindSimilarTextAvailability::test_model_load_failure_returns_unavailable_and_never_raises` + `test__embedding.py::TestGetDefaultProvider::test_model_load_failure_propagates_out_of_get_default_provider` + `test__embedding.py::TestSimilarityAvailability::test_backend_unavailable_when_the_model_fails_to_load`; ACC-010 `test__embedding.py::TestFastEmbedProviderWrapper` (+ `TestGetDefaultProvider::test_constructs_a_fastembed_provider_cached_for_the_process`); ACC-011 `test_find_related.py::TestFindRelatedCacheLifecycle` (+ `test__embedding_cache.py::TestInvalidate`/`TestMove`); ACC-012 `test__embedding.py::TestChunkText`/`TestEvenlySample`/`TestMeanPool` (+ `test__similarity_ranking.py`/`test__similarity_text.py`); ACC-013 `test__domains.py` (+ the full-suite-green evidence below); ACC-014 `test__similarity_search.py::TestStartSimilarityWarmup`/`TestWarmupSimilarityCache`/`TestServerLifespan`; ACC-015 `test__similarity_ranking.py::TestValidateRankingBounds` + `test_find_related.py::TestFindRelatedErrorContract` + `test_find_similar_text.py::TestFindSimilarTextErrorContract` (+ `test__similarity_corpus.py::TestIterCandidatePathsRejection`/`TestResolveSource`). **Final gate (Task 4.8) evidence:** `ruff format --check` 1716 files clean; `ruff check` clean; `vulture src/ whitelist.py --min-confidence 60` clean; full `pytest -n auto --cov=src` **3447 passed, 1787 subtests** (pre-Phase-4 baseline 3307 passed / 1750 subtests; +140 tests, the 2 marked real-backend tests excluded by `addopts`); `pytest -m embedding_model` **2 passed in 3.26s**; docs trio regenerated + idempotent; `specmgr coverage-badge` **96% -> 99%**. No Phase 1-3 `src/` behavior was changed; the tests confirmed no bug in the committed code.
#### 2026-09-19 17:56:52.758Z - Phase 3 complete (Tasks 3.1-3.7): find_related / find_similar_text + cache wiring + warmup

Phase 3 shipped, per the accepted ADR 750842b2 and this plan. (1) **Task 3.1/3.2**: `general/tools/find_related.py` and `general/tools/find_similar_text.py` -- one module per tool (the `delete.py`/`update.py` convention), each `@mcp.tool()`-decorated and imported from `general/tools/__init__.py`'s own side-effect import list (kept alphabetical; without it the tools would silently never register). Both bodies check `_similarity_availability()` first thing (REQ-003's normative order -- a disabled/backend-missing environment returns the structured `SimilarityUnavailableResult` even for bad arguments), then validate every argument before any filesystem access (Task 3.5: `top_k`/`min_score` via the newly factored-out `_similarity_ranking.validate_ranking_bounds`, `target_types` via `iter_candidate_paths`' up-front registry check, `type`/`id` via `resolve_source`'s `whole_body_domain`/`validate_id` pair -- Task 3.4's `adr`/unknown rejection is structural, no per-tool check), then rank: `find_related` embeds the source through `read_embedding` and excludes it from the hits by resolved-path comparison (ACC-001); `find_similar_text` embeds the query via `embed_query` (ACC-002); a missing source propagates the domain's own `XNotFoundError` (smoke: `ReqNotFoundError`, a `LookupError`). Hits are the new `SimilarityHit` pydantic row (`general/models/similarity_hit.py`, re-exported from `general/models/__init__.py`): `{type, id, title, status, path, score}`, `list[SimilarityHit]` on success, unparseable candidates as marker rows with `id = None`. Task 3.3 was resolved by the user before implementation (no additional ranking parameters; signatures final per ACC-001/ACC-002) -- see Decisions Made. (2) **Shared helper**: `general/tools/_similarity_search.py` -- `CollectedCandidate` + `make_embed_fn` (the TOCTOU-safe `embed_fn` closure that re-derives the embedding text from the cache's own read, never the tool's separately-read copy) + `collect_candidates` (one per-candidate loop for both tools and the warmup; a candidate file vanishing mid-walk is skipped, the `_listing` `silent_skip_types` precedent) + `to_similarity_hit`, plus the warmup pair `warmup_similarity_cache` (blocking, never raising, full default corpus through the same `collect_candidates` path) and `start_similarity_warmup` (`_similarity_availability()` gate first; a structured result starts no thread at all). (3) **Task 3.7**: `server.py`'s `_lifespan` (formerly a placeholder) now calls `start_similarity_warmup()` before its `yield` -- function-level import (a module-level one would cycle: `general/tools/__init__` imports the tool modules, which import `server` for the decorator); a named (`specmgr-similarity-warmup`) daemon thread, no join, failures logged and swallowed inside the thread (repo's first `logging.getLogger(__name__)` usage -- no existing pattern to mirror). (4) **Task 3.6**: `delete.py` now calls `invalidate_embedding_cache(domain, path)` immediately after every domain adapter's own `invalidate_<d>_cache(path)` (only after a successful `unlink`/`rmtree`; the `feat` case its `README.md` path, the cache's own key), and `set_feat_id.py` calls `move_embedding_cache("feat", old, new)` immediately after its own `move_feat_cache_entry` (only after `write_feat_file` succeeds) -- feat-107's placement precedent, mirrored exactly. (5) **Two real-backend-only runtime fixes** surfaced by the Phase 3 smoke (invisible to list-returning fakes): `numpy.ndarray` is **not** a virtual subclass of `collections.abc.Sequence` (`isinstance` reports `False` despite the array being indexable and length-carrying), so `_similarity_ranking._assert_vector` now checks structurally (reject `str`/`bytes`; require `__getitem__` + `__len__`); and `dot_product` accumulates in the array's dtype, so scores are `numpy.float32` with the default provider -- `to_similarity_hit` normalizes via `float(score)` so the hit row always carries a plain Python float. (6) **Gate evidence**: `ruff format --check` (1701 files already formatted), `ruff check` (all checks passed), `vulture src/ whitelist.py --min-confidence 60` (no findings -- no new whitelist entries needed; the `SimilarityHit` field names are all already in use across `src/`), `pytest -n auto --cov=src` (3307 passed), `specmgr docs` (regenerated; 4 new `docs/api` module pages), `specmgr mcp-docs` (docs/MCP.md 90 -> 92 tools; both new tools' parameter tables, the `type` enum the registry-derived domain set), `specmgr adr-toc` (no-op), `specmgr coverage-badge` (97% -> 96%, the new tool code is tested in Phase 4). (7) **Smoke evidence** (a 65-check script against a temp corpus with a deterministic fake provider, plus a real-backend run on the 3 small `tsk` docs): disabled flag -> structured result with zero backend/fs work even for `top_k=0` + `target_types=["adr"]`; backend-unavailable (simulated) -> the same structured result; 9 `ValueError` cases (bad `type` `adr`/`bogus`, bad id format, path-injection id, bad `target_types` x2, `top_k` 0/101, `min_score` 2.0) all raised with a nonexistent docs dir (no fs access); `find_related` -> near-duplicate top hit, source excluded, marker row for the broken candidate, cross-domain hits, descending scores, exactly one embed per document on a cold cache and zero on the repeat call; `find_similar_text` -> query-side `embed_query` used, the title's own document on top; `delete` -> the `(req, path)` embedding entry gone, others intact; `set_feat_id` -> old entry gone, new entry present, exactly one self-healing re-embed; warmup -> flag set: no thread; enabled: daemon thread started, start call returned while the warmup was still running, cache fully warm afterwards (a spy `embed_fn` was never called post-warmup), mid-warmup failure logged + swallowed with the cache left 2/5 partial. Real backend: `find_similar_text("markdown list item numbering and extent calculation")` ranked the matching `tsk` doc first (0.776); `find_related` on a `tsk` source self-excluded it and ranked the other two (0.887/0.783); a repeat call was identical and cache-served.

#### 2026-09-19 13:27:37.978Z - Phase 2 complete (Tasks 2.1-2.4): text extraction + chunking + ranking

Phase 2 shipped, per the accepted ADR 750842b2 and this plan. (1) **Task 2.1**: `general/tools/_similarity_corpus.py` -- the candidate enumeration + source resolution layer on top of the Phase 1 registry. `iter_candidate_paths(target_types=None)` yields every `(domain, path)` candidate pair (default: the full registry set; caller-supplied subset order-preserving and de-duplicated; every name validated via `whole_body_domain` up front, so `adr`/unknown names are a `ValueError` before any filesystem access); `resolve_source(type_, id_)` validates `type_` via the registry and `id_` via the existing `_path_safety.validate_id` (both `ValueError` before any filesystem access), resolves through the domain's own `load_by_id` (the domain's own `XNotFoundError` propagates unchanged for a missing -- and, by id-lookup-skip design, unparseable -- source), and confines the resolved path via `assert_within`; `is_parseable(domain, text)` + `candidate_similarity_text(domain, text)` run the domain's own pure-text parser over the candidate's exact text (the REQ-009 "parse failure of any channel" decision -- `DEFAULT_ERROR_TYPES`: `AssertionError`/`pydantic.ValidationError`/`yaml.YAMLError`) and hand the text to Task 2.2's extraction. To make that possible, `WholeBodyDomain` gained a fourth adapter field, `parse_text` (each domain's own `parse_<d>(text)` from its `models` package; `_domains.py` extended in place -- see Decisions Made). (2) **Task 2.2**: `general/tools/_similarity_text.py` -- domain-agnostic embedding-input extraction: the `SimilarityText` dataclass (`embedding_text`/`title`/`id_`/`status`), `first_h1` (first level-1 ATX heading of the raw frontmatter-stripped body -- the deliberate double-weighted title signal, no dedup), `compose_embedding_text` (title + one `key: value` line per non-bookkeeping, non-`None` frontmatter field + raw body, `python-frontmatter` base dependency), and `failed_similarity_text` (unparseable documents: full raw text as the embedding input, the `FAILED_TO_PARSE_MARKER` title/status **reused from `_listing`, not redefined**, `id = None`). `BOOKKEEPING_FRONTMATTER_KEYS = {id, type, version, created, updated, status}`; across all domains only `classification` survives. Row `id`/`status` come from the validated domain frontmatter (domain defaults applied, exactly what `list_<d>` surfaces), so a blank `status` reads `planning` for feat / `open` for rsk / `draft` elsewhere. (3) **Task 2.3**: `FastEmbedProvider` in `general/tools/_embedding.py` now implements the full REQ-010 strategy without any public-surface change: `embed` takes the single-embed fast path for input at/below **`_CHUNK_SIZE = 2000` characters** (the new, documented fast-path threshold -- the ADR's own ~2000-char chunk size, one constant with two roles; ~2000 chars ≈ `bge-small`'s 512-token max at ~4 chars/token English), and above it splits via `_chunk_text` (greedy whitespace-boundary character chunks, at most `_CHUNK_SIZE` chars, never splitting a word, no private tokenizer API), caps via `_evenly_sample` (`_MAX_CHUNKS_PER_DOC = 128`, first + last kept, interior at equal spacing), embeds **all** inputs' backend strings in one batched `TextEmbedding.embed` call, and pools chunked inputs as `normalize(mean(chunk_vectors))` via the pure-Python `_mean_pool` (a plain `list[float]` -- the numpy-free consequence is documented; the fast path keeps the backend's own arrays, and the cache stores provider output unconverted per its own contract). `embed_query` prepends `bge-small`'s retrieval instruction (`_QUERY_INSTRUCTION = "Represent this sentence for searching relevant passages:"`, model-card `{instruction} {query}` shape) and never chunks, even for long queries. (4) **Task 2.4**: `general/tools/_similarity_ranking.py` -- pure-Python (numpy-free) `dot_product` + `rank_candidates(query_vector, candidates, *, top_k=10, min_score=None)`: dot product on the protocol's normalized vectors (== cosine, no re-normalization -- that would hide a broken provider), stable descending sort (equal scores keep the deterministic corpus-enumeration input order), **inclusive** `min_score` filter validated to [-1, 1], `top_k` validated 1..100 (`MIN_TOP_K`/`MAX_TOP_K`/`DEFAULT_TOP_K` constants) -- all `ValueError`s raised before any scoring (the path-safety convention, REQ-009/ACC-015). Gate: `ruff format --check`/`ruff check` green, `vulture` clean (no whitelist entries needed -- everything new is `__all__`-exported or used), full suite 3307 passed, `specmgr docs` regenerated (3 new `docs/api` pages, `_domains`/`_embedding` pages updated), `specmgr mcp-docs` + `specmgr adr-toc` verified byte-identical no-ops (no new MCP tools/ADRs this phase), `specmgr coverage-badge` updated 98% -> 97% (the new code is untested until Phase 4). No Phase 4 tests written (deferred by the plan), no Phase 3 work started.

#### 2026-09-19 10:52:35.766Z - Phase 1 complete (Tasks 1.2-1.6): provider + cache + availability + registry

Phase 1 shipped, per the accepted ADR 750842b2 and this plan. (1) **Task 1.2**: new `similarity` extra holding `fastembed>=0.5.0` (lock resolves 0.8.0 + onnxruntime/tokenizers/numpy tree); the `dev` extra now reads `biz-dfch-specmgr[cli,mcp,test,similarity]`; `uv.lock` regenerated. (2) **Task 1.3**: `general/tools/_embedding.py` -- the `EmbeddingProvider` protocol (`embed`/`embed_query`, batch list-in/list-out, one vector per input), `FastEmbedProvider` (pure wrapper over an injected `fastembed.TextEmbedding`; Phase 1 calls `.embed` directly, the Phase 2.3 chunk + mean-pool and BGE query prefix slot into these two methods without a surface change), `get_default_provider()` (lazy `import fastembed` + eager model load under a double-checked load lock; `fastembed` is never imported at module level, so `server.py`'s unconditional import line stays safe on a base/`mcp`-only install), plus a test-only `reset_default_provider()` seam for ACC-010. (3) **Task 1.4**: presence-based `SPECMGR_SIMILARITY_DISABLED` (`SIMILARITY_DISABLED_ENV_VAR`, checked as `os.environ.get(name) is not None`), the shared `_similarity_availability()` helper (disabled checked first, then provider acquisition; `Exception` on import/model-load -- including first-use download failure -- is the backend-unavailable trigger), and the structured, non-raising `SimilarityUnavailableResult` pydantic model (`{available, reason, message}` with per-reason enablement hints) in `general/models/similarity_unavailable.py`, mirroring the `InvalidStatusResult` precedent. (4) **Task 1.5**: `general/tools/_embedding_cache.py` -- one global `EmbeddingCache` keyed `(domain, resolved path) -> (blake2b digest, vector)`, mirroring `DocCache`'s hash-check-then-recompute pattern including the Phase 6 TOCTOU closure (the exact text read and hashed is the text handed to `embed_fn` on a miss); the lock guards dict bookkeeping only and is always innermost; vectors stored in the provider's native arrays (numpy float32), returned as-is on hit (read-only convention documented); public surface `read_embedding`/`invalidate_embedding_cache`/`move_embedding_cache` (the Phase 3.6 delete/`set_feat_id` wiring targets) + `reset_embedding_cache()` (test-only); the module imports only stdlib + `_embedding`, so it stays importable without the `similarity` extra. (5) **Task 1.6**: `general/tools/_domains.py` -- the shared `WHOLE_BODY_DOMAINS` registry (the 12-domain tuple, per-domain `WholeBodyDomain` adapters: base-dir resolver + path iterator (`iter_feat_paths` the one `feat`-bespoke case) + `load_by_id`, an import-time invariant asserting the mapping and tuple stay in sync, `whole_body_domain()` lookup with the path-safety-convention `ValueError`, and the derived `WholeBodyType`/`WholeBodyOrAdrType` `Literal` aliases); all five existing generic tools' `type` parameter literals re-pointed at the derived aliases (`set_status` at the +`adr` one), and the two unused copy-pasted tuples (`_DELETE_TYPES`, `_VALIDATE_TYPES`) deleted; `validate`'s unknown-type error message now joins `WHOLE_BODY_DOMAINS` (byte-identical text). **Gate evidence**: `ruff format --check` (1687 files already formatted), `ruff check` (all passed), `vulture src/ whitelist.py --min-confidence 60` (clean -- with early Task 4.7 whitelist entries for the not-yet-called Phase 3 seams: `embed_query`, `_similarity_availability`, `documents`, `available`, `reason`, `iter_paths`), `pytest -n auto --cov=src` (3307 passed), `specmgr docs` (regenerated; 4 new API pages + re-pointed tool pages), `specmgr adr-toc` (no-op), `specmgr mcp-docs` (no-op: `docs/MCP.md` byte-identical, proving the derived `Literal` aliases generate the same MCP schema as the former inline literals). Smoke-verified at runtime: disabled flag -> `reason="disabled"` result with no backend load; blocked `fastembed` import -> `reason="backend-unavailable"` result; real model (first-use download occurred during smoke testing and is now cached) -> 384-dim float32 native arrays, cached provider instance, cache hit/miss/invalidate/move/reset semantics.

#### 2026-09-19 08:25:17.270Z - ADR written and accepted (Task 1.1) ahead of Phase 1

At the user's request, the design ADR was written and accepted before implementation starts (ADR 750842b2-aca4-4649-ba0c-855ec8e1f505, `decision-makers: dfch`). It documents the full approved design: pluggable CPU-only provider protocol, chunk + mean-pool long-document strategy, hash-validated in-memory vector cache with `delete`/`set_feat_id` lifecycle wiring, presence-based opt-out flag, shared `WHOLE_BODY_DOMAINS` registry, background warmup (GIL rationale), the `adr` exclusion, and the corpus boundary. PR #137 opened against `dev`. Phase 1 implementation (Tasks 1.2-1.6) is deliberately not started here; it will be driven by the PhaseOrchestrator in a new session.

#### 2026-09-18 15:29:13.994Z - Plan refined after internal review

Reviewed the plan against the codebase and the sibling features (feat-133, feat-135). Changes: (1) long documents are now chunked + mean-pooled in the default provider instead of silently truncated at 512 tokens, with a query-side `embed_query` method carrying the BGE retrieval instruction, plus a gated `_MAX_CHUNKS_PER_DOC` cap; (2) background embedding warmup in `_lifespan` so the full-corpus first call (~330k tokens across the feat READMEs, estimated minutes on CPU) never blocks a tool call or exceeds client timeouts -- safe to background because ONNX Runtime inference runs outside the GIL; (3) a shared `WHOLE_BODY_DOMAINS` registry in `general/tools` re-pointed at by all five existing generic tools plus both new ones; (4) embedding-cache lifecycle wiring into generic `delete` (invalidate) and `set_feat_id` (move); (5) explicit error contract (`ValueError` for bad `type`/`target_types`/`top_k` bounds before filesystem access, domain `XNotFoundError` for missing sources, unparseable candidates included with marker fields); (6) `status` excluded from the embedded frontmatter (resolves former open question 3); (7) `top_k` capped at 1..100; (8) CI exclusion of the real-backend test via `addopts` in `pyproject.toml` instead of editing ci.yml/pre-commit; (9) hit shape drops redundant `ref`, adds `status`; (10) `dev` extra gains `similarity`, `uv.lock` regenerated, `whitelist.py` entries planned; (11) doc regeneration expanded to `specmgr docs` + `specmgr mcp-docs` + `specmgr adr-toc`; (12) feat-135 cross-referenced as adjacent work. Requirements grew from 7 to 12, acceptance criteria from 8 to 15.

#### 2026-09-17 00:00:00.000Z - Created

Feature created from GitHub issue #134 to track the embedding-based related-artifact similarity engine, split out of a broader related-artifact discovery request alongside the smaller, independent feat-133-tags-dec-rsk (issue #133).

### Decisions Made

<!-- Newest entry first -- prepend new entries directly below this comment. -->

#### 2026-09-20 08:41:40.538Z - Phase 4 test-architecture choices beyond the plan (deterministic fake design, provider injection point, import-boundary simulators, registration assertion, the unparseable-fixture channel, test layout/naming)

Six test-architecture decisions the plan left open, settled during Phase 4: (1) **The deterministic fake is a bag-of-*whole-words* vector over a fixed 3-word vocabulary** (`alpha`/`beta`/`gamma`), L2-normalized -- not a hash-based or positional fake -- so every expected cosine is hand-computable (e.g. `2/sqrt(5)`, `1/sqrt(2)`, `1/sqrt(6)`) and the ranking tests assert exact values rather than just ordering. The seed helpers deliberately keep vocabulary words out of every structural section (the title, the EARS statement, and the `## Characteristics`/`## Level`/`## Source` words), so a document's vector is determined solely by the free-text section; the query-based tests (`find_similar_text`) use vocabulary-neutral titles because the title is *deliberately double-weighted* in the embedding text (it appears both as its own line and inside the body), which would otherwise skew the hand-computed scores. (2) **The fake provider is injected by setting `_embedding._default_provider` directly** (the one shared `get_default_provider` reads that module global, so both `_similarity_availability` and every tool's own reference observe it) rather than monkeypatching `get_default_provider` (which would require patching each tool's already-bound import); `reset_default_provider()` runs in `setUp`/`tearDown` so the process-level singleton never leaks a fake (or the real model) across tests. (3) **The `fastembed` import boundary is simulated two ways**: a `sys.meta_path` finder whose `find_spec` raises `ImportError` for `fastembed*` (the missing-extra case, ACC-003 -- Python 3.13 removed the legacy `find_module` hook) and a stub `sys.modules["fastembed"]` whose `TextEmbedding` constructor raises (the model-load-failure case, ACC-009); both save/restore `sys.modules` on exit. (4) **ACC-003's registration assertion introspects the live `mcp` server** via `asyncio.run(mcp.list_tools())` (the `test_delete.py` registration-smoke precedent), asserting both tools are present and that `find_related`'s `type` enum is the registry-derived 12-domain set with `adr` absent. (5) **The unparseable-fixture channel is malformed YAML frontmatter** (an unterminated flow sequence -> `yaml.YAMLError` out of `parse_req`), *not* a corrupted frontmatter `id`: the base `MarkdownFrontmatter.id` is a free-form `str | None` with no UUID validator, so `id: not-a-uuid` parses fine and would not produce a marker row (verified empirically); the malformed-YAML channel is the repo-proven unparseable trigger (`tests/req/tools/test_doc_cache_wiring.py`). (6) **The test layout mirrors the existing `general/tools` tests** -- one file per source module (`test__<module>.py` for the private `_` modules, `test_<tool>.py` for the public tools) plus a shared `_similarity_helpers.py` (the `tests/adr/tools/_helpers.py` precedent), under `tests/general/tools/` (the real test root is `tests/`, mirroring `src/` -- not `tests/biz/...`).
#### 2026-09-19 17:56:52.758Z - Task 3.3 resolved by the user (FINAL): no additional ranking parameters -- the ACC-001/ACC-002 signatures are final

Resolves former Design Notes open question 1 before the tool signatures were frozen: **no additional or different ranking parameters** beyond `target_types`/`top_k`/`min_score`. `find_related(type, id, target_types=None, top_k=10, min_score=None)` and `find_similar_text(query, target_types=None, top_k=10, min_score=None)` are the final signatures (exactly as ACC-001/ACC-002 pin them). Specifically: NO per-domain result cap, NO "exclude these ids" list, and the tools are NOT pageable in the `list_*`/`PagedResult` sense (no `offset`/`total`/`truncated`) -- `top_k` (default 10, validated 1..100, the same cap the `list_*` tools apply to `max_results`) is the only result-count control; a client that wants more results raises `top_k`. (Score normalization was already settled: dot product on normalized vectors.)

#### 2026-09-19 17:56:52.758Z - Phase 3 layout choices beyond what the ADR/plan fixes (hit-row model, shared helper module + signature, warmup placement, strict pre-fs validation order, mid-scan vanish-skip, ndarray/float32 runtime fixes)

Seven implementation details the ADR/plan left open, settled during Phase 3: (1) the hit-row representation is a standalone pydantic model -- **`SimilarityHit` in `general/models/similarity_hit.py`** (re-exported from `general/models/__init__.py`), not a `DocSummary` subclass: the hit carries the cross-domain `type` + `score` fields and deliberately drops `DocSummary`'s `ref`/`error` (the 2026-09-18 plan refinement's own hit shape), so it is a sibling of `DocSummary` rather than a subclass; both tools return `list[SimilarityHit]`, with the unavailable case returning the sibling `SimilarityUnavailableResult` (the `set_status` tool's own union-return precedent). The model's `type` field is a plain `str` (a `Literal` there would invert the models -> tools layering -- the tools' own `type` parameters still use `WholeBodyType`, so the MCP input schema keeps the closed enum while the output stays layering-clean). (2) The shared per-candidate loop lives in **`general/tools/_similarity_search.py`**: `CollectedCandidate` (frozen dataclass: domain/path/SimilarityText/vector), `make_embed_fn(provider, domain)` (the TOCTOU-safe closure: re-runs `candidate_similarity_text` on the cache's own read text -- the stored vector's content always matches the stored hash), `collect_candidates(provider, candidates)` + `to_similarity_hit(candidate, score)`, and the warmup pair. Deliberately, `collect_candidates` takes an **already-materialized `iter_candidate_paths` iterator, not `target_types`**: that is what lets `find_related` order *all* of its argument `ValueError`s (`top_k`/`min_score` via the new `validate_ranking_bounds`, `target_types` via the iterator's own up-front check, `type`/`id` via `resolve_source`) **before any filesystem access** -- a `target_types`-taking signature would have validated `target_types` only inside the collection walk, i.e. after the source resolution had already read files (REQ-009's strict reading in the compound-error case). (3) Up-front `top_k`/`min_score` validation: `rank_candidates`'s own bounds check runs at the *end* of the call (it needs the walked vectors), so the check was factored out as **`_similarity_ranking.validate_ranking_bounds(top_k, min_score)`** -- `rank_candidates` calls it (single source of the bounds), and both tools call it first thing after the availability check. (4) A candidate file **vanishing mid-walk** (a concurrent `delete` racing the intentionally lock-free scan) is **silently skipped** in `collect_candidates` -- the exact `general.tools._listing.build_summaries` `silent_skip_types=(FileNotFoundError,)` precedent (feat-107-doc-cache Phase 8): the same "deleted outside specmgr's own tooling" event `list_<domain>` treats as silently-omitted, here as skipped-not-raised; any other failure (a provider failure on a cache miss) propagates. (5) Warmup placement: the gate + thread spawn is **`_similarity_search.start_similarity_warmup() -> threading.Thread | None`** (returns the started thread, or `None` when unavailable -- the testable seam for Task 4.4), and `server.py`'s `_lifespan` does a **function-level** `from .general.tools._similarity_search import start_similarity_warmup` plus the one call before `yield` -- function-level on purpose, since a module-level import in `server.py` would be a circular import (`general/tools/__init__` imports the tool modules, which import `server` for the `@mcp.tool()` decorator; `mcp` would not yet be bound). The thread is named `specmgr-similarity-warmup`, a daemon (dies with the process; no shutdown join logic the plan does not ask for), and its body (`warmup_similarity_cache`) wraps everything in `try`/`except Exception` with a module-level `logging.getLogger(__name__)` (the repo had no existing logging pattern to mirror) -- a mid-warmup failure logs and swallows, leaving the cache partially warm and the demand path working. (6) Two **real-backend-only runtime fixes** (found by the Phase 3 smoke; invisible to list-returning fakes, so Phase 4's deterministic-fake tests will not catch a regression of either -- worth an explicit real-backend assertion in Task 4.3): `numpy.ndarray` is **not** a virtual subclass of `collections.abc.Sequence` (`isinstance` reports `False` even though the array is indexable and length-carrying), so `_similarity_ranking._assert_vector` checks structurally (reject `str`/`bytes`; require `__getitem__` + `__len__`) instead of `isinstance(vector, Sequence)` -- Phase 2's "`Sequence[float]` is the one annotation both satisfy" holds at the typing level, not the `isinstance` level; and `dot_product` accumulates in the array's dtype, so with the default provider every score is a `numpy.float32` (not a Python `float`) -- `to_similarity_hit` normalizes via `float(score)` (guarded by the same structural numeric check) so the hit row's own `score: float` field always carries a plain Python float. (7) Task 3.6 wiring placement mirrors feat-107 exactly: in `delete.py`, `invalidate_embedding_cache(domain, path)` sits **immediately after** each adapter's own `invalidate_<d>_cache(path)`, still inside the lock, still only after the successful `unlink`/`rmtree` (the `feat` adapter passes the `README.md` path -- the cache's own key -- not the removed folder); in `set_feat_id.py`, `move_embedding_cache("feat", old_path, new_path)` sits **immediately after** `move_feat_cache_entry(old_path, new_path)`, i.e. only after `write_feat_file` succeeded (the moved entry's stale hash then mismatches the rewritten frontmatter on the next read -- a guaranteed, harmless, self-healing miss, verified in the smoke).

#### 2026-09-19 13:27:37.978Z - Phase 2 layout choices beyond what the ADR/plan fixes (module names, fast-path threshold, registry `parse_text` adapter, pooled-vector type, ranking signature)

Six implementation details the ADR/plan left open, settled during Phase 2: (1) module names for the tasks the plan does not name -- **`general/tools/_similarity_corpus.py`** (Task 2.1: candidate enumeration + source resolution + per-candidate parseability) and **`general/tools/_similarity_ranking.py`** (Task 2.4), alongside the plan-named **`_similarity_text.py`** (Task 2.2); the `_similarity_*` prefix keeps the Phase 2 surface self-identifying next to the Phase 1 `_domains`/`_embedding`/`_embedding_cache` modules. (2) the fast-path character threshold is **`_CHUNK_SIZE = 2000`** -- one constant with two documented roles (the per-chunk character budget *and* the single-embed fast-path cutoff: input at/below it embeds whole, above it chunks). The ADR fixes "~2000-character whitespace-boundary chunks" and "input at/below the max takes a single-embed fast path" but not the exact character value for the fast path; 2000 is the natural choice since it is the chunk size itself (~4 chars/token English ≈ `bge-small`'s 512-token max, the backend's own `truncate` guarding any slight overflow). (3) the "parse failure of any channel" unparseable decision (REQ-009) is made by running the domain's own pure-text parser, so **`WholeBodyDomain` gained a fourth adapter field, `parse_text`** (each domain's `parse_<d>(text)` from its `models` package) -- the registry stays the one source of per-domain adapters (REQ-012), and `_similarity_text.py` itself stays domain-agnostic (it receives the parse decision, it does not make it). This also means a candidate's row `id`/`status` are the *validated* frontmatter values (domain defaults applied for a blank key: `planning`/`open`/`draft`), exactly what `list_<d>` surfaces. (4) the mean-pool is **pure Python and returns a plain `list[float]`** -- the dependency-light constraint forbids a module-level numpy import, and a function-local one would fork the pooling into two code paths (the CI deterministic-fake test would only ever exercise the pure one). Consequence, documented in `_mean_pool`: the ADR's "vectors are stored in the provider's native arrays" applies to the single-embed fast path (the backend's own arrays, returned unconverted); a *pooled* vector is a list by construction, and the embedding cache stores whatever the provider returns unconverted per its own contract -- worst-case cost is at most ~128 x 384 floats per chunked document (a negligible fraction of the whole-corpus cache). (5) **`rank_candidates(query_vector, candidates: Sequence[tuple[KeyT, Vector]], *, top_k=DEFAULT_TOP_K, min_score=None) -> list[tuple[KeyT, float]]`** is generic over the candidate key (Phase 3 passes the candidate's own list index into the tool's candidate/row lists; the key is carried through uninterpreted), the `min_score` filter is **inclusive** (`score >= min_score`) and validated to [-1, 1] with `ValueError` (REQ-009 only mandates the `top_k` 1..100 validation; a threshold outside cosine's own range is meaningless, so it is rejected up front, before any scoring, in the path-safety convention), and ties keep the candidates' input order via the stable sort (deterministic corpus enumeration order). (6) `iter_candidate_paths` **validates all target names up front** (before any filesystem access) and then returns a lazy iterator -- a generator function would defer the `ValueError` to the first `next()`, letting earlier domains' paths slip through before an invalid name fires (the REQ-009 error contract requires rejection before any filesystem access); a caller-supplied `target_types` is order-preserving and de-duplicated.

#### 2026-09-19 10:52:35.766Z - Phase 1 layout choices beyond what the ADR fixes (registry module name, `Literal`-derivation mechanism, vector typing, result-model location)

Five implementation details the ADR/plan left open, settled during Phase 1: (1) the registry lives in **`general/tools/_domains.py`** (the ADR fixes only "in `general/tools`"), exporting `WHOLE_BODY_DOMAINS`, the `WholeBodyDomain` adapter dataclass, `whole_body_domain()`, and the two derived annotation aliases. (2) The five tools' `type` parameters are re-pointed via **`WholeBodyType: TypeAlias = Literal[WHOLE_BODY_DOMAINS]`** (and `WholeBodyOrAdrType = Literal[WHOLE_BODY_DOMAINS + ("adr",)]` for `set_status`) -- `Literal[<tuple>]` unpacks to the exact same `Literal` an inline spelling produces (verified: `Literal[T] == Literal['req', ...]` and pydantic's generated JSON schema is identical, and `specmgr mcp-docs` regenerated `docs/MCP.md` byte-identical), so the MCP schema is unchanged while the set is derived from one place per REQ-012/ACC-013. Note: static checkers (pyright, and mypy 2.3, which cannot even parse this codebase's namespace layout) reject `Literal[<runtime tuple>]` at the alias definition site ("only literal values allowed") -- neither runs in this repo's gates (ruff/vulture/pylint/pytest), so the derivation stands. (3) The vector type is **`Vector = Sequence[float]`** in `_embedding.py` (the plan's Design Notes say `list[list[float]]`; the ADR's normative "native arrays" requirement is honored by storing/returning the provider's `np.ndarray` unconverted -- `Sequence[float]` is the one annotation both the ndarray and a test-fake's `list[float]` satisfy, without importing numpy into a dependency-light module). (4) `SimilarityUnavailableResult` lives in **`general/models/similarity_unavailable.py`** (re-exported from `general/models/__init__.py`), mirroring the `InvalidStatusResult` location precedent the plan names; the `_similarity_availability()` helper itself is in `_embedding.py` as the plan says. (5) The unused copy-pasted tuples `_DELETE_TYPES` (delete.py) and `_VALIDATE_TYPES` (validate.py) were **deleted** rather than aliased to the registry (they were read by nothing, src or tests); the derived `Literal` aliases in the signatures are the re-pointing. Also noted: the feat frontmatter `status` stays `planning` for now -- the feat model's status vocabulary is `planning`/`progress`/`review`/`done` (no `in-progress`; AGENTS.md's stale `in-progress` wording is fixed in Task 4.5).

#### 2026-09-18 15:29:13.994Z - Long documents are chunked + mean-pooled, never silently truncated

`fastembed`/`bge-small` would otherwise silently cut input at 512 tokens. Measured corpus: every current spec artifact (129-246 tokens) fits, but the ~52 `.specmgr/feat` READMEs (5k-40k tokens, ~330k total) would be reduced to their opening sections. Decision: chunk + mean-pool inside `FastEmbedProvider` (whitespace-boundary character chunks, `normalize(mean(chunk_vectors))`, `_MAX_CHUNKS_PER_DOC = 128` even-sampling cap) so the protocol still returns one vector per input and tool code stays unchanged; queries use a separate `embed_query` method carrying the BGE retrieval instruction prefix. Consequence: the full-corpus first call costs estimated minutes of CPU, so the server warms the cache in a background thread at startup -- safe because ONNX Runtime inference runs outside the GIL (unlike the markdown/Pydantic parsing feat-107 identified as the GIL-holding bottleneck). Multi-vector retrieval and longer-context models remain explicitly out of scope (revisit only if recall proves insufficient).

#### 2026-09-18 15:29:13.994Z - `status` is excluded from the embedded frontmatter

Resolves the former open question 3. `status` is a closed, low-cardinality enum with near-zero semantic signal for similarity; verified that across all 12 whole-body domains the only surviving non-bookkeeping frontmatter field after the exclusion is `classification` (free-text, meaningful when present).

#### 2026-09-18 15:29:13.994Z - Unparseable candidates are included, not excluded

A document that fails to parse still embeds (its full raw file text) and can appear in results with the `FAILED_TO_PARSE_MARKER` title/status and `id = None`, so broken artifacts stay discoverable instead of vanishing from similarity searches. The `find_related` source itself must be parseable (id lookup already skips unparseable files, so a missing/unparseable source raises the domain's `XNotFoundError`).

#### 2026-09-18 15:29:13.994Z - One shared `WHOLE_BODY_DOMAINS` registry, existing tools re-pointed

The 12-domain set was copy-pasted as `Literal`/tuples across the five existing generic tools; similarity would have been the sixth copy (and it must grow when the reserved `ac` domain lands). The registry is extracted in `general/tools` and all seven tools derive their domain set from it -- one place to add a domain, and `adr`'s exclusion becomes structural rather than per-tool.

#### 2026-09-18 15:29:13.994Z - Real-backend test excluded via `pyproject.toml` `addopts`

Registering `embedding_model` in `[tool.pytest.ini_options]` `markers` plus `addopts = '-m "not embedding_model"'` excludes the model-loading test from every default pytest invocation (CI matrix step and pre-commit hook) with one line, without editing `.github/workflows/ci.yml` or `.pre-commit-config.yaml`, and keeps the AGENTS.md dev command from accidentally triggering a model download. Developers opt in locally with `-m embedding_model`.

#### 2026-09-18 15:29:13.994Z - `SPECMGR_SIMILARITY_DISABLED` is presence-based

The flag is checked as `os.environ.get(name) is not None`, matching the repo's only existing env-flag convention (`specmgr://config` reports env vars by presence, never value). No truthy-string vocabulary to document or drift.

#### 2026-09-17 00:00:00.000Z - Tool registration is independent of dependency availability

Because `@mcp.tool()` registration happens via decorator introspection at module-import time, `find_related`/`find_similar_text` will always be visible in the tool list regardless of whether the `similarity` extra is installed. Runtime unavailability (missing dependency or explicit `SPECMGR_SIMILARITY_DISABLED` env var) is therefore handled inside the tool bodies via a shared structured "unavailable" result, not by conditionally registering the tools.

#### 2026-09-17 00:00:00.000Z - ADR excluded from similarity scope

`adr` is excluded as both a source and target domain, since it is being removed as an artifact type entirely (issue #46).

#### 2026-09-17 00:00:00.000Z - Offline real-backend test kept, but out of CI

A genuine (non-mocked) test against the real `fastembed` backend will exist, under a feature-named pytest marker (not "smoke"), but is excluded from the CI matrix and run manually/locally only, since first use requires a one-time model download.

### Related PRs / Commits

- [Issue #134](https://github.com/dfch/biz.dfch.SpecMgr/issues/134): tracking issue for this feature.
- [PR #137](https://github.com/dfch/biz.dfch.SpecMgr/pull/137): plan + ADR against `dev`; implementation follows in this same branch as one commit per phase.

### More Information

- See feat-133-tags-dec-rsk (issue #133) for the companion, independent Tags-on-dec/rsk feature that was split out of the same original request.
- See feat-135-related-artifacts-risks (issue #135), the other sibling from the same design discussion: it removes the never-implemented `### Acceptance Criteria` sub-list from req/gol/dec/sop's `## Related Artifacts`, replaces it with a validated `### Risks` reference, and enforces a shared `"<TYPE> <uuid>: <title>"` format across those sub-lists. No dependency in either direction; it stays purely format-validation (no existence-checking), deliberately mirroring this feature's discovery-only scope. If feat-135 ships first, this feature's Overview wording ("advisory, unvalidated bullets") goes stale and should be reworded.
- Follow-up opportunity (not this feature, file separately once this ships): the seven `create_*` prompts (dec/feat/gol/prb/sop/sysrs/vcr) currently do advisory near-duplicate checks by scanning `list_<d>` titles; they are natural `find_similar_text` adopters.
