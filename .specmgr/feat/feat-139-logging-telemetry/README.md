---
classification: null
created: '2026-09-19 12:24:35.775+02:00'
id: feat-139-logging-telemetry
status: progress
type: feat
updated: '2026-09-24 14:58:43.000+02:00'
version: 1.0.0
---

# Feature: Logging and Telemetry for the SpecMgr MCP Server

## Plan

### Overview

Add opt-in structured logging and OpenTelemetry-based telemetry (metrics + tracing) to the specmgr MCP server, serving both local development debugging and production/operator observability, without ever writing to stdout under the default `stdio` transport. This formalizes GitHub issue #139 ("Add telemetry and logging") through the standard doc-driven pipeline: QA `40a17fb8-c069-4c38-a092-47912e1fa41d` (elicitation) -> REQ `bc356fc9-964a-4274-93ec-4627c5aeb2e5` (logging), REQ `dacd01f4-ffd8-4363-a20b-5ac1ce11eef2` (telemetry), and REQ `41444084-6821-426d-84a2-028a3f4fed0b` (status resource) -> ADR `fdbb6d22-278a-4ecf-b2f6-db208bc49fc6` (technology decision: stdlib `logging` + OpenTelemetry API/SDK, wired through a single `ServerMiddleware` appended to `mcp.middleware`) -> this feature folder. A second elicitation round refined the two original REQs (every MCP item, not tools only; id/type-only logging; an opt-in file sink; correlation ID on error responses only; MCP-specific metric naming; fail-open on a broken SDK middleware contract) and produced the third REQ. Per that same round's answer, three initial, partial-coverage VCRs (one per REQ, since a VCR's `## Verifies` is single-valued) were authored immediately in Phase 0 below, ahead of any implementation, and are extended phase-by-phase rather than written after the fact -- see `.specmgr/conventions.md`'s "Verification Case Records (VCR) -- Author Early, Extend as Implementation Lands" section for the general convention this follows. Before Phase 1 started, an implementation-readiness review (codebase research plus stakeholder decisions, see Progress below) refined several previously-unspecified design details -- static-config failure mode, logging/OTel interaction with code the MCP SDK already runs, redaction scope, and metric domain attribution -- into the Design Notes and Task List below. A second, plan-consistency review (see Progress below) then closed several gaps found between the Task List and the VCRs/REQs themselves -- a premature metrics claim in Phase 3, a missing OTLP-endpoint validation case, a missing telemetry-VCR acceptance criterion for the metrics themselves, and a naming ambiguity between "tool" metrics and the broadened "every MCP item" scope -- before any implementation started. A third, plan-fix review (Task 0.9, see Progress below) then verified the remaining technical claims directly against the installed SDK source and the current codebase and closed six further gaps that had survived the first two reviews: unfiltered `ServerMiddleware` method scope, resource `uri`-vs-`name` target extraction, missing server-startup wiring for the config/logging/OTel setup calls, an unachievable-as-worded universal title-redaction guarantee, an unspecified correlation-ID/span-ID reconciliation mechanism across Phases 3 and 4, and a domain-lock wait-time measurement detail -- still before any implementation started. A fourth review, prompted externally by the question of why real design errors kept surfacing on every pass, diagnosed the pattern as verification-by-reading rather than verification-by-doing, and appended a technical-spike-and-relocated-packaging task batch (Tasks 1a.1-1a.7) to Phase 1's own checklist (a literal new phase heading is not schema-valid, since phase headings require an integer number) so this work runs before Phase 2 and converts several remaining assumption-based OpenTelemetry SDK claims into empirically confirmed facts before more implementation code is written on top of them, alongside directly fixing a handful of independent gaps (env-var validation completeness, fail-open guard sequencing, an ACC-004/ACC-012 overlap, and DocCache/lock-refactor regression-test coverage) that did not require empirical confirmation -- see Progress below. After the Phase 1a spike batch completed, a fifth, post-exploration re-evaluation pass re-verified the plan against the installed `mcp==2.0.0` SDK's actual request/error wire model (not only its middleware surface) and filled in the remaining implementation-level details the exploration enabled -- the per-method error-channel model (tool exceptions arrive at the middleware as `isError` *results*, not raised exceptions), the correlation-ID attachment mechanisms, the formatter-level scrub application point, the OTel bootstrap pins (`BatchSpanProcessor`, lifespan shutdown hook, proxy-tracer ordering), the `DocCache` registry shape, the lock-helper import safety, the pinned status-resource URI/format, the CLI fail-closed error surface, and the Task 6.7 rewording census (uc titles mandatory, ADR titles excluded as an accepted residual gap, paths scrub-covered) -- all recorded in the Design Notes/Task List/ACCs above and in the Progress section below.

### Requirements

- REQ-001: REQ-bc356fc9-964a-4274-93ec-4627c5aeb2e5 (MCP Server Structured Logging) -- opt-in, correlation-ID-tagged, dual rich/JSON format logging of every MCP tool/resource/prompt invocation, never touching stdout.
- REQ-002: REQ-dacd01f4-ffd8-4363-a20b-5ac1ce11eef2 (MCP Server Telemetry (Metrics and Tracing)) -- opt-in tool-call latency/count/error metrics, doc-cache hit/miss rate, domain-lock wait time, and one distributed trace span per MCP tool/resource/prompt invocation correlated with the logging correlation ID.
- REQ-003: REQ-41444084-6821-426d-84a2-028a3f4fed0b (MCP Server Logging/Telemetry Status Resource) -- a read-only resource reporting current logging/telemetry enablement state as an extensible `list[str]`.

### Acceptance Criteria

- [ ] ACC-001: With both logging and telemetry left at their defaults (no `SPECMGR_LOG_*`/`SPECMGR_OTEL_*` env vars set), running `specmgr mcp` emits zero log records and zero telemetry, and writes nothing to stdout beyond the JSON-RPC channel.
- [ ] ACC-002: With `SPECMGR_LOG_ENABLED=true`, every MCP tool call (`tools/call`), resource read (`resources/read`), or prompt invocation (`prompts/get`) produces a start, completion (or error) log record on stderr, tagged with a per-invocation correlation ID, in the format selected by `SPECMGR_LOG_FORMAT` (`rich` or `json`); protocol-bookkeeping methods that also pass through the same middleware (`initialize`, `tools/list`, `resources/list`, `prompts/list`, `ping`, `notifications/*`) do not produce these records (see ACC-013).
- [ ] ACC-003: With `SPECMGR_OTEL_ENABLED=true`, every MCP tool call, resource read, or prompt invocation this feature's own middleware/meter observes (per ACC-002/ACC-013's method scope) has its call/error/latency metrics recorded for tool-call latency, tool-call counts by tool, error counts by the per-channel error-type signal (the exception type where observable at the middleware layer, the SDK's own `tool_error` value for `tools/call` error results, the JSON-RPC error code for SDK-wrapped errors -- see the Design Notes' "Per-method error channels" bullet), the `feat-107` doc-cache hit/miss rate, and per-domain lock wait time; the correlation ID from ACC-002 matches the trace ID of the span the SDK's built-in `OpenTelemetryMiddleware` already creates for that same request (note: that SDK middleware spans every JSON-RPC method, including the protocol-bookkeeping ones ACC-013 excludes from logging/metrics -- this feature does not suppress or filter those pre-existing spans, only its own log records and metric increments). Regardless of which `SPECMGR_OTEL_EXPORTER` is configured (including the default `console`), enabling telemetry never writes any byte to stdout -- the console exporter's output is redirected to stderr (see Design Notes; Task 1a.2's spike, Phase 1).
- [ ] ACC-004: No log record that this feature's own code (middleware, meter, status resource, redaction filter) constructs ever carries a full document body, an absolute filesystem path, or a document/artifact title as a *structured* field value; free-text content reaching a log record (e.g. an exception's `str()`) is passed through a best-effort regex-based absolute-path scrub as a defense-in-depth backstop (see the Design Notes' "Redaction scope and limits" bullet for why a document/artifact *title* embedded in free text cannot be reliably detected this way, and for the narrow, message-text-only rewording of the title-embedding exception call sites verified in non-deprecated domains -- the deprecated ADR domain's title sites are an accepted residual gap, see the Design Notes' "Redaction scope and limits" bullet). ACC-012 states the equivalent guarantee for span attributes; the two are deliberately split (logs vs. spans) rather than restated together, since they are verified by different mechanisms (a formatter-level scrub vs. a `SpanProcessor`).
- [ ] ACC-005: `README.md` documents every new `SPECMGR_LOG_*`/`SPECMGR_OTEL_*` environment variable, its default, and how to enable logging/telemetry for local development vs. production.
- [ ] ACC-006: The standard pre-commit/CI quality gate (`ruff format`, `ruff check`, `vulture`, `pytest`, docs regen) passes after every phase below.
- [ ] ACC-007: The status resource (REQ-003) returns a `list[str]` accurately reflecting whether logging and telemetry are each currently enabled, reflecting the actual configuration in effect.
- [ ] ACC-008: With logging or telemetry enabled, a failing MCP tool/resource/prompt call's error response includes a correlation ID (attached via the tool error result's `_meta` field and via the JSON-RPC error's `data` field -- see the Design Notes' "Per-method error channels" bullet); a successful call's result never includes one. With both features disabled (the default) the middleware is a pure pass-through: no correlation ID is attached and the error response is unchanged (see the Design Notes' "Middleware enablement gating" bullet).
- [ ] ACC-009: The file log sink honors its own on/off and filename environment variables (active only when `SPECMGR_LOG_ENABLED=true`), and always emits structured JSON regardless of the console format setting.
- [ ] ACC-010: An unreachable OTLP endpoint, or a broken `Server.middleware` contract, never breaks a tool call; each condition logs at most one stderr message per failure episode, never repeating while the condition persists.
- [ ] ACC-011: A static logging/telemetry misconfiguration (e.g. `SPECMGR_LOG_FILE_ENABLED=true` with `SPECMGR_LOG_FILE_PATH` unset/blank, `SPECMGR_OTEL_EXPORTER=otlp` with `SPECMGR_OTEL_ENDPOINT` unset/blank, an unrecognized `SPECMGR_LOG_FORMAT`/`SPECMGR_OTEL_EXPORTER`/`SPECMGR_LOG_LEVEL` value, or a boolean-flag env var -- `SPECMGR_LOG_ENABLED`, `SPECMGR_LOG_FILE_ENABLED`, `SPECMGR_OTEL_ENABLED` -- set to anything other than `true`/`false` case-insensitively) fails closed: `specmgr mcp` refuses to start with a clear error, rather than silently disabling the misconfigured piece.
- [ ] ACC-012: No span attribute this feature's own code sets, on any span (including ones the SDK's built-in `OpenTelemetryMiddleware` creates), ever carries a full document body, an absolute filesystem path, or a document/artifact title as a *structured* attribute value; the global `SpanProcessor` additionally runs the same best-effort regex-based absolute-path scrub as ACC-004 against every span's attributes and exception-event message text, and the title-embedding exception call sites in non-deprecated domains (enumerated in Task 6.7: the 13 `UcParseError` sites mandatory, the deprecated ADR domain's 9 title sites an accepted residual gap, and the ~25 path-embedding sites scrub-covered and optionally reworded) have that message text reworded so their `record_exception`/`set_status(str(e))` payload no longer carries it -- this is the achievable, defense-in-depth guarantee this feature provides; see the Design Notes for why an unconditional, universal guarantee against *any* free-text title leaking through an as-yet-unaudited exception message is not something a generic filter can make.
- [ ] ACC-013: This feature's own logging, correlation-ID assignment, and metric increments engage only for `tools/call`, `resources/read`, and `prompts/get` requests; `initialize`, `tools/list`, `resources/list`, `prompts/list`, `ping`, and `notifications/*` (which also flow through the same `ServerMiddleware` chain, per the installed SDK's documented behavior) are observed by the middleware but produce no log record, no correlation ID, and no metric increment.

### Scope

#### Included

- A new top-level `src/biz/dfch/specmgr/telemetry/` package (config parsing, logging setup, the `ServerMiddleware` implementation, and the OpenTelemetry SDK bootstrap).
- Structured logging: dual rich (local dev) / JSON (production) format, gated by `SPECMGR_LOG_ENABLED`/`SPECMGR_LOG_LEVEL`/`SPECMGR_LOG_FORMAT`, covering every MCP tool call, resource read, and prompt invocation (`tools/call`/`resources/read`/`prompts/get`) -- not every JSON-RPC method the shared middleware chain happens to see (ACC-013).
- A method-name filter in the middleware (`ctx.method in {"tools/call", "resources/read", "prompts/get"}`) gating which requests produce a log record, a correlation ID, or a metric increment, since `Server.middleware` wraps every inbound JSON-RPC request/notification (`initialize`, `*/list`, `ping`, `notifications/*` included), not only item invocations.
- An opt-in file log sink, off by default, its own on/off sub-switch layered under `SPECMGR_LOG_ENABLED`, its own configurable filename, always emitting structured JSON regardless of the console format setting.
- A per-tool-call correlation ID, generated once per invocation by the middleware, included in every related log record and, when telemetry is enabled, matching the active span's trace ID; included in a client-visible error response (only when at least one of logging/telemetry is enabled; in the default both-off config the middleware is a pure pass-through and attaches nothing), but never in a successful result.
- OpenTelemetry metrics: tool-call latency histogram, tool-call counts by tool/domain, error counts by exception type, `feat-107` doc-cache hit/miss rate, and per-domain lock wait/contention time, named using an MCP-specific scheme (e.g. `mcp.tool.duration`, `mcp.tool.name`, `mcp.domain`, `mcp.item.type`) rather than the raw OpenTelemetry RPC semantic-convention names.
- OpenTelemetry tracing: activating the MCP SDK's existing built-in `OpenTelemetryMiddleware` (which spans every JSON-RPC method, always -- this feature does not change what gets a span, only what gets logged/counted, per ACC-013), with always-on (100%) sampling and no sample-rate configuration.
- A read-only status resource (REQ-003) returning a `list[str]` reporting current logging/telemetry enablement state.
- A redaction safeguard, achievable and best-effort rather than an unconditional universal guarantee (see the Design Notes' "Redaction scope and limits" bullet): (a) this feature's own code (middleware, meter, status resource) never attaches a full document body, an absolute filesystem path, or a document/artifact title as a *structured* log/span/metric attribute; (b) a global, `TracerProvider`-level `SpanProcessor` for spans (covering the SDK's own built-in `OpenTelemetryMiddleware` spans too) plus a scrubbing formatter hook in every specmgr-installed `Handler` (console/stderr and the opt-in file sink alike) additionally run a regex-based absolute-path scrub against the free-text content reaching those handlers/spans (log messages, exception `str()` and tracebacks, span exception events) regardless of originating logger; (c) the title-embedding exception call sites verified in non-deprecated domains (Task 6.7's mandatory rewording set -- today the 13 `UcParseError` sites in `uc/models/v1/parser.py`; the deprecated ADR domain's 9 title sites are an explicitly accepted residual gap, and the ~25 path-embedding sites are scrub-covered and optionally reworded) have that message text reworded to omit it, since no generic filter can reliably detect free-form title text.
- Graceful, fail-open degradation for *runtime* observability failures only (an unreachable OTLP endpoint, a broken `Server.middleware` contract): never breaks a tool call, and never spams stderr. A *static* logging/telemetry misconfiguration (an invalid or incomplete `SPECMGR_LOG_*`/`SPECMGR_OTEL_*` combination, discoverable at startup) fails closed instead -- see the new "Observability Degrades Gracefully, Application Logic Never Does" convention in `.specmgr/conventions.md`.
- Three initial, partial-coverage VCRs (one per REQ above), authored in Phase 0, extended phase-by-phase as implementation lands.
- `README.md` documentation of every new environment variable.

#### Explicitly Out Of Scope

- Propagating the correlation ID any further than a client-visible error response (e.g. into successful results, or into the MCP SDK's per-tool `Context` object).
- Any change to the existing exception-raising error-handling convention -- when an exception is raised, its type, and its being raised rather than swallowed -- (`XNotFoundError`, `DeleteError`, ...); logging/telemetry observes these, it does not replace them. **Narrow carve-out (Task 6.7):** the *message text* of the title-embedding exception call sites in non-deprecated domains (the verified set: `UcParseError`'s `f"...{node.title!r}"`/`f"...{child.title!r}"` messages in `uc/models/v1/parser.py` -- 13 sites) is reworded to drop the title -- this changes only the string passed to the exception constructor, never the exception's type, its raise condition, or whether it propagates. The deprecated ADR domain's 9 title-embedding sites (`models/adr/v1/parser.py`'s `AdrParseError` x8, `models/adr/v1/mutations.py`'s `AdrOptionNotFoundError` x1) are explicitly excluded from that rewording as an accepted residual gap (the domain is planned for phase-out, mirroring its `feat-107` cache exclusion), and the path-embedding sites (e.g. `DeleteError`'s `f"failed to delete {path}: {ex}"`, the 12 domains' `<domain>/tools/_io.py` `*NotFoundError` messages, `feat/tools/_paths.py`) are covered by the regex scrub and optionally reworded.
- Suppressing, filtering, or otherwise changing which JSON-RPC methods receive a span from the MCP SDK's own built-in `OpenTelemetryMiddleware` -- that middleware spans every method (`initialize`, `*/list`, `ping`, `notifications/*` included) today, independent of this feature, and this feature's Phase 6 redaction safeguard scrubs the *content* of those spans without changing *which* requests get one. Only this feature's own logging/correlation-ID/metric-increment behavior is scoped down to actual item invocations (ACC-013).
- Building a dashboard, alerting rules, or any specific backend deployment (Grafana/Prometheus/Jaeger/etc.) -- only the exporter configuration surface (`console`/`otlp`) is in scope.
- A configurable trace sample rate -- always-on (100%) sampling is accepted for this feature; revisit only if real-world overhead becomes a problem.
- The deferred ADR `3bf0326f-065a-424c-a2b9-87e5d5bcfa99` MCP-singleton extraction into its own module -- this feature instruments `mcp` where it is constructed today in `server.py`.
- An upper version bound on the `mcp` dependency (e.g. `mcp>=2.0.0,<3.0.0`) -- left at `mcp>=2.0.0`; the Task 4.4 compatibility test plus the runtime fail-open behavior are relied on instead to catch a breaking SDK change, rather than a version ceiling.

### Dependencies

#### Depends On

- ADR `fdbb6d22-278a-4ecf-b2f6-db208bc49fc6` (technology decision, already accepted).
- The three initial VCRs authored in Phase 0 (`0a1c2f63-9576-4463-bf40-8f771f14fefb` logging, `54fd355f-0978-4dd0-9e1d-da89432e5321` telemetry, `f494d230-97f3-4db9-9ef9-ed9c903ee008` status resource), each starting at `## Coverage: partial` and extended by the Task List below.
- `feat-107-doc-cache`'s `DocCache` class (`general/tools/_doc_cache.py`), since Task 5.4 adds hit/miss counters directly to it.

#### Blocks

- None currently -- the VCRs are authored within this feature's own Phase 0 and Task List, not tracked as a separate follow-up.

### Design Notes

- **Package location**: new top-level `telemetry/` package (not a document domain, so it does not fit the domain-first `<domain>/tools` convention; mirrors `models/` as shared infrastructure). Proposed modules: `telemetry/config.py` (env var parsing), `telemetry/logging.py` (formatter + setup, building on `mcp.server.mcpserver.utilities.logging.configure_logging`), `telemetry/middleware.py` (the `ServerMiddleware` implementation: correlation ID, latency timing, call/error counters, correlation-ID-on-error attachment), `telemetry/otel.py` (OpenTelemetry SDK bootstrap: `TracerProvider`/`MeterProvider` + exporter, fail-open handling), `telemetry/redact.py` (the body/path/title redaction safeguard: a scrubbing formatter hook for the log handlers plus a global `SpanProcessor`).
- **Environment variables** (all default to "disabled"/safe; the `SPECMGR_*` prefix follows the existing convention; note the codebase's only enum-valued `SPECMGR_*` precedent, `SPECMGR_MCP_TRANSPORT` (`stdio`/`sse`/`streamable-http`, `commands/mcp.py`), is case-insensitive and *permissively* falls back to `stdio` for unrecognized values -- the new vars deliberately diverge from that permissiveness, and there is no boolean-flag precedent at all: `telemetry/config.py` (Task 1.1) defines its own accepted spellings -- exact-match `"true"`/`"false"` case-insensitively for the boolean flags, closed sets for `SPECMGR_LOG_FORMAT`/`SPECMGR_OTEL_EXPORTER`, and exactly the five named levels for `SPECMGR_LOG_LEVEL` (see below) -- and documents them in `README.md` (Task 8.1)): `SPECMGR_LOG_ENABLED` (`true`/`false`, default `false`); `SPECMGR_LOG_LEVEL` (`DEBUG`|`INFO`|`WARNING`|`ERROR`|`CRITICAL`, case-insensitive, default `INFO`); `SPECMGR_LOG_FORMAT` (`rich`|`json`, default `rich`); `SPECMGR_LOG_FILE_ENABLED` (`true`/`false`, default `false`, only takes effect when `SPECMGR_LOG_ENABLED=true`); `SPECMGR_LOG_FILE_PATH` (file path for the JSON-only file sink, required only when the file sink is enabled); `SPECMGR_OTEL_ENABLED` (`true`/`false`, default `false`); `SPECMGR_OTEL_EXPORTER` (`console`|`otlp`, default `console`); `SPECMGR_OTEL_ENDPOINT` (OTLP **base URL**; required and must be non-blank when `SPECMGR_OTEL_EXPORTER=otlp`, fails closed otherwise -- mirroring the file-sink path requirement below; per Task 1a.1's confirmed exporter choice, the OTLP/HTTP exporter consumes this base URL natively and appends the spec-mandated `/v1/traces` and `/v1/metrics` paths itself). This task also validates `SPECMGR_LOG_LEVEL` against the five named levels above (case-insensitively -- the same literal set the SDK's own `configure_logging()` accepts) and every boolean-flag var above against the exact-match `"true"`/`"false"` closed set (case-insensitively) -- both fail closed on any other value, exactly like the `SPECMGR_LOG_FORMAT`/`SPECMGR_OTEL_EXPORTER` closed-set validation already described above, rather than being silently ignored or defaulted.
- **Static config validation fails closed**: `telemetry/config.py` raises immediately (e.g. `ValueError`) when parsing an invalid/incomplete combination -- `SPECMGR_LOG_FILE_ENABLED=true` without a non-blank `SPECMGR_LOG_FILE_PATH`, `SPECMGR_OTEL_EXPORTER=otlp` without a non-blank `SPECMGR_OTEL_ENDPOINT`, or an `SPECMGR_LOG_FORMAT`/`SPECMGR_OTEL_EXPORTER` value outside its closed set -- so `specmgr mcp` refuses to start rather than silently running with a half-broken sink. This validation is unconditional: it fires regardless of whether `SPECMGR_LOG_ENABLED`/`SPECMGR_OTEL_ENABLED` are themselves `true`, since a nonsensical combination is rejected outright rather than silently tolerated just because it happens to be currently inert. This mirrors how every other `SPECMGR_*` environment variable in this codebase is already treated, and is the static-misconfiguration counterpart to the *runtime* fail-open policy below; see `.specmgr/conventions.md`'s new "Observability Degrades Gracefully, Application Logic Never Does" section for the general rule this follows (ACC-011).
- **Tool-call instrumentation mechanism**: a single `ServerMiddleware` appended to `mcp.middleware` in `server.py`, right where `mcp = MCPServer(...)` is constructed -- no edits to any of the ~100+ existing `@mcp.tool()`/`@mcp.resource()`/`@mcp.prompt()` functions (confirmed feasible via research into the installed `mcp==2.0.0` SDK: `Server.middleware` is a live, appendable list, and the SDK's own built-in `OpenTelemetryMiddleware` in `mcp/server/_otel.py` is a ready-made template for span creation, exception tagging, and `error.type` recording).
- **`ServerMiddleware` wraps every JSON-RPC method, not only item invocations -- the middleware must filter**: confirmed via the installed SDK's `ServerMiddleware` protocol docstring (`mcp/server/context.py`): it "wraps every inbound request and notification" -- `initialize`, `tools/list`, `resources/list`, `prompts/list`, `ping`, `notifications/cancelled`, `notifications/initialized`, etc. -- not only `tools/call`/`resources/read`/`prompts/get`. Task 3.1's middleware must check `ctx.method` and only emit a log record / assign a correlation ID / increment a metric when it is one of `{"tools/call", "resources/read", "prompts/get"}` (ACC-013); the other methods pass through `call_next(ctx)` unobserved by this feature's own code. This is independent of the SDK's own built-in `OpenTelemetryMiddleware`, which keeps spanning every method regardless (out of this feature's control, and out of scope to change -- see Explicitly Out Of Scope).
- **Per-method target extraction differs -- resources are keyed by `uri`, not `name`**: confirmed via `mcp/server/_otel.py`, the SDK's own `OpenTelemetryMiddleware` only reads `ctx.params.get("name")` for `tools/call` and `prompts/get`; it has no `resources/read` branch, because a resource invocation identifies its target via `ctx.params["uri"]`. Task 3.1's id/type-only logging and Task 5.1's `mcp.domain` mapping must both branch on `ctx.method` to pick the right params key (`name` for tools/prompts, `uri` for resources) rather than assuming a single `name`-shaped extraction works for all three.
- **Per-method error channels differ -- the middleware must handle three shapes** (verified against the installed `mcp==2.0.0` SDK source in this re-evaluation: `mcp/server/mcpserver/server.py`'s `_handle_call_tool`/`_handle_read_resource`/`_handle_get_prompt` and `mcp/server/runner.py`'s `modern_error_data`/`on_request` error mapping): for `tools/call`, every non-`MCPError` exception raised by a tool function is converted by the SDK *before* the middleware sees it -- `except Exception as e: return CallToolResult(content=[TextContent(str(e))], is_error=True)` -- i.e. a JSON-RPC *success* envelope whose dumped wire dict carries `isError: true` (camelCase alias; the middleware observes the serialized dict, not the model), so no exception object is available at the middleware layer for tools; for `resources/read`, the SDK's own `ResourceNotFoundError`/`ResourceError` are wrapped as `MCPError` (carrying the SDK's own `data={"uri": ...}`) and other exceptions propagate raw; for `prompts/get` nothing is converted, raw exceptions propagate; and at the dispatcher, an `MCPError` keeps its own `ErrorData` (incl. `data`), a `ValidationError` maps to `INVALID_PARAMS` with empty `data`, and any other exception maps to a generic `INTERNAL_ERROR`/"Internal server error" **without a data field** (plus an SDK-side `logger.exception` under `mcp.server.runner`). Task 3.1 must therefore detect a `tools/call` failure from the post-`call_next` result dict (`isError: true`) as well as from the exception path (the tool error record's text comes from the result's content, i.e. the SDK's `str(e)`); Task 3.3 attaches the correlation ID per channel: (a) `tools/call` error result -- merge `"_meta": {"correlationId": ...}` into the result dict (verified: `CallToolResult.meta` dumps as `_meta`); (b) `MCPError` on any of the three methods -- merge `correlationId` into `e.error.data`, preserving the SDK's own `uri` entry; (c) raw exception/`ValidationError` on any of the three methods -- convert to a wire-identical `MCPError` (same code/message the dispatcher would otherwise produce, plus the `data` carrying the correlation ID) and re-raise, so ACC-008 holds universally; the accepted side effect is that the SDK's own server-side `logger.exception` of that traceback is replaced by this feature's own error log record, which carries the correlation ID anyway. Task 5.3's error-type counter likewise uses the per-channel signal observable at the middleware layer: `tool_error` (the SDK's own value for the `isError` result shape) for `tools/call` error results, the `MCPError`'s JSON-RPC error code for wrapped errors, and `type(exc).__qualname__` for raw exceptions -- which satisfies REQ `dacd01f4-ffd8-4363-a20b-5ac1ce11eef2`'s "error counts by exception type" clause without changing the REQ text.
- **Wiring the Phase 1/2/4 setup calls into the actual startup path**: `commands/mcp.py`'s `mcp` command imports `server.py` lazily inside the command function (`from ..server import mcp as mcp_server`), which is what first executes `server.py`'s module body -- the same point where `mcp = MCPServer(...)` is constructed and where Task 3.2 appends the middleware. `telemetry/config.py`'s validation (Task 1.6), `telemetry/logging.py`'s setup (Task 2.5), and `telemetry/otel.py`'s bootstrap (Task 4.9) must all be called unconditionally at that same module-level point (not left as library functions nobody calls), in that order (config validated first, since a bad config must fail closed before anything else runs; then logging, so early log records are correctly formatted; then OTel, then the middleware), for ACC-001/ACC-011 to actually hold at `specmgr mcp` startup rather than only in unit tests that call these functions directly. This desired call order (config, then logging, then OTel, then the middleware append) spans three phases (1, 2, 4) plus the append itself (Phase 3, earlier) -- Task 3.2's append necessarily lands before Task 4.9's OTel-bootstrap call exists yet, so Task 4.9 must insert its call *above* the already-existing Task 3.2 line in `server.py` (reordering, not merely appending) once Phase 4 lands, and Task 4.9's own description is written to make that explicit rather than assume the desired order is already in place. Phase 1's Task 1a.1 additionally moves the `opentelemetry-sdk` dependency this whole chain needs to before Phase 2, rather than leaving it until Phase 7, so Phase 4's `telemetry/otel.py` has something importable to configure by the time Task 4.1 is written.
- **`MCPServer.__init__` already calls `configure_logging()` unconditionally today**: it installs a stderr `RichHandler` on the root logger at the default `INFO` level the moment `mcp = MCPServer(...)` runs in `server.py`, before any of this feature's code executes. Confirmed via research that, in this default configuration, the SDK itself emits **zero** stderr output during a normal stdio session (every non-DEBUG SDK log call is gated behind an abnormal/error condition; the two unconditional calls are DEBUG-level and suppressed by the INFO default) -- so ACC-001's "zero log records by default" baseline holds without this feature needing to actively suppress anything. However, `telemetry/logging.py`'s own setup (Phase 2, once `SPECMGR_LOG_ENABLED=true`) must not be a silent no-op against the handlers `configure_logging()` already installed -- `logging.basicConfig()` is a no-op if handlers already exist -- so it must explicitly reconfigure (e.g. `force=True`, or replace/clear the existing handler set) rather than assume a clean slate.
- **Metric/span naming scheme**: MCP-specific names (`mcp.tool.duration`, `mcp.tool.call.count`, `mcp.tool.error.count`, `mcp.cache.hit`/`mcp.cache.miss`, `mcp.lock.wait_time`, with attributes `mcp.tool.name`/`mcp.domain`) rather than the literal OpenTelemetry RPC semantic-convention names (e.g. `rpc.server.duration`), since MCP has no registered `rpc.system` value and resource/prompt calls do not map cleanly onto RPC semantics; follows the spirit (structured names, standard units, histogram buckets), not the letter, of the OTel semantic conventions. Since this requirement explicitly covers every MCP item (tools, resources, and prompts), not tools only, every metric/span additionally carries an `mcp.item.type` attribute (`tool`/`resource`/`prompt`) alongside `mcp.tool.name`/`mcp.domain`, so a resource or prompt invocation recorded under the `mcp.tool.*` metric namespace is not mistaken for a literal tool call.
- **`mcp.domain` attribution**: no tool/resource/prompt-name -> domain mapping exists anywhere in the codebase today. The generic dispatch tools (`update`/`set_status`/`set_classification`/`delete`/`validate`) carry the domain directly in their own `type` argument (from `ctx.params["arguments"]`); domain-specific tools (e.g. `create_req`, `get_req`) map to a domain by their name/module; resources map to a domain (or no domain) by their `uri` (e.g. `specmgr://req/...` vs. the no-domain `specmgr://version`), not by a `name` argument, per the per-method target-extraction bullet above; some resources/prompts have no domain concept at all. Phase 5 needs an explicit, small mapping table/function in `telemetry/` deriving `mcp.domain` from the invocation's method plus the appropriate per-method key (tool/prompt `name`, resource `uri`) and, where present, its `type` argument -- this table does not exist yet and is new design work, not a refactor of an existing mechanism.
- **Correlation ID exposure and its Phase 3/Phase 4 reconciliation**: generated once per invocation by the middleware; included in every related log record and, when telemetry is enabled, tied to the active span's trace/span ID; included in a client-visible error response so an operator/agent can quote it back for log lookup, but never included in a successful result. Concretely, so that Task 3.1 (Phase 3, before any OTel/span concept exists) and Task 4.2 (Phase 4, "tie" the ID to the span) do not each independently generate an unrelated value that can never actually "match" (ACC-003): the correlation-ID helper always first checks for a current, recording OpenTelemetry span (`trace.get_current_span()`); when one exists (`SPECMGR_OTEL_ENABLED=true`, and the SDK's built-in `OpenTelemetryMiddleware` -- which runs outside/before this feature's own middleware in the chain, confirmed via `MCPServer.middleware`'s "outermost-first" ordering -- has already started that request's span by the time this feature's own middleware runs), the correlation ID *is* that span's trace ID (hex-formatted); otherwise (telemetry disabled) it falls back to a freshly generated `uuid4().hex`. Task 3.1 implements this helper directly (it degrades to the `uuid4()` branch until Phase 4 exists); Task 4.2 is therefore not new "tying" logic grafted on afterwards, only the test/confirmation that the span-ID branch engages once telemetry is enabled.
- **Middleware enablement gating (pure pass-through when both features are off)**: the middleware's observability behavior -- correlation-ID generation, start/completion/error log records, metric increments, and (critically) Task 3.3's raw-exception-to-`MCPError` conversion and error-response correlation-ID attachment -- engages only when at least one of `SPECMGR_LOG_ENABLED` / `SPECMGR_OTEL_ENABLED` is `true`. When both are `false` (the default), the middleware is a pure pass-through for *every* method, including the three invocations: it adds no correlation ID to any response and performs no exception conversion, so the default config's error handling is byte-for-byte the SDK's own -- in particular the dispatcher's stderr `logger.exception` of a raw exception (under `mcp.server.runner`) is *not* suppressed by this feature (Task 3.3's stated "accepted side effect" of replacing that traceback log only applies when this feature's own error record is actually emitted, i.e. when logging is enabled). Metrics are already a no-op when disabled (Task 5.2); this note extends the same gating to the correlation-ID and error-conversion paths, which ACC-008 / Task 3.1 / Task 3.3 otherwise describe unconditionally. This preserves the ADR's "fully opt-in, off by default" driver and ACC-001's zero-change default baseline.
- **Phase 3/Phase 5 division of labor for counters**: Phase 3's middleware (Task 3.1) only generates the correlation ID and emits log records; it does not create or increment any OTel counter/histogram itself, since no `MeterProvider` exists until Phase 4 (Task 4.1) configures one. All counter/histogram creation and incrementing (call counts, error-type counts, latency) is Task 5.2/5.3's responsibility, once the Task 5.1 domain mapping exists -- Phase 3's task description was reworded to stop describing metrics work two phases early.
- **OTel bootstrap pins (Task 4.1, from the Phase 1a findings plus this re-evaluation's verification)**: the `TracerProvider` uses a **`BatchSpanProcessor`**, not a `SimpleSpanProcessor` -- with a simple processor, an unreachable OTLP endpoint would block each span's `end()` synchronously for the exporter's timeout (10 s production default, per Task 1a.4), stalling every tool call in a way the fail-open design does not cover; the batch path's background failure is exactly what Task 4.5's wrapper de-duplicates. The `MeterProvider` uses the **`PeriodicExportingMetricReader`** (default 60 s interval). The OTLP exporters are constructed as `OTLPSpanExporter(endpoint=...)`/`OTLPMetricExporter(endpoint=...)` with the `SPECMGR_OTEL_ENDPOINT` base URL as-is, and the Task 4.5 wrapper is applied to both. Both providers are shut down at process exit -- `TracerProvider.shutdown()`/`MeterProvider.shutdown()` (the latter performs the final metric collection Task 1a.2 found reaches real stdout after serving ends without the `out=sys.stderr` redirection) -- hooked into `server.py`'s existing `_lifespan` placeholder (its post-`yield` section), which today is a no-op. Note for Task 4.2: `mcp/shared/_otel.py` fetches its tracer (`get_tracer("mcp-python-sdk")`) at module-import time, before any provider exists -- verified against the installed API in this re-evaluation that such a tracer is a proxy that resolves the provider set *later* (a span started through it still lands in the later-configured provider), so the prescribed module-scope order (import `mcp` -> construct `MCPServer` -> bootstrap) works; Task 4.2's confirmation test must use that real import order, not the spike's pre-import provider installation. Histograms use unit `ms` with explicit bucket advice (`mcp.tool.duration`: 5, 10, 25, 50, 100, 250, 500, 1000, 2500, 5000, 10000; `mcp.lock.wait_time`: 1, 5, 10, 25, 50, 100, 250, 500). The `Meter` is fetched once at bootstrap (`metrics.get_meter("specmgr")`) into a module-level slot; the middleware's increments are slot-guarded (a `None` slot is a no-op, consistent with the Phase 3/Phase 5 division-of-labor bullet above). The stdout-safety requirement (ACC-001/ACC-003, Task 4.10) is specific to the **stdio** transport, where stdout is the JSON-RPC channel; `specmgr mcp` also serves `sse` and `streamable-http` (see `README.md`'s flags table), where stdout is not the protocol channel -- the `out=sys.stderr` redirection applies uniformly regardless.
- **Fail-open policy (runtime failures only)**: both the OTLP-exporter-unreachable case and a broken `Server.middleware` contract (a future SDK upgrade) must never break a tool call -- the server keeps operating normally, logging/telemetry disable themselves (fully or partially) with at most one stderr warning per failure episode, and a further single message is only emitted after a subsequent reconnect-then-drop cycle, never repeating while the condition persists. This is distinct from the static-config-validation rule above, which fails closed instead. **Implementation risk flagged, confirmed by spike**: Task 1a.4 (Phase 1; see Decisions Made) confirmed the installed 1.44.0 SDK's actual failure-logging behavior: the HTTP OTLP exporters log **per failed attempt within an export cycle, plus one final error per cycle** (with the production-default 10 s exporter timeout: 3x WARNING `Transient error ... retrying in N.NNs.` + 1x ERROR `Failed to export span/metrics batch due to timeout, max retries or shutdown.` = 4 lines per failed cycle), and the cycles repeat every `schedule_delay_millis`/`export_interval_millis` for as long as telemetry data keeps flowing -- all under the exporters' own module loggers (`opentelemetry.exporter.otlp.proto.http.trace_exporter` / `...metric_exporter`); the exporters return `FAILURE` without raising, so the SDK's own exception-logging paths (incl. `opentelemetry.sdk._shared_internal`'s built-in 20-second `DuplicateFilter`) never fire for an unreachable endpoint, and no SDK-provided episode de-duplication exists. Because the SDK logs no success at all, recovery is not observable from the log stream: achieving "at most one message per failure episode" therefore requires a thin wrapper around the configured OTLP exporter that tracks first-failure/recovery transitions itself (suppressing the exporters' own loggers while active) -- a bare `logging.Filter` on those logger names cannot do it. Phase 4's Task 4.5 implements de-duplication against that confirmed behavior. This work was moved from an earlier draft's Phase 5 into Phase 4, since it is squarely about the OTel exporter/SDK bootstrap Phase 4 already owns, not about the domain-level metrics instrumentation Phase 5 covers. Task 3.2 (Phase 3) additionally wraps its own middleware-append call in a minimal version of this same fail-open policy from the moment it lands, so the append itself can never crash `specmgr mcp` even before this phase's full policy exists; Task 4.3 upgrades that minimal guard into the complete behavior described above, rather than adding an unrelated, independent one.
- **Doc-cache hit/miss counters**: add counters directly to the shared `general/tools/_doc_cache.py`'s `DocCache` class (instruments all 12 per-domain caches at once) plus a `stats()` accessor the OTel meter reads. Since these are 12 independent per-domain singleton instances, not one shared instance, a single meter callback cannot discover them on its own -- Task 5.4 also adds a small registry (a module-level list/dict of live `DocCache` instances, populated at each domain's own cache-module import time) the callback iterates to attribute `mcp.cache.hit`/`mcp.cache.miss` per `mcp.domain`; this registry is new design work, not part of the existing `DocCache` mechanism. Task 1a.5's spike confirmed the observable-instrument mechanics Task 5.4 assumes, against the installed SDK 1.44.0 (see Decisions Made): callbacks are registered at instrument creation time (`meter.create_observable_counter(name, callbacks=[...])` -- the SDK instrument class has no public `add_callback`), are invoked only at collection/export time (never at registration), must return an iterable of `Observation` (the older `result(Observation)` callable protocol is gone in this version), and the returned values reflect the stand-in's state at collection time, with per-domain attribute series kept distinct and the default reader temporality CUMULATIVE (an unchanged collection re-reports the same cumulative value rather than drifting; DELTA only if explicitly requested via `preferred_temporality`, which must be keyed by the SDK's internal instrument classes). Two confirmed caveats Task 5.4's callback must respect: it must yield an `Observation` for every live registry entry every cycle (a series the callback does not yield is absent from that collection), and its registry iteration must be defensive (a raising callback makes the SDK log `Callback failed for instrument ...` and drop the instrument's entire collection cycle). Registry shape (pinned in this re-evaluation): `DocCache.__init__` gains an optional `domain: str | None = None` parameter and self-registers into a module-level `dict[str, DocCache]` living in `general/tools/_doc_cache.py` itself (keyed by domain name, last-constructed-wins, with a test-only reset mirroring the existing `reset()` pattern -- unit tests construct additional `DocCache` instances and must not pollute the registry unboundedly); each of the 12 `<domain>/tools/_cache.py` files changes its singleton line from `DocCache()` to `DocCache("<domain>")` (12 one-line edits), and `_doc_cache.py` stays free of any `opentelemetry.*` import (the hit/miss counters are plain ints plus the `stats()` accessor; the observable-instrument callback lives in `telemetry/` and reads the registry) -- consistent with the base-library dependency split, since `opentelemetry-api` is not a base `dependencies` entry.
- **Domain-lock wait time**: add one new, shared timing helper in `telemetry/` (e.g. `record_lock_wait(domain, duration)`) and call it from each of the 13 `<domain>/tools/_lock.py` files' `_lock_for`/`<domain>_lock` context manager, rather than re-implementing the timing/recording logic independently 13 times -- this is what actually mirrors the `feat-99-list-item` "one shared helper, many wiring sites" precedent (a single shared implementation, many call sites), not 13 independent implementations of the same logic. **Implementation detail confirmed against the current code** (e.g. `req/tools/_lock.py`'s `req_lock`): every `<domain>_lock` context manager today is `lock = _lock_for(id_); with lock: yield`, which times *acquire-plus-hold*, not *wait*, if wrapped naively. Recording actual wait time requires each of the 13 context managers to change to an explicit `start = time.monotonic(); lock.acquire(); record_lock_wait(domain, time.monotonic() - start); try: yield finally: lock.release()` shape (or equivalent), not just calling the new helper somewhere inside the existing `with lock:` block. Because this changes the acquire/release control flow of 13 concurrency-critical context managers, not just their timing, Task 5.6 must also assert every refactored context manager still releases its lock on every exit path -- including when the caller's `yield`-wrapped body raises -- as a regression check independent of the new timing metric itself. Import safety (pinned in this re-evaluation): the 13 `_lock.py` modules are base-library-safe today (pure-stdlib `threading`), and `opentelemetry-api` is **not** a base `dependencies` entry -- so the shared `record_lock_wait(domain, duration)` helper must live in a `telemetry/` module that carries a module-level meter slot (default `None` -> the helper is a no-op) which the Task 4.1 bootstrap sets, and each `_lock.py` imports only that slot-holder module, never `opentelemetry.*` or `telemetry/otel.py` directly.
- **Redaction scope and limits (revised -- an unconditional "never, under any configuration" guarantee for free-text titles is not achievable)**: Phase 4's tracing does **not** create a new span in this feature's own middleware -- it only configures a `TracerProvider`/exporter so the SDK's already-present, built-in `OpenTelemetryMiddleware` starts actually exporting the spans it has always been (inertly) creating. That means essentially all trace spans, including `span.record_exception(e)`/`span.set_status(str(e))` on every exception, come from SDK-owned code, not this feature's own. Per stakeholder decision, Phase 6 implements a **global, `TracerProvider`-level `SpanProcessor`** (`on_end` hook; per Task 1a.3's confirmed finding -- the hook receives a `ReadableSpan` with no public mutation API, and the SDK locks the span's attribute container at `end()`, so the scrub must replace the span's private `_attributes` container and the exception `Event`'s private `_attributes` container with scrubbed copies, see Decisions Made) that scrubs every span the process creates -- including the SDK's built-in middleware's spans -- not only a filter local to this feature's own code (ACC-012), and the logging-side redaction runs inside every specmgr-installed `Handler`'s own formatter (both the console/stderr handler and the opt-in file-sink handler from Phase 2), so it scrubs every rendered record reaching those handlers regardless of which logger emitted it -- not only records whose logger name happens to be under `biz.dfch.specmgr` (application point pinned in this re-evaluation: **formatter level on the final rendered string**, because a handler-attached `logging.Filter` sees `LogRecord.msg`/`args`/`exc_info` but not the `exc_text` the formatter renders afterwards, and tracebacks embed absolute file paths; concretely, the JSON `Formatter` (console JSON + file sink) and a `RichHandler` subclass for the rich console format each pass their rendered output through the same scrub function). This matters because `SPECMGR_LOG_ENABLED=true` reconfigures the root logger, so third-party and MCP-SDK-internal loggers also emit through the same handlers once logging is enabled; scoping the filter to only specmgr's own logger namespace would leave that other output unredacted.
  **However**, this scrubbing has a real, verified limit that the original "no log record or span attribute ever contains ... a title" wording did not acknowledge. Absolute filesystem paths have a structural signature a regex can target (with known false-positive/negative risk, e.g. paths containing spaces, or non-POSIX/non-Windows-shaped strings). A document/artifact **title** does not: it is arbitrary free text with no distinguishing shape, so a generic `Filter`/`SpanProcessor` operating on an already-rendered string cannot know that a given substring *is* "the title" as opposed to any other quoted text. This is not hypothetical -- grepping the codebase confirms concrete existing call sites that already embed exactly this content in an exception's `str()` message, which the SDK's `record_exception(e)`/`set_status(str(e))` calls capture verbatim: `uc/models/v1/parser.py`'s `UcParseError(f"unrecognized H2 heading {node.title!r}")` (and its sibling `*ParseError` call sites using `node.title`/`child.title`), and `general/tools/delete.py`'s `DeleteError(f"failed to delete {path}: {ex}")` (`path` is an absolute `Path`). Redaction therefore has two distinct components, not one: (1) a *structured*-attribute guarantee -- this feature's own code (the middleware, the meter, the status resource) never attaches a full body/path/title as a dedicated attribute value, which is fully achievable since this feature controls what it names and sets; and (2) a *free-text* backstop -- the regex-based absolute-path scrub described above, plus a narrow, message-text-only rewording (Task 6.7) of the title-embedding exception call sites in **non-deprecated domains** (the verified mandatory set today: the 13 `UcParseError` sites in `uc/models/v1/parser.py`), carved out of the Explicitly-Out-Of-Scope exception-handling-convention exclusion (see Scope). The deprecated **ADR domain's 9 title-embedding sites** (`models/adr/v1/parser.py`'s `AdrParseError` x8 and `models/adr/v1/mutations.py`'s `AdrOptionNotFoundError` x1) are **explicitly excluded** from that rewording as an accepted residual gap -- the ADR domain is planned for phase-out (mirroring its exclusion from the `feat-107` cache), so its parse-error messages can still carry a title into free-text log/span content, and the path scrub still applies to them; path-embedding sites (`DeleteError`, the 12 domains' `<domain>/tools/_io.py` `*NotFoundError`-style messages, `feat/tools/_paths.py` -- ~25 verified sites) are scrub-covered in logs/spans and optionally reworded, since absolute paths are already disclosed client-side by the existing `specmgr://config` resource. ACC-004/ACC-012/ACC-013 above and the VCR acceptance criteria they back (VCR `0a1c2f63...` AC-003, VCR `54fd355f...` AC-007) are worded to match this achievable, two-component guarantee rather than an unconditional universal one. Task 1a.3's spike (Phase 1; see Decisions Made) answered the open question whether component (2)'s `SpanProcessor` can actually mutate an already-recorded span's attributes/exception-event text via public `opentelemetry-sdk` API, or must rely on undocumented internals: it cannot via public API (the `on_end` hook receives a `ReadableSpan` with no `set_attribute`, the span's `BoundedAttributes` is locked immutable at `end()`, and event `BoundedAttributes` are immutable from construction), so Task 6.1 is written against the confirmed private-container-replacement mechanism and its fragility notes (Task 6.3 therefore also adds a Task 4.4-style canary test asserting those private containers exist and the replacement is visible end-to-end on the installed SDK, so the fragility fails loudly in CI rather than silently leaking).
- **Status resource**: a read-only MCP resource at the **final URI `specmgr://telemetry/status`** (fixed in this re-evaluation, no longer a candidate) returning a `list[str]` describing current logging/telemetry enablement state, reading the same parsed config object as the middleware; entry string format: `logging: disabled` | `logging: enabled (level=<LEVEL>, format=<rich|json>, file=<on|off>)` and `telemetry: disabled` | `telemetry: enabled (exporter=<console|otlp>)` -- i.e. each line reports enablement plus mode, per the REQ's/VCR's "in what mode"; since `telemetry/` is shared infrastructure rather than a document domain, register it from a new `general/resources/telemetry_status.py` module (added to `general/resources/__init__.py`'s imports and `__all__`, mirroring the existing `specmgr://version` precedent in `version.py`) rather than inventing a new domain-first location.
- **VCR maintenance**: each of Phases 2 through 6 below ends with a task extending the relevant initial VCR(s) (created in Phase 0) with concrete test references for whatever acceptance criteria that phase just made verifiable, per `.specmgr/conventions.md`'s VCR-early convention; Phase 9 confirms every VCR has reached `## Coverage: full`. Phase 6 extends **both** the logging VCR (log-record redaction) and the telemetry VCR (span redaction, via AC-007, already added to the VCR during the pre-Phase-1 implementation-readiness review) given the widened redaction scope above. Phase 5 similarly adds a new telemetry-VCR acceptance criterion (AC-008, added during the later plan-consistency review) for the metrics themselves being recorded with correct values/attributes -- a REQ `dacd01f4-ffd8-4363-a20b-5ac1ce11eef2` clause no acceptance criterion covered until that review, since the initial Phase-0 VCR draft predates any metrics design.
- **Risk flagged in the ADR**: `Server.middleware` is documented as a provisional API in the installed SDK ("signature and semantics change ... before v2 final") -- Phase 4 adds a dedicated compatibility test (Task 4.4) with two jobs: (a) a canary assertion pinning today's `Server.middleware`/`ServerMiddleware.__call__` shape, so a future `mcp` upgrade that changes either trips this test in CI before runtime fail-open ever needs to engage, and (b) a simulated-incompatibility assertion that the fail-open behavior actually engages when that contract is broken -- so a breaking SDK change is caught loudly (via the test) while the running server itself degrades quietly (via the fail-open policy). No upper version bound is added to the `mcp` dependency to further mitigate this (see Explicitly Out Of Scope) -- the compatibility test and the runtime fail-open policy are considered sufficient.
- **`AGENTS.md` doc drift, unrelated to this feature but folded into its Phase 8**: `AGENTS.md`'s `general/` bullet currently lists only `specmgr://version`/`iso25010`/`dtais`/`rasci` and omits the already-existing `specmgr://config`/`specmgr://ears` resources (both correctly documented in `docs/GENERATED.md` and `server.py`'s own module docstring). Task 8.2 fixes this in passing while already editing that bullet for the new `telemetry/` package.

### Related Decisions

- ADR fdbb6d22-278a-4ecf-b2f6-db208bc49fc6: Adopt stdlib logging and OpenTelemetry for SpecMgr logging and telemetry.

### Task List

#### Phase 0: Requirements finalization and initial verification records

- [x] Task 0.1: Reword REQ `bc356fc9-964a-4274-93ec-4627c5aeb2e5` (Logging) per the second elicitation round: every MCP item, id/type-only logging (`set_status` includes the actual status), opt-in file sink, redaction extended to titles, correlation ID on error responses only.
- [x] Task 0.2: Reword REQ `dacd01f4-ffd8-4363-a20b-5ac1ce11eef2` (Telemetry) per the same round: every MCP item, fail-open on a broken `Server.middleware` contract, graceful OTLP-unreachable degradation, always-on sampling, MCP-specific metric/span naming.
- [x] Task 0.3: Create REQ `41444084-6821-426d-84a2-028a3f4fed0b` (MCP Server Logging/Telemetry Status Resource), cross-linked to the two REQs above.
- [x] Task 0.4: Finalize QA `40a17fb8-c069-4c38-a092-47912e1fa41d`: formalize the second round's raw answers into prose, and answer the four previously-pending questions (trace sampling, `Server.middleware`, correlation-ID/`ctx` exposure, `rpc.server.duration` naming impact).
- [x] Task 0.5: Author three initial, partial-coverage VCRs, one per REQ above (`0a1c2f63-9576-4463-bf40-8f771f14fefb` logging, `54fd355f-0978-4dd0-9e1d-da89432e5321` telemetry, `f494d230-97f3-4db9-9ef9-ed9c903ee008` status resource), each populated only with acceptance criteria genuinely derivable from its REQ's specified configuration/behavior contract.
- [x] Task 0.6: Add the "author VCR early, extend phase-by-phase" convention to `.specmgr/conventions.md`, generalizing this phase's approach for future features.
- [x] Task 0.7: Implementation-readiness review: codebase research confirming every ADR/plan technical claim against the installed `mcp==2.0.0` SDK, plus stakeholder decisions on static-config failure mode (fails closed), redaction scope (global `SpanProcessor`, not just this feature's own attributes), and the `mcp` dependency's version bound (left unbounded); add the "Observability Degrades Gracefully, Application Logic Never Does" convention to `.specmgr/conventions.md` documenting the fail-open/fail-closed split as a deliberate, bounded exception to the existing error-handling convention.
- [x] Task 0.8: Plan-consistency review: cross-checked the Task List, Design Notes, and all three VCRs against each other and against the REQs, and resolved the gaps found -- a premature "increment call/error counters" claim in Phase 3's Task 3.1 (no `MeterProvider` exists until Phase 4/5), a missing fail-closed validation case for `SPECMGR_OTEL_EXPORTER=otlp` without `SPECMGR_OTEL_ENDPOINT`, a missing VCR acceptance criterion for the metrics themselves (added as AC-008 to VCR `54fd355f-0978-4dd0-9e1d-da89432e5321`), a naming ambiguity between the `mcp.tool.*` metric namespace and the "every MCP item" REQ scope (resolved via a new `mcp.item.type` attribute), an under-specified redaction-filter attachment point (resolved as handler-level, not logger-name-scoped), and moved the OTLP-unreachable de-duplication task from Phase 5 into Phase 4 where the exporter it concerns is actually configured.
- [x] Task 0.9: Plan-fix review against the actually-installed `mcp==2.0.0` SDK source (`mcp/server/context.py`, `mcp/server/_otel.py`, `mcp/server/mcpserver/server.py`, `mcp/server/mcpserver/utilities/logging.py`) and the current codebase (`general/tools/_doc_cache.py`, the 13 `_lock.py` files, `commands/mcp.py`, `pyproject.toml`), resolving six gaps that survived Tasks 0.7/0.8: (1) `ServerMiddleware` wraps every JSON-RPC method (`initialize`, `*/list`, `ping`, `notifications/*`), not only item invocations -- added ACC-013, a method-name filter to Task 3.1, and a new Task 3.7 test; (2) `resources/read` identifies its target via `ctx.params["uri"]`, not `["name"]` (confirmed: the SDK's own `OpenTelemetryMiddleware` only extracts `name` for `tools/call`/`prompts/get`) -- reworded Task 3.1 and Task 5.1 accordingly; (3) no task wired `telemetry/config.py`/`telemetry/logging.py`/`telemetry/otel.py` into `server.py`'s actual startup path (only the middleware append, Task 3.2, was explicit) -- added Tasks 1.6/2.5/4.9; (4) the redaction guarantee (ACC-004/ACC-012) was unachievable as worded for free-text document/artifact titles, since a title has no structural signature a generic filter can detect, confirmed against real existing exception messages that already embed one (`UcParseError`'s `f"...{node.title!r}"`, `DeleteError`'s `f"failed to delete {path}: {ex}"`) -- reworded ACC-004/ACC-012, the redaction Design Notes bullet, and the Scope bullets into an achievable two-component guarantee (structured-attribute redaction, fully achievable; a regex-based path scrub plus narrow message-text rewording of the enumerated exception classes as a free-text backstop, added as new Task 6.7 with a corresponding carve-out in Explicitly Out Of Scope); (5) the correlation ID generated in Phase 3 (before any span exists) and "tied" to a span in Phase 4 had no described reconciliation mechanism for how they end up actually equal -- reworded the Design Notes' "Correlation ID exposure" bullet and Task 4.2 to specify a single helper that prefers the current recording span's trace ID and falls back to `uuid4()` when none exists; (6) domain-lock wait-time timing (Task 5.5) cannot be obtained by wrapping the existing `with lock:` blocks unchanged, since that times acquire-plus-hold, not wait -- clarified Task 5.5 to require switching to explicit `acquire()`/`release()` timing. Softer fixes: removed the inaccurate claim that `SPECMGR_LOG_ENABLED`-style flags "follow the existing `SPECMGR_*_DIR` naming convention" (no boolean-flag precedent exists) and made Task 7.1's OTLP exporter package choice a required, documented decision instead of "most likely." REQ `bc356fc9-964a-4274-93ec-4627c5aeb2e5`, REQ `dacd01f4-ffd8-4363-a20b-5ac1ce11eef2`, VCR `0a1c2f63-9576-4463-bf40-8f771f14fefb`, and VCR `54fd355f-0978-4dd0-9e1d-da89432e5321` were updated in the same pass so their redaction/scope wording matches. No implementation code was written during this review.

#### Phase 1: Config surface, status resource, and OpenTelemetry SDK technical spike

- [x] Task 1.1: Add `telemetry/config.py` parsing the eight env vars above (including the two file-sink vars) into a typed, validated config object, all defaulting to disabled; raise a dedicated config-error exception (a `ValueError` subclass, e.g. `TelemetryConfigError` -- the type Task 1.6's CLI error surface catches) immediately on an invalid/incomplete combination (`SPECMGR_LOG_FILE_ENABLED=true` without a non-blank `SPECMGR_LOG_FILE_PATH`, `SPECMGR_OTEL_EXPORTER=otlp` without a non-blank `SPECMGR_OTEL_ENDPOINT`, an `SPECMGR_LOG_FORMAT`/`SPECMGR_OTEL_EXPORTER` value outside its closed set, an `SPECMGR_LOG_LEVEL` outside the five named levels, or a boolean-flag var outside `true`/`false`) -- static misconfiguration fails closed, unconditionally (regardless of the parent `SPECMGR_LOG_ENABLED`/`SPECMGR_OTEL_ENABLED` switch), per the Design Notes above (ACC-011).
- [x] Task 1.2: Unit tests for config parsing (defaults, valid values, and each invalid/incomplete combination raising -- including the `SPECMGR_OTEL_EXPORTER=otlp`/`SPECMGR_OTEL_ENDPOINT` pairing -- and confirming validation fires even when the parent enable switch is `false`).
- [x] Task 1.3: Add the read-only status resource at the fixed URI `specmgr://telemetry/status` (new `general/resources/telemetry_status.py` module, registered per the Design Notes' "Status resource" bullet), returning `list[str]` in the bullet's pinned entry format, reporting current logging/telemetry enablement plus mode per REQ `41444084-6821-426d-84a2-028a3f4fed0b`; unit tests.
- [x] Task 1.4: Extend VCR `f494d230-97f3-4db9-9ef9-ed9c903ee008` (status resource) with concrete test references for AC-001/AC-002; move `## Coverage` to `full` once both are demonstrably covered.
- [x] Task 1.5: Quality gate (ruff, vulture, pytest).
- [x] Task 1.6: Call the Task 1.1 config validation unconditionally at `server.py`'s module scope, at (or immediately before) the point `mcp = MCPServer(...)` is constructed -- the same import-time location Task 3.2 later appends the middleware to -- so a static misconfiguration actually makes `specmgr mcp` refuse to start (ACC-011) rather than only raising when a test calls `telemetry/config.py` directly. Add a unit/integration test confirming `commands/mcp.py`'s `mcp` command (which triggers this via `from ..server import mcp as mcp_server`) fails before touching stdio when a required env var combination is invalid. CLI error surface: `commands/mcp.py`'s `mcp` command currently catches only `ImportError` around its lazy server import -- extend that handling to also catch the Task 1.1 config error and exit with a single clear stderr line naming the offending env var(s) plus exit code 1 (mirroring the existing ImportError branch), rather than a raw traceback (ACC-011's "clear error"); the test asserts that clean message.
- [x] Task 1a.1: Add `opentelemetry-sdk` and an OTLP exporter package under the existing `mcp` extra in `pyproject.toml` (not a new extra -- telemetry is meaningless without the MCP server) and regenerate `uv.lock`, now rather than in the original Phase 7, since Phases 4-6 need to import SDK symbols (`TracerProvider`, `MeterProvider`, `SpanProcessor`) that do not exist in the already-installed `opentelemetry-api` alone. Decide and document, in this task's own commit/PR description, which OTLP exporter protocol package is used (e.g. `opentelemetry-exporter-otlp-proto-http` vs. `-grpc`) and why, rather than leaving the choice implicit or deferred. (Numbered `1a` rather than `1.7`: this and the following spike tasks are a deliberately separable, run-first-and-re-evaluate batch appended to Phase 1's own checklist -- the `Phase N: title` heading itself requires an integer `N`, so a fractional "Phase 1a" heading is not schema-valid; these tasks stay inside Phase 1 rather than becoming a heading of their own, while keeping their own `1a.N` sub-numbering so Design Notes/Task List cross-references elsewhere in this document can name them precisely.)
- [x] Task 1a.2: Spike: instantiate `opentelemetry.sdk.trace.export.ConsoleSpanExporter` and `opentelemetry.sdk.metrics.export.ConsoleMetricExporter` directly and confirm their actual default output stream; confirm that constructing them with their output redirected to stderr (e.g. `out=sys.stderr`) is sufficient, then run a real `specmgr mcp` stdio smoke test with `SPECMGR_OTEL_ENABLED=true` and the default `SPECMGR_OTEL_EXPORTER=console` and assert zero bytes are ever written to stdout beyond JSON-RPC frames. Record the finding in Decisions Made; Task 4.1 must incorporate whatever redirection this spike confirms is required.
- [x] Task 1a.3: Spike: implement a minimal custom `SpanProcessor` and confirm, against the installed `opentelemetry-sdk`, whether its `on_end()` hook can actually mutate a `ReadableSpan`'s already-recorded attributes and exception-event message text using only public SDK API, or whether this requires relying on undocumented/private internals (e.g. reassigning `span._attributes`). Record the finding and its implication for Phase 6's redaction design in Decisions Made -- including, if private internals turn out to be required, whether `opentelemetry-sdk`'s version range in `pyproject.toml` should be narrowed beyond a bare lower bound to reduce the risk of a future upgrade silently breaking redaction.
- [x] Task 1a.4: Spike: point the OTLP exporter at a deliberately unreachable endpoint and observe the OTel SDK's actual batch-span-processor/periodic-metric-reader failure-logging behavior (does it log once per failed export cycle, once per failed attempt, or once per episode; under which logger name) to ground Task 4.5's de-duplication design in confirmed behavior rather than the Design Notes' previous "likely" assumption.
- [x] Task 1a.5: Spike: prototype an OTel observable/asynchronous instrument (e.g. an observable counter with a callback) reading a throwaway stand-in for `DocCache.stats()`, confirming it behaves the way Task 5.4 assumes before that task is implemented against the real, 12-independent-instance `DocCache`.
- [x] Task 1a.6: Record every finding from Tasks 1a.2-1a.5 as its own Decisions Made entry, and revise the affected Phase 4/5/6 Design Notes/Task List wording to match confirmed behavior wherever it differs from the current (assumption-based) text -- the re-evaluation gate this batch of tasks exists to reach before Phase 2 continues toward Phase 4-6's territory.
- [x] Task 1a.7: Quality gate.

#### Phase 2: Structured logging

- [x] Task 2.1: Add `telemetry/logging.py`: a JSON `Formatter` and a setup function building on the MCP SDK's `configure_logging`/rich handler, selected by `SPECMGR_LOG_FORMAT`, gated by `SPECMGR_LOG_ENABLED`, plus the opt-in JSON-only file sink gated by `SPECMGR_LOG_FILE_ENABLED`/`SPECMGR_LOG_FILE_PATH`. Per the Design Notes' wiring bullet, this setup runs at `server.py`'s module scope **before** `mcp = MCPServer(...)` is constructed, so the SDK's own `configure_logging()` call (a `logging.basicConfig`, hence a no-op once the root logger has handlers) runs *afterwards* and cannot override the enabled configuration -- but the setup must still be explicit and idempotent (install exactly the intended root-handler set with the selected formatter/level, correct in either handler state) rather than relying on a bare `logging.basicConfig()`, which would silently no-op against whatever handlers exist and leave `SPECMGR_LOG_FORMAT`/`SPECMGR_LOG_LEVEL` without effect. Log record shape (pinned in this re-evaluation): JSON records carry `timestamp` (ISO 8601), `level`, `logger`, `message`, plus the structured fields `correlation_id`, `method`, `item_type`, `item_name`, `domain`, `status` (only for `set_status` invocations), `duration_ms`, and `exception` (type + message + traceback); the rich format renders the same fields human-readably; the formatter-level path scrub the Design Notes' "Redaction scope and limits" bullet defines is wired onto these same handlers' formatters by Task 6.2.
- [x] Task 2.2: Unit tests asserting stdout is never written to, in either format, that the JSON formatter produces valid, parseable JSON, that the file sink is JSON-only and inactive unless both its own switch and the main logging switch are enabled, and that enabling `SPECMGR_LOG_ENABLED` after the SDK's own default `configure_logging()` call actually changes the effective format/handlers (no silent no-op).
- [x] Task 2.3: Extend VCR `0a1c2f63-9576-4463-bf40-8f771f14fefb` (logging) with concrete test references for AC-001/AC-005 now that this phase makes them demonstrable; keep `## Coverage: partial` if other criteria remain outstanding.
- [x] Task 2.4: Quality gate.
- [x] Task 2.5: Call the Task 2.1 logging setup unconditionally at `server.py`'s module scope, at the same import-time location as Task 1.6, after config validation and before Task 3.2's middleware append -- so `SPECMGR_LOG_ENABLED`/`SPECMGR_LOG_FORMAT` actually take effect the moment `specmgr mcp` runs, not only when a test calls `telemetry/logging.py` directly.

#### Phase 3: Correlation ID + call middleware

- [x] Task 3.1: Add `telemetry/middleware.py`'s `ServerMiddleware` implementation: **first, if neither `SPECMGR_LOG_ENABLED` nor `SPECMGR_OTEL_ENABLED` is `true` (the default), pass every method -- including the three invocations -- straight to `call_next(ctx)` unmodified (no correlation ID, no log record, no metric increment, no exception conversion), so the default config is a byte-for-byte no-op over the SDK's own behavior (see the Design Notes' "Middleware enablement gating" bullet); otherwise** filter on `ctx.method in {"tools/call", "resources/read", "prompts/get"}` (ACC-013) -- pass every other method straight to `call_next(ctx)` unobserved -- then, for a matching method, extract the invoked item's identity from the correct per-method params key (`ctx.params["name"]` for `tools/call`/`prompts/get`, `ctx.params["uri"]` for `resources/read`; see Design Notes), generate a correlation ID via the shared helper described in the Design Notes' "Correlation ID exposure" bullet (current span's trace ID if one is active, else `uuid4().hex`), and log start/completion/error records, including, for `set_status`, the new status value per REQ `bc356fc9-964a-4274-93ec-4627c5aeb2e5`. Error detection is per-method (see the Design Notes' "Per-method error channels" bullet): for `tools/call` a failure arrives as a *result*, not an exception -- the middleware must inspect the post-`call_next` dumped wire dict for `isError: true` (the tool error record's text comes from the result's content, i.e. the SDK's `str(e)`) in addition to the exception path the other two methods use. Does not create or increment any OTel counter/histogram yet -- no `MeterProvider` exists until Phase 4, and the counters themselves are Task 5.2/5.3's responsibility once the Task 5.1 domain mapping exists (see Design Notes).
- [x] Task 3.2: Append the middleware to `mcp.middleware` in `server.py`, wrapping the append itself in a minimal try/except so an incompatible `Server.middleware` contract (the provisional-API risk the ADR flags) cannot crash `specmgr mcp` at startup even before Phase 4's full fail-open policy (Task 4.3) exists -- log a single warning and continue with logging/telemetry unappended rather than propagating the exception; Task 4.3 later upgrades this into the complete config-aware disable-with-warning behavior, it is not a second, independent guard.
- [x] Task 3.3: Attach the correlation ID to a client-visible error response only, never to a successful result, **and only when at least one of `SPECMGR_LOG_ENABLED` / `SPECMGR_OTEL_ENABLED` is `true` (when both are off the middleware is Task 3.1's pure pass-through: no attachment, no conversion, so the SDK's own default error handling -- including its stderr `logger.exception` of a raw exception -- is preserved) -- via the three per-channel mechanisms pinned in the Design Notes' "Per-method error channels" bullet:** (a) `tools/call` error results: merge `"_meta": {"correlationId": ...}` into the returned result dict (verified: `CallToolResult.meta` dumps as `_meta`); (b) `MCPError` on any of the three methods: merge `correlationId` into `e.error.data`, preserving the SDK's own `uri` entry where present; (c) raw exceptions/`ValidationError` on any of the three methods: convert to a wire-identical `MCPError` (same code/message the dispatcher would otherwise produce, plus the `data` carrying the correlation ID) and re-raise -- the accepted side effect being that the SDK's own server-side `logger.exception` of that traceback (under `mcp.server.runner`) is replaced by this feature's own error log record, which carries the correlation ID anyway. Success results (including non-error `tools/call` results) never receive the ID.
- [x] Task 3.4: Unit tests exercising the middleware directly against a fake `call_next`, covering the success and failure paths -- where the tool path's failure is an `isError: true` *result dict* (not a raised exception, per the Design Notes' "Per-method error channels" bullet) and the resource/prompt paths' failures are raised exceptions (both the raw-exception and `MCPError` shapes) -- asserting the correlation-ID attachment lands in `_meta` for tool error results and in `error.data` for JSON-RPC errors while never appearing on any success result, and asserting that a `set_status` invocation's log record includes the new status value while other invocations include only `id`/`type`. Add a case confirming the enablement gate: with both `SPECMGR_LOG_ENABLED` and `SPECMGR_OTEL_ENABLED` unset/false, a failing call that raises a raw exception passes through the middleware unmodified -- no correlation ID attached, no exception conversion -- i.e. the middleware is a pure pass-through in the default config.
- [x] Task 3.5: Extend VCR `0a1c2f63-9576-4463-bf40-8f771f14fefb` (logging) with concrete test references for AC-002/AC-004; keep `## Coverage: partial` if other criteria remain outstanding.
- [x] Task 3.6: Quality gate.
- [x] Task 3.7: Unit tests confirming the Task 3.1 method-name filter: `initialize`, `tools/list`, `resources/list`, `prompts/list`, `ping`, and a representative `notifications/*` message produce no log record, no correlation ID, and (once Phase 5 exists) no metric increment, while `tools/call`/`resources/read`/`prompts/get` do; and confirming a `resources/read` invocation's logged identity comes from `ctx.params["uri"]`, not a `"name"` key that isn't present for that method.

#### Phase 4: OpenTelemetry SDK bootstrap

- [x] Task 4.1: Add `telemetry/otel.py`: configure `TracerProvider`/`MeterProvider` + exporter (`console`/`otlp`) only when `SPECMGR_OTEL_ENABLED`; this activates the SDK's existing built-in `OpenTelemetryMiddleware` (no new span is created by this feature's own code -- see Design Notes). The `console` exporter (the default `SPECMGR_OTEL_EXPORTER` value) must be constructed with its output stream explicitly redirected to stderr -- per Task 1a.2's confirmed spike finding (Phase 1; see Decisions Made), exactly `ConsoleSpanExporter(out=sys.stderr)` and `ConsoleMetricExporter(out=sys.stderr)` (both default to stdout, and `out=` must be passed by keyword -- `ConsoleSpanExporter`'s first positional arg is `service_name`) -- this is not optional polish, it is required for ACC-001/ACC-003 to hold once telemetry is enabled (the spike's real-`specmgr mcp` stdio smoke test measured zero non-JSON-RPC bytes on stdout with the redirection, and a full `resource_metrics` console blob contaminating stdout without it; the spike also confirmed mcp 2.0.0's stdio transport diverts fd 1 to stderr only *while serving*, so default-stream exports after serving ends -- e.g. the `PeriodicExportingMetricReader`'s final collection at `MeterProvider.shutdown()` -- still reach real stdout). Also configure a `Resource` with a `service.name` attribute (e.g. `"specmgr"`) on the `TracerProvider`/`MeterProvider`, so exported spans/metrics are identifiable at a shared OTLP collector/backend -- no env var override is needed for this; a fixed value is sufficient. Per the Design Notes' "OTel bootstrap pins" bullet: use a `BatchSpanProcessor` (not `SimpleSpanProcessor`), a `PeriodicExportingMetricReader` (default 60 s interval), construct the OTLP exporters as `OTLPSpanExporter(endpoint=...)`/`OTLPMetricExporter(endpoint=...)` with the `SPECMGR_OTEL_ENDPOINT` base URL as-is, apply the Task 4.5 wrapper to both OTLP exporters, fetch the `Meter` once into a module-level slot for the Phase 5 increments, create the histograms with unit `ms` and the bullet's explicit bucket advice, and register `TracerProvider.shutdown()`/`MeterProvider.shutdown()` at process exit in `server.py`'s existing `_lifespan` placeholder's post-`yield` section.
- [x] Task 4.2: Confirm/test that the Task 3.1 correlation-ID helper's span-ID branch (see Design Notes) actually engages once `SPECMGR_OTEL_ENABLED=true` -- this is a verification of behavior Task 3.1 already implements, not new "tying" logic added here, since Phase 3 is deliberately built against the same helper from the start. The test must use the real import order (import `server` -> run the Task 4.1 bootstrap -> make a call), not the Phase 1a spike's pre-import provider installation, since `mcp/shared/_otel.py` fetches its tracer at module-import time and only the OTel API's proxy resolution (verified in this re-evaluation) makes the later bootstrap engage the SDK's built-in middleware.
- [x] Task 4.3: Implement the fail-open policy: if the installed SDK's `Server.middleware` contract is incompatible with this feature's middleware, log a single warning and continue operating with logging/telemetry disabled, rather than failing to start. Builds on the minimal try/except Task 3.2 already wraps its own `mcp.middleware.append(...)` call in from Phase 3 -- that guard alone only prevents a crash at append time; this task adds the config-aware single-warning-and-disable behavior described above, so the narrow Phase-3 protection and this phase's full policy are the same fail-open mechanism, not two independent ones.
- [x] Task 4.4: Compatibility test guarding against a breaking change to the installed SDK's provisional `Server.middleware` contract: (a) a canary assertion pinning the currently-installed SDK's `Server.middleware` list type and `ServerMiddleware.__call__` signature, so a future `mcp` upgrade that changes either trips this test in CI, and (b) a simulated-incompatibility assertion that the fail-open behavior from Task 4.3 actually engages when the contract is broken.
- [x] Task 4.5: Implement graceful OTLP-unreachable degradation: at most one stderr message per failure episode, a further single message only after a subsequent reconnect-then-drop cycle, never spamming. Per Task 1a.4's confirmed spike finding (Phase 1; see Decisions Made) -- the installed SDK logs 3x WARNING (one per internal retry attempt) + 1x ERROR per failed export cycle, on every cycle, under `opentelemetry.exporter.otlp.proto.http.trace_exporter`/`...metric_exporter`, and the exporter returns `FAILURE` without raising (so the SDK's own exception paths and `opentelemetry.sdk._shared_internal`'s built-in 20-second `DuplicateFilter` never engage, and no success is ever logged) -- implement a thin wrapper around the configured OTLP exporter that tracks first-failure/recovery transitions itself and emits at most one stderr message per episode (suppressing the exporter's own logger while the wrapper is active); a bare `logging.Filter` on those logger names cannot meet the "never repeating while the condition persists" requirement on its own, because recovery is not observable from the log stream. Wrapper shape (pinned in this re-evaluation): one shared wrapper class applied to both the OTLP span and the OTLP metric exporter (two instances); an episode runs from the first `FAILURE` return until the first `SUCCESS` or provider shutdown; exactly one stderr message (via this feature's own logger) is emitted at episode start and a further single message at the start of any subsequent episode; while the wrapper is in the failing state it suppresses the two exporters' own loggers (`opentelemetry.exporter.otlp.proto.http.trace_exporter` / `...metric_exporter`, the names Task 1a.4 confirmed) via a state-gated `logging.Filter` on exactly those two logger names; the wrapper passes through `export()`'s result and the `force_flush()`/`shutdown()` signatures unchanged; the console exporter needs no wrapper. Moved here from an earlier draft's Phase 5, since it is squarely about the OTel exporter/SDK bootstrap this phase already owns.
- [x] Task 4.6: Unit tests for the Task 4.5 OTLP-unreachable degradation/de-duplication behavior.
- [x] Task 4.7: Extend VCR `54fd355f-0978-4dd0-9e1d-da89432e5321` (telemetry) with concrete test references for AC-001/AC-002/AC-003/AC-004, now that this phase makes span creation/correlation (AC-002) and OTLP degradation (AC-003) demonstrable in addition to off-by-default (AC-001) and middleware fail-open (AC-004); keep `## Coverage: partial` if other criteria remain outstanding.
- [x] Task 4.8: Quality gate.
- [x] Task 4.9: Call the Task 4.1 OTel bootstrap unconditionally at `server.py`'s module scope, at the same import-time location as Tasks 1.6/2.5, after logging setup and before Task 3.2's middleware append -- so `SPECMGR_OTEL_ENABLED` actually takes effect the moment `specmgr mcp` runs, not only when a test calls `telemetry/otel.py` directly.
- [x] Task 4.10: Unit test mirroring Task 2.2's logging stdout-safety test: with `SPECMGR_OTEL_ENABLED=true` and every supported `SPECMGR_OTEL_EXPORTER` value (including the default `console`), assert zero bytes are ever written to stdout, confirming Task 4.1's stderr-redirection requirement actually holds end-to-end and not only for the exporter constructor call in isolation (the assertion is scoped to the stdio transport, where stdout is the JSON-RPC channel; `sse`/`streamable-http` have no stdout protocol constraint, and the `out=sys.stderr` redirection applies uniformly regardless).
- [x] Task 4.11: Unit test confirming the Task 4.1 `Resource`/`service.name` attribute is present on every exported span and metric.

#### Phase 5: Metrics instrumentation

- [x] Task 5.1: Define an explicit tool/resource/prompt-name -> domain mapping (new design work; no such mapping exists today) used to derive the `mcp.domain` attribute for every metric/span, covering generic dispatch tools (via their own `type` argument), domain-specific tools (via `name`), resources (via `uri`, not `name` -- see Design Notes), and the no-domain case (e.g. `specmgr://version`).
- [x] Task 5.2: Tool-call latency histogram + call counters by tool/domain, in the middleware, via an OTel `Meter` (no-op when disabled), named per the MCP-specific scheme (`mcp.tool.duration`, `mcp.tool.name`, `mcp.domain`, `mcp.item.type`), using the Task 5.1 mapping, and the Task 4.1 module-level meter slot (increments are a no-op until the slot is set by the bootstrap); `mcp.tool.duration` uses unit `ms` with the Design Notes' "OTel bootstrap pins" bucket advice. This is the first place these counters/histograms are actually created (see the Phase 3/Phase 5 division-of-labor Design Note).
- [x] Task 5.3: Error-type counter using the per-channel signal pinned in the Design Notes' "Per-method error channels" bullet: `tool_error` (the SDK's own value) for `tools/call` `isError: true` results (no exception object exists at the middleware layer for tools), the `MCPError`'s JSON-RPC error code for SDK-wrapped errors, and `type(exc).__qualname__` for raw exceptions.
- [x] Task 5.4: Add hit/miss counters + a `stats()` accessor to the shared `DocCache` class; wire an observable meter instrument reading them. Since each domain has its own independent `DocCache` singleton (12 today, one per domain's `tools/_cache.py`), also add a small registry (e.g. a module-level list/dict of live `DocCache` instances, appended to at each domain's cache-module import time) that the meter callback iterates to attribute `mcp.cache.hit`/`mcp.cache.miss` per `mcp.domain` -- this registry does not exist today and is new design work, not a refactor of an existing mechanism. Per Task 1a.5's confirmed spike finding (Phase 1; see Decisions Made), code against the installed SDK 1.44.0's observable-instrument API: `meter.create_observable_counter(name, callbacks=[...])` (callbacks registered at creation time -- no public `add_callback`), the callback (called with a `CallbackOptions`) returning a list of `Observation`, and the default CUMULATIVE reader temporality (an unchanged collection re-reports the same cumulative value, so there is no drift to guard against). The callback must yield an `Observation` for every live registry entry every cycle (a series not yielded is absent from that collection), and its registry iteration must be defensive (a raising callback makes the SDK log `Callback failed for instrument ...` and drop the instrument's entire collection cycle). Registry shape (pinned in this re-evaluation; see the Design Notes' "Doc-cache hit/miss counters" bullet): `DocCache.__init__` gains an optional `domain` parameter and self-registers into a module-level `dict[str, DocCache]` in `general/tools/_doc_cache.py` (domain-keyed, last-wins, test-only reset mirroring `reset()`); each of the 12 `<domain>/tools/_cache.py` files changes its singleton line to `DocCache("<domain>")`; `_doc_cache.py` itself stays free of `opentelemetry.*` imports (plain-int counters + `stats()`), the observable-instrument callback living in `telemetry/`.
- [x] Task 5.5: Add a single shared timing helper in `telemetry/` (e.g. `record_lock_wait(domain, duration)`), and call it from each of the 13 `<domain>/tools/_lock.py` files' lock context manager, recording wait time to the same meter -- one shared implementation, many wiring sites, per the Design Notes. Because this changes the acquire/release control flow of 13 concurrency-critical context managers, Task 5.6 must also verify each refactored context manager still releases its lock on every exit path, including when the caller's `yield`-wrapped body raises -- not just that the new timing value is recorded correctly. The helper must be import-safe from the base library (see the Design Notes' "Domain-lock wait time" bullet): a `telemetry/` module with a meter slot set by the Task 4.1 bootstrap, no-op while unset; each `_lock.py` imports only that slot-holder module.
- [x] Task 5.6: Unit tests for each counter/histogram and for the Task 5.1 domain mapping, confirming metric attributes (`mcp.tool.name`, `mcp.domain`, `mcp.item.type`) are populated correctly. Also includes the Task 5.4 `DocCache`-registry test (metrics attribute correctly per domain across multiple live cache instances) and the Task 5.5 lock-release-on-exception regression test called for above.
- [x] Task 5.7: Extend VCR `54fd355f-0978-4dd0-9e1d-da89432e5321` (telemetry) with concrete test references for AC-005/AC-006 and for AC-008 (the metrics being recorded with correct values/attributes -- AC-008 is already present in this VCR, added during the plan-consistency review as the REQ `dacd01f4-ffd8-4363-a20b-5ac1ce11eef2` clause no earlier draft carried an AC for); keep `## Coverage: partial` pending Phase 6's span-redaction extension.
- [x] Task 5.8: Quality gate.

#### Phase 6: Redaction safeguards

- [x] Task 6.1: Add `telemetry/redact.py`: (a) a scrubbing formatter hook running at **formatter level on the final rendered string** in every specmgr-installed `Handler` (the console/stderr handler and the opt-in file sink alike) -- covering every rendered record reaching those handlers regardless of originating logger, not only records from specmgr's own logger namespace, since `SPECMGR_LOG_ENABLED=true` reconfigures the root logger and third-party/SDK loggers share the same handlers, and (pinned in this re-evaluation) a handler-attached `logging.Filter` alone cannot see the formatter-rendered `exc_text`, whose tracebacks embed absolute file paths -- plus a **global, `TracerProvider`-level `SpanProcessor`** (`on_end` hook or equivalent) applying the same logic to every span's attributes and exception events, including spans produced by the SDK's own built-in `OpenTelemetryMiddleware`, not only attributes this feature's own middleware/meter sets (ACC-012). Per the Design Notes' "Redaction scope and limits" bullet, this is a *free-text backstop*, not a title detector: it runs a regex-based scrub for absolute-filesystem-path-shaped substrings (POSIX `/...`, Windows drive `X:\...`, UNC `\\host\...` -> a fixed `<redacted-path>` token). It does not, and structurally cannot, reliably detect an arbitrary document/artifact title embedded in free text -- that is Task 6.7's job, not this filter's. Per Task 1a.3's confirmed spike finding (Phase 1; see Decisions Made), `on_end()` receives a `ReadableSpan` with **no public mutation API** (no `set_attribute`; the `attributes` property is a `MappingProxyType`; the span's `BoundedAttributes` is locked immutable at `end()`, and event `BoundedAttributes` are immutable from construction), so the processor must scrub by **replacing the private containers** before any later exporter reads the span: `readable_span._attributes = BoundedAttributes(None, scrubbed, immutable=False)` (a plain dict works equally well) and, for the exception event, `event._attributes = BoundedAttributes(None, scrubbed, immutable=False)` (or rebuilding `readable_span._events` with new `Event` objects) -- both proven visible to the exporter (in-memory exporter in simple and batch chains, and the OTLP protobuf via `_encode_span`). The span scrub covers every attribute value plus each event's attributes -- in particular the `exception.message`/`exception.stacktrace`/`exception.type` keys `record_exception` sets (Task 1a.3 confirmed the OTLP protobuf carries them) -- and this re-evaluation pins the per-event `event._attributes` replacement (Task 1a.3 confirmed both per-event replacement and `_events` rebuilding work; per-event is the simpler mechanism). Fragility to carry into implementation and the Task 6.3 tests: this relies on the private field names `ReadableSpan._attributes`/`_events`, `Event._attributes` and on the SDK reading those live containers at export time -- a future SDK release that restructures them would silently stop redacting (spans leak, no error), which is why Task 1a.3's Decisions Made entry carries the record-only recommendation to narrow `opentelemetry-sdk` to `<2.0.0` in `pyproject.toml` (applied by Task 7.1).
- [x] Task 6.2: Wire the scrubbing formatter hook onto every handler installed by the logging setup from Phase 2 (console/stderr and the opt-in file sink alike -- replacing/wrapping Phase 2's formatters so the scrub runs on their final rendered string, per this re-evaluation's pinned application point), and the span processor into the `TracerProvider` from Phase 4 (so it applies process-wide, not just to spans this feature's own code creates).
- [x] Task 6.3: Unit tests asserting the Task 6.1 path scrub redacts an absolute-path-shaped substring in representative log payloads -- including formatter-rendered `exc_text` tracebacks carrying fake absolute file paths -- regardless of which logger emitted them, and that the span processor does the same for a span's attributes and exception event (incl. the `exception.message`/`exception.stacktrace` keys) even when that span is created entirely by the SDK's built-in `OpenTelemetryMiddleware` (e.g. inject an exception whose message contains a fake absolute path and assert the exported span no longer contains it) -- not only spans this feature's own middleware would create. Plus this re-evaluation's pinned canary test (mirroring Task 4.4's pattern): assert the installed SDK's `ReadableSpan._attributes`/`_events` and `Event._attributes` private containers exist and that the Task 6.1 replacement is visible end-to-end, so a future SDK restructure fails loudly in CI instead of silently leaking. Title redaction is verified separately, at the exception-message level, by Task 6.7's own tests -- not by asserting the Task 6.1 scrub catches an arbitrary title, which it does not attempt to do.
- [x] Task 6.4: Extend VCR `0a1c2f63-9576-4463-bf40-8f771f14fefb` (logging) with a concrete test reference for AC-003; move `## Coverage` to `full` if every logging AC is now demonstrably covered.
- [x] Task 6.5: Extend VCR `54fd355f-0978-4dd0-9e1d-da89432e5321` (telemetry) with a concrete test reference for AC-007 (span-level redaction, already added to this VCR during the pre-Phase-1 implementation-readiness review); move `## Coverage` to `full` if every telemetry AC (through AC-008, added in Phase 5) is now demonstrably covered.
- [x] Task 6.6: Quality gate.
- [x] Task 6.7: Grep for and enumerate every existing exception-message call site that embeds a document/artifact title or an absolute filesystem path verbatim (the verified census from this re-evaluation: **title-embedding, 22 sites** -- `uc/models/v1/parser.py`'s `UcParseError(f"...{node.title!r}")`/`f"...{child.title!r}"` x13, `models/adr/v1/parser.py`'s `AdrParseError` x8, `models/adr/v1/mutations.py`'s `AdrOptionNotFoundError(f"no option with full title {full_title!r}")` x1; **path-embedding, ~25 sites** -- the 12 domains' `<domain>/tools/_io.py` `*NotFoundError` messages (`"...{id_!r} exists at {path}..."`), `general/tools/delete.py`'s `DeleteError(f"failed to delete {path}: {ex}")` x10, `feat/tools/_paths.py` x3; `models/md/*`'s `{path}` f-strings are document-relative *field* paths, not filesystem paths, and `commands/req_parse.py`'s `typer.echo` is a CLI message, not an exception -- both out of scope). Reword the **mandatory set -- the title-embedding sites in non-deprecated domains (today: the 13 `UcParseError` sites)** -- to omit the title (e.g. reference the heading level/kind or the document `id` instead) without changing the exception's type or raise condition; the **deprecated ADR domain's 9 title sites are explicitly excluded** as an accepted residual gap (the domain is planned for phase-out, mirroring its `feat-107` cache exclusion -- its parse-error messages can still carry a title into free-text log/span content, and the path scrub still applies to them), and the **path-embedding sites are optionally reworded** (they are scrub-covered in logs/spans, and absolute paths are already disclosed client-side by the existing `specmgr://config` resource) -- the narrow, message-text-only carve-out recorded in Scope's Explicitly Out Of Scope section. Add a unit test per reworded call site asserting the new message no longer contains the offending content.
- [x] Task 6.8: Quality gate.

#### Phase 7: Packaging Confirmation

- [x] Task 7.1: Apply the version-range narrowing Task 1a.3's Decisions Made entry recommends and Task 1a.7 recorded as the batch's sole open follow-through -- narrow `opentelemetry-sdk>=1.44.0` and `opentelemetry-exporter-otlp-proto-http>=1.44.0` to `>=1.44.0,<2.0.0` in `pyproject.toml`, so a future major OTel SDK release that restructures Task 6.1's relied-upon private span containers trips at install/lock time instead of silently breaking redaction at runtime -- and confirm the Task 1a.1 OTLP/HTTP exporter choice is still the right, documented one given everything built in Phases 2-6; update `uv.lock` per Task 7.2.
- [x] Task 7.2: Regenerate `uv.lock` if Task 7.1 changed anything.
- [x] Task 7.3: Quality gate.

#### Phase 8: Documentation

- [ ] Task 8.1: Add a "Logging and Telemetry" section to `README.md` documenting every new environment variable (including the file-sink pair), its default, the status resource, and local-dev vs. production usage examples.
- [ ] Task 8.2: Regenerate `docs/GENERATED.md` (`specmgr docs`) and update `AGENTS.md`'s Status section with a short bullet for the new `telemetry/` package; while there, fix the pre-existing, unrelated staleness in `AGENTS.md`'s `general/` bullet (missing `specmgr://config`/`specmgr://ears`, both of which already exist).
- [ ] Task 8.3: Quality gate.

#### Phase 9: Final verification

- [ ] Task 9.1: Run the full acceptance-criteria checklist above (ACC-001..013) end to end.
- [ ] Task 9.2: Full pre-commit/CI quality gate.
- [ ] Task 9.3: Confirm all three VCRs (`0a1c2f63-9576-4463-bf40-8f771f14fefb`, `54fd355f-0978-4dd0-9e1d-da89432e5321`, `f494d230-97f3-4db9-9ef9-ed9c903ee008`) have reached `## Coverage: full`; for any acceptance criterion that still cannot be demonstrated, resolve it or explicitly document why before closing the feature.

## Progress

### Current Status

**As of 2026-09-24**: Phase 1 (Tasks 1.1-1.6), the Phase 1a spike batch (Tasks 1a.1-1a.7), Phase 2 (Tasks 2.1-2.5), Phase 3 (Tasks 3.1-3.7), Phase 4 (Tasks 4.1-4.11), Phase 5 (Tasks 5.1-5.8), Phase 6 (Tasks 6.1-6.8), and Phase 7 (Tasks 7.1-7.3) are complete. Planning, Phase 0, an implementation-readiness review, a plan-consistency review, a plan-fix review, a post-exploration re-evaluation, and a final pre-implementation review are complete. QA `40a17fb8-c069-4c38-a092-47912e1fa41d` has been through two elicitation rounds and is fully answered. REQ `bc356fc9-964a-4274-93ec-4627c5aeb2e5`, REQ `dacd01f4-ffd8-4363-a20b-5ac1ce11eef2`, REQ `41444084-6821-426d-84a2-028a3f4fed0b`, and ADR `fdbb6d22-278a-4ecf-b2f6-db208bc49fc6` are all created/updated and cross-linked. Three initial, partial-coverage VCRs (one per REQ) are authored, and VCR `54fd355f-0978-4dd0-9e1d-da89432e5321` now also carries AC-007 (span redaction) and AC-008 (metrics correctness). A pre-implementation review validated every ADR/plan technical claim against the installed `mcp==2.0.0` SDK's actual source, surfaced several previously-unspecified design details (static-config failure mode, root-logger interaction, redaction scope, metric domain attribution, OTLP failure de-duplication), and resolved them via stakeholder decisions now folded into the Design Notes and Task List above. A subsequent plan-consistency review found and closed several internal gaps in the Task List/VCRs themselves. A third, plan-fix review then closed six further gaps (unfiltered middleware method scope, resource `uri`-vs-`name` extraction, missing server-startup wiring, an unachievable universal title-redaction guarantee, an unspecified correlation-ID/span-ID reconciliation mechanism, and a lock-wait-time measurement detail). A fourth review then diagnosed why real gaps kept surfacing across all three prior passes (verification-by-reading rather than verification-by-doing), and appended a technical-spike-and-relocated-packaging task batch (Tasks 1a.1-1a.7) to Phase 1's own checklist -- a literal new `Phase 1a` heading is not schema-valid, since `Phase N: title` headings require an integer `N` -- so this work runs before Phase 2 without renumbering any later phase, converting the remaining OpenTelemetry-SDK-dependent design assumptions into confirmed facts before Phases 4-6 build on them, and directly fixed several independent gaps not requiring empirical confirmation -- see Updates below. A fifth, post-exploration re-evaluation pass then ran after the Phase 1a spike batch (Tasks 1a.1-1a.7) completed: it re-verified the plan against the installed `mcp==2.0.0` SDK's actual request/error wire model (the `tools/call` exception-to-`isError`-result conversion, the dispatcher's error mapping, the `CallToolResult.meta` -> `_meta` dump), confirmed the proxy-tracer ordering the wiring bullet assumes, censused every title- and path-embedding exception call site, and filled in the remaining implementation-level details (per-method error channels + correlation-ID attachment mechanisms, formatter-level scrub application point, OTel bootstrap pins, `DocCache` registry shape, lock-helper import safety, pinned status-resource URI/format, CLI fail-closed error surface, Task 6.7 scope decision) -- see Updates below. As of this phase: Phase 0, the Phase 1a batch (Tasks 1a.1-1a.7), Phase 1's own tasks (1.1-1.6), Phase 2 (Tasks 2.1-2.5), Phase 3 (Tasks 3.1-3.7), Phase 4 (Tasks 4.1-4.11), Phase 5 (Tasks 5.1-5.8), Phase 6 (Tasks 6.1-6.8), and Phase 7 (Tasks 7.1-7.3) are complete -- Task 1.1 landed the fail-closed config parsing in a new stdlib-only `src/biz/dfch/specmgr/telemetry/config.py` (the eight `SPECMGR_LOG_*`/`SPECMGR_OTEL_*` env vars into a frozen `TelemetryConfig` dataclass, with `TelemetryConfigError` raised immediately on every invalid/incomplete combination, unconditionally), Task 1.3 landed the read-only `specmgr://telemetry/status` resource (new `general/resources/telemetry_status.py`), Task 1.4 moved the status VCR to `## Coverage: full` with concrete test references, Task 1.6 wired the validation into `server.py`'s module scope immediately before `mcp = MCPServer(...)` with the clean single-stderr-line-plus-exit-1 refusal at the CLI entry points (`cli.py`'s command-import chain and `commands/mcp.py`'s lazy server import -- the plan-vs-codebase discrepancy this settled is recorded in the Decisions Made), and Tasks 2.1/2.5 landed the structured logging (new `src/biz/dfch/specmgr/telemetry/logging.py`: the pinned record shape in the JSON `JsonFormatter` and the `SpecmgrRichHandler` rich console format built on the SDK's own rich handler, plus the explicit, idempotent `setup_logging` root-logger setup gated by `SPECMGR_LOG_ENABLED` with the opt-in always-JSON file sink), called unconditionally at that same module-scope point before `MCPServer` construction so the SDK's own `configure_logging()` call cannot override the enabled configuration, with Task 2.2's 29 unit tests (stdout-safety in both formats, valid parseable JSON, file-sink gating, no-silent-no-op against the SDK default) and Task 2.3 extending the logging VCR `0a1c2f63-9576-4463-bf40-8f771f14fefb` with concrete test references for AC-001/AC-005 (coverage stays `partial`); Phase 3's Tasks 3.1/3.2/3.3 then landed the call-observability middleware in new `src/biz/dfch/specmgr/telemetry/middleware.py` -- the enablement-gated `SpecmgrTelemetryMiddleware` (a pure pass-through when both features are off, so the default config stays byte-for-byte the SDK's own), with the ACC-013 method filter over `tools/call`/`resources/read`/`prompts/get`, per-method identity extraction (`name` for tools/prompts, `uri` for resources), the shared correlation-ID helper (the active span's trace ID when a recording span is current, else `uuid4().hex` -- Phase 3 always takes the `uuid4()` branch, since no `TracerProvider` exists until Phase 4), start/completion/error log records with id/type-only messages (plus the new status value for `set_status` invocations), and the per-channel error behavior (a `tools/call` `isError: true` result annotated via its `_meta`, a raised `MCPError` annotated via `error.data` preserving the SDK's own `uri` entry, and a raised `ValidationError`/raw exception converted to the wire-identical dispatcher error carrying the ID in `data` and re-raised), appended to `mcp.middleware` by `server.py`'s module scope under the Task 3.2 startup guard (a broken `Server.middleware` contract logs one warning and keeps starting) -- with Task 3.4/3.7's 43 unit tests (the enablement gate, the method filter, the identity extraction, the three error channels, the record shape, the correlation-ID helper, and the server wiring incl. the broken-contract guard) and Task 3.5 extending the logging VCR `0a1c2f63-9576-4463-bf40-8f771f14fefb` with concrete test references for AC-002/AC-004; Phase 4 then landed the OpenTelemetry SDK bootstrap in new `src/biz/dfch/specmgr/telemetry/otel.py` (providers/exporters only when `SPECMGR_OTEL_ENABLED=true` -- a `BatchSpanProcessor` + `PeriodicExportingMetricReader` with the `service.name = "specmgr"` resource, the console exporters redirected to `out=sys.stderr`, the Task 4.5 `OtlpExporterWrapper` one-message-per-episode OTLP degradation, the Task 4.3 fail-open policy, and provider shutdown in `_lifespan`'s post-`yield` section -- with Task 4.2/4.4/4.10/4.11's 41 unit tests incl. the real-stdio correlation and stdout-safety sessions), Phase 5 landed the metrics instrumentation (`telemetry/domain_mapping.py`'s explicit name/uri -> domain mapping with a live-registration drift test, the middleware's lazily created `mcp.tool.duration`/`mcp.tool.call.count`/`mcp.tool.error.count` instruments, the `DocCache` hit/miss counters + registry + observable callbacks, and the thirteen `_lock.py` context managers' acquire/release refactor around the shared `record_lock_wait` helper, with Task 5.6's 85 new tests), and Phase 6 landed the redaction safeguards in new `src/biz/dfch/specmgr/telemetry/redact.py` -- the shared `<redacted-path>` scrub for absolute-filesystem-path-shaped substrings (POSIX/Windows/UNC, raw and JSON-escaped), wired at formatter level onto every specmgr-installed handler (the JSON console handler and the file sink via `ScrubbingFormatter` around their `JsonFormatter`, the rich console handler via `ScrubbingSpecmgrRichHandler` at the `render_message` seam) and, span-side, as the global `RedactionSpanProcessor` the bootstrap adds before the exporting `BatchSpanProcessor` (scrubbing every span's attributes and exception events -- including the SDK's own built-in `OpenTelemetryMiddleware`'s spans -- by replacing the SDK's private attribute containers, per Task 1a.3's confirmed mechanism, with the Task 6.3 canary pinning that reliance), plus the Task 6.7 rewording of the 13 `UcParseError` title sites in `uc/models/v1/parser.py` (the deprecated ADR domain's 9 title sites excluded as the accepted residual gap, the ~25 path-embedding sites scrub-covered and deliberately not reworded) -- with Task 6.3/6.7's 47 new tests (34 in `tests/telemetry/test_redact.py`, 13 in `tests/uc/models/v1/test_parser.py::TestParseErrorMessagesOmitTitles`) and Tasks 6.4/6.5 moving both the logging VCR `0a1c2f63-9576-4463-bf40-8f771f14fefb` (AC-003) and the telemetry VCR `54fd355f-0978-4dd0-9e1d-da89432e5321` (AC-007) to `## Coverage: full` (every one of their acceptance criteria now carries a concrete test reference); Phase 7 then landed the packaging confirmation -- Task 7.1 applied Task 1a.3's recommended `opentelemetry-sdk`/`opentelemetry-exporter-otlp-proto-http` version-range narrowing (`>=1.44.0,<2.0.0` in `pyproject.toml`'s `mcp` extra, so a future major OTel SDK release restructuring Task 6.1's relied-upon private span containers trips at install/lock time) and re-confirmed Task 1a.1's OTLP/HTTP exporter choice against what Phases 2-6 built (confirmed: the bootstrap constructs `OTLPSpanExporter`/`OTLPMetricExporter` from the HTTP package, the `OtlpExporterWrapper` subclasses the SDK's exporter base classes, the de-dup filter targets the HTTP exporters' own logger names, and no `grpcio`/gRPC exporter is in the lock; the one refinement since Task 1a.1 -- the bootstrap appends the spec-mandated `/v1/traces`/`/v1/metrics` paths itself, since an explicit `endpoint=` argument is POSTed as-is -- is already recorded in the Phase 4 Decisions Made entry), and Task 7.2's `uv lock` regeneration left every resolved package version unchanged (both OTel packages still 1.44.0); Phases 8-9 remain outstanding.

### Blockers

None currently.

### Updates

<!-- Newest entry first -- prepend new entries directly below this comment. -->

#### 2026-09-24 14:58:43.000+02:00 - Phase 7 (Tasks 7.1-7.3) landed: packaging confirmation -- OTel version-range narrowing applied, lock regeneration version-neutral, OTLP/HTTP exporter choice re-confirmed

Task 7.1 applied the version-range narrowing Task 1a.3's Decisions Made entry recommended and Task 1a.7 recorded as the batch's sole open follow-through: `pyproject.toml`'s `mcp` extra now reads `opentelemetry-sdk>=1.44.0,<2.0.0` and `opentelemetry-exporter-otlp-proto-http>=1.44.0,<2.0.0` (two lines changed; the `mcp>=2.0.0` bound untouched, per the Explicitly Out Of Scope decision), so a future major OTel SDK release that restructures Task 6.1's relied-upon private span containers (`ReadableSpan._attributes`/`_events`, `Event._attributes`) trips at install/lock time instead of silently breaking redaction at runtime. Task 7.2 regenerated `uv.lock` with `uv lock` ("Resolved 109 packages"): the before/after diff is exactly two lines -- the project's own dependency-specifier echo in the `biz-dfch-specmgr` block (`>=1.44.0` -> `>=1.44.0,<2.0.0` for the two OTel entries) -- and every resolved package version is unchanged (both OTel packages still 1.44.0; no upgrade/downgrade forced by the narrowing). The Task 1a.1 OTLP/HTTP exporter choice was re-confirmed against what Phases 2-6 actually built: `telemetry/otel.py` imports `OTLPSpanExporter`/`OTLPMetricExporter` from `opentelemetry.exporter.otlp.proto.http.trace_exporter`/`...metric_exporter` (the HTTP package) and constructs them as `OTLPSpanExporter(endpoint=...)`/`OTLPMetricExporter(endpoint=...)`; the Task 4.5 `OtlpExporterWrapper` subclasses the SDK's exporter base classes (`SpanExporter, MetricExporter`); and the episode de-dup filter targets exactly Task 1a.4's confirmed HTTP-exporter logger names (`opentelemetry.exporter.otlp.proto.http.trace_exporter`/`...metric_exporter`); no `grpcio`/gRPC exporter appears anywhere in the lock. The choice is confirmed -- its four documented rationale points (dependency footprint, endpoint reachability, configuration shape, fail-open parity) all still hold. The one codebase change since Task 1a.1 that bears on the choice is already recorded in the Phase 4 Decisions Made entry: an explicit `endpoint=` constructor argument is POSTed as-is (the SDK appends the spec-mandated paths only to its own `OTEL_EXPORTER_OTLP_ENDPOINT` env-var fallback), so `telemetry/otel.py`'s bootstrap appends `/v1/traces`/`/v1/metrics` to `SPECMGR_OTEL_ENDPOINT` itself -- a refinement of how the choice is *used*, not of the choice itself; no new Decisions Made entry is warranted. Task 7.3's quality gate ran the full standard set, all green: `ruff format --check` (1729 files already formatted), `ruff check` (all checks passed), `vulture src/ whitelist.py --min-confidence 60` (clean, exit 0), `pytest -n auto --cov=src --cov-report=` (3636 passed in 37.95 s -- the exact Phase 6 baseline), `specmgr docs` (474 module files + `docs/GENERATED.md` rewritten, zero changed files), `specmgr mcp-docs` (`docs/MCP.md` rewritten, zero changed files), and `specmgr coverage-badge` (`docs/coverage.svg` rewritten at 99%, zero changed files).

#### 2026-09-24 09:57:00.000+02:00 - Phase 6 (Tasks 6.1-6.8) landed: redaction safeguards -- formatter-level path scrub, global span scrubber, 13 uc parse-error rewordings, 47 new tests, both VCRs to full coverage

Task 6.1 added `src/biz/dfch/specmgr/telemetry/redact.py`: the free-text redaction backstop -- `scrub_paths` (a regex scrub for absolute-filesystem-path-shaped substrings: POSIX two-or-more-segment paths, Windows drive paths, and UNC paths, raw and JSON-escaped alike, each replaced by the pinned `<redacted-path>` token; best-effort by design, with the false-positive/negative limits documented in the module docstring), `ScrubbingFormatter` (wraps a delegate formatter and runs the scrub on its final rendered string -- the point where `exc_text` exists, which a handler-attached `logging.Filter` cannot see), `ScrubbingSpecmgrRichHandler` (the scrub at the rich `render_message` seam, covering the combined message text incl. the `exception` field's `Type: message` rendering), and `RedactionSpanProcessor` (the global, `TracerProvider`-level `SpanProcessor` whose `on_end` applies the same scrub to every span's attributes and each event's attributes -- in particular the `exception.message`/`exception.stacktrace`/`exception.type` keys `record_exception` sets -- by replacing the SDK's private containers, per Task 1a.3's confirmed mechanism, with a one-warning-per-process fail-open on an unexpected shape). Task 6.2 wired both hooks: `telemetry/logging.py`'s two handler builders now emit the scrub-wired formatters/handlers (function-local `redact` imports -- `redact.py` imports `logging.py`'s `SpecmgrRichHandler` at its own module level, so that stays the only edge of the pair), and `telemetry/otel.py`'s bootstrap adds the `RedactionSpanProcessor` *before* the exporting `BatchSpanProcessor` (verified against the installed 1.44.0 source: the provider's `SynchronousMultiSpanProcessor` invokes `on_end` in add order, so the scrub runs before the span is handed to the export path). Task 6.3 added `tests/telemetry/test_redact.py` (34 tests: the scrub's catch/leave-alone matrix incl. the documented limits; the formatter-level scrub on message/exception-extra/`exc_text`-traceback payloads from any originating logger; the file-sink scrub end to end; the rich seam; the span attribute/event scrub in both the simple and batch chains; the mandated SDK-built-in-middleware case -- a `tools/call` span created entirely by the SDK's own `OpenTelemetryMiddleware`, the exception injected through its own `record_exception`/`set_status` calls, exported with its exception-event paths scrubbed; the Task 4.4-style canary pinning `ReadableSpan._attributes`/`_events`/`Event._attributes` and the end-to-end visibility of the replacement; the fail-open one-shot warning; and the Task 6.2 wiring assertions incl. the real production chain). Task 6.7 reworded the mandatory set -- the 13 `UcParseError` title sites in `uc/models/v1/parser.py` -- to reference the heading level/kind (and the expected shape where one was already given) instead of the title, changing only the string passed to the constructor (type and raise condition untouched), with 13 per-site unit tests in `tests/uc/models/v1/test_parser.py::TestParseErrorMessagesOmitTitles`; the deprecated ADR domain's 9 title sites were left untouched (the accepted residual gap) and the ~25 path-embedding sites deliberately not reworded (scrub-covered; minimal diff); three `test_logging.py` assertions were updated for the new wiring (the handler formatters are now `ScrubbingFormatter` wrapping `JsonFormatter`). Tasks 6.4/6.5 extended the two VCRs with the concrete test references (logging VCR `0a1c2f63-9576-4463-bf40-8f771f14fefb` AC-003; telemetry VCR `54fd355f-0978-4dd0-9e1d-da89432e5321` AC-007) and moved both from `## Coverage: partial` to `full` -- every one of their acceptance criteria now carries a concrete test reference. Quality gates (Tasks 6.6/6.8, both green): `ruff format --check` (1728 files already formatted), `ruff check` (all checks passed), `vulture src/ whitelist.py --min-confidence 60` (clean; `on_end` added to the whitelist as the SDK-dispatched `SpanProcessor` hook, mirroring the existing `format` entry), the full `pytest -n auto --cov=src` suite (3636 passed in ~39 s, baseline 3589 + 47 new), `specmgr docs` (474 module files + `docs/GENERATED.md` rewritten, second run idempotent; new `docs/api/biz.dfch.specmgr.telemetry.redact.md` page, plus the updated `telemetry.logging`/`telemetry.otel`/`uc.models.v1.parser` pages and the two VCR mirrors), `specmgr mcp-docs` (`docs/MCP.md` unchanged), and `specmgr coverage-badge` (99%, unchanged).

#### 2026-09-22 20:40:00.000Z - Phase 5 (Tasks 5.1-5.8) landed: domain mapping, call/cache/lock-wait metrics, 85 new tests, telemetry VCR extended (recovered from a cancelled partial attempt)

A previous, cancelled Phase 5 attempt left partial work on disk (HEAD still `981d60e`); this pass reviewed every hunk against the orchestrator's pins, repaired the gaps, and completed the phase. Task 5.1 added `src/biz/dfch/specmgr/telemetry/domain_mapping.py`: the explicit, static name/uri -> domain mapping behind `mcp.domain` (domain-specific tools/prompts by registered name -- explicit lists, not heuristics, since e.g. `refine` -> `qa` and `implement_task` -> `tsk` carry no domain substring; the five generic dispatch tools via their own `type` argument at call time, a non-domain `type` yielding no domain; resources by their uri's first path segment when it is one of the 13 domains, else no domain -- `specmgr://rsk/tara`/`specmgr://rsk/risk-matrix` -> `rsk`, `specmgr://adr/{id}` -> `adr`; the no-domain case -- `mdformat`, `compact_history`, the seven cross-cutting resources -- yields `None`, and the caller omits the attribute/extra entirely, never an empty string). A live-registration drift test (`tests/telemetry/test_domain_mapping.py`, importing the real `server` module) keeps the tables exactly in sync with what `server.py` registers in both directions. Task 5.2/5.3 extended `telemetry/middleware.py`: the middleware now lazily creates (create-once, cached) its own `mcp.tool.duration` histogram (unit `ms`), `mcp.tool.call.count` counter (exactly one increment per observed invocation, whatever its outcome), and `mcp.tool.error.count` counter (per-channel `error.type`: `tool_error` for `tools/call` `isError: true` results, the `MCPError`'s JSON-RPC code as a string, `type(exc).__qualname__` for raw/`ValidationError` exceptions) from the `telemetry/metrics.py` meter slot -- a fast no-op that allocates nothing per call while the slot is `None` (telemetry disabled) -- with a first-failure fail-open (one warning, metric recording off for the process); every log record also gains the `domain` extra when the mapping yields one (the Phase 3 pin's second half; the Phase 3 test asserting its absence was updated to the new pinned behavior, with new no-domain-case tests). Task 5.4 added plain-int hit/miss counters at `DocCache.read`'s real decision points (a hash-matched entry -- cached success or failure alike -- is a hit; anything re-invoking `parse_fn` is a miss), the `stats()` accessor, the optional `domain` init parameter with self-registration into the new module-level `DOC_CACHE_REGISTRY` (domain-keyed, last-wins, test-only `reset_doc_cache_registry()`; bare instances do not register), the 12 one-line `DocCache("<domain>")` singleton edits, and the two observable-counter callbacks in `telemetry/metrics.py` (lazy import of the registry inside the callback body per the circular-import pin, defensive per-entry iteration, one `Observation` per live entry every cycle). Task 5.5 refactored all 13 `<domain>/tools/_lock.py` context managers (incl. `adr_lock` and feat's `feat_create_lock`) to the explicit `started = time.monotonic(); lock.acquire(); record_lock_wait(domain, (time.monotonic() - started) * 1000.0); try: yield finally: lock.release()` shape, importing only the base-library-safe slot-holder `telemetry/metrics.py` (no `opentelemetry.*` at module level there, `from __future__ import annotations`, slots default `None`); `telemetry/otel.py`'s bootstrap now builds the `MeterProvider` with the pinned explicit-bucket Views (SDK 1.44.0's `opentelemetry.sdk.metrics.view.View` + `ExplicitBucketHistogramAggregation`: `mcp.tool.duration` 5..10000 ms, `mcp.lock.wait_time` 1..500 ms -- registered before any instrument is created, so the middleware's lazily created histogram picks its View up) and creates the `mcp.lock.wait_time` histogram + the `mcp.cache.hit`/`mcp.cache.miss` observable counters at bootstrap, storing the `Meter`/histogram in the `telemetry/metrics.py` slots (cleared by `shutdown_telemetry`). Task 5.6 added the tests: `tests/telemetry/test_metrics.py` (39 tests: the middleware's instruments and attributes incl. the domain-omitted no-domain case and the generic-dispatch `type` argument, the per-channel `error.type` values, the slot-`None` no-op and the fail-once metrics fail-open, `record_lock_wait` no-op/fail-open, the cache callbacks' registry iteration/defensiveness/collection-time values, and the production bootstrap's Views/instruments via an `InMemoryMetricReader` under the Phase 4 global-state-reset pattern), `tests/telemetry/test_lock_wait.py` (10 tests / 65 subtests over all 13 modules: release-on-exception, held-lock-blocks-concurrent-acquirer, distinct-ids-concurrency, per-domain wait recording, plus the `TestLockWaitProductionPath` regression pair that drives the real `create_req` + generic `set_status` production chain and pins when the `mcp.lock.wait_time` series does and does not appear -- see the Decisions Made entry of 2026-09-23), and `TestDocCacheStatsAndRegistry` in `tests/general/tools/test__doc_cache.py` (11 tests). Task 5.7 extended VCR `54fd355f-0978-4dd0-9e1d-da89432e5321` with concrete test references for AC-005/AC-006/AC-008 (coverage stays `partial` pending Phase 6's AC-007). Task 5.8's gate: `ruff format --check` clean, `ruff check` clean, `vulture` clean, full pytest 3589 passed (baseline 3504; 85 new tests), `specmgr docs` regenerated (473 API pages, test files 364 -> 367), `specmgr mcp-docs` left `docs/MCP.md` unchanged, `specmgr coverage-badge` unchanged (99%); a fresh clean-env `import biz.dfch.specmgr.server` with `SPECMGR_OTEL_ENABLED=true SPECMGR_LOG_ENABLED=true SPECMGR_LOG_FORMAT=json` exits 0 (the final shutdown collection's `mcp.cache.*` blob lands on stderr), and a real stdio session (list_req + 2x get_req + set_status + resources/read against a temp docs dir) exported all five metric families with the pinned attributes, the domain attribute omitted for `specmgr://version`, the pinned `mcp.tool.duration` bucket boundaries on the lazily created histogram, and a `mcp.lock.wait_time` data point for the locked `set_status`. The Phase 4 otlp end-to-end test was updated per the pin: with metric instruments now existing, the shutdown-time final collection reaches the unreachable endpoint, so it now asserts exactly ONE metric-side episode message too (stdout purity unchanged).

#### 2026-09-22 18:18:50.000Z - Phase 4 correction: wrapper suppression filter armed at each export attempt's start (first-cycle leak fixed), gate re-run green

Phase verification reproduced an ACC-010 violation in the Task 4.5 wrapper: the installed SDK's OTLP HTTP exporters log their per-attempt failure lines *during* the `export()` call (inside the attempt's own retry loop), i.e. before the `FAILURE` return the wrapper keyed its episode state on -- so the first cycle's raw SDK line(s) leaked to stderr ahead of the wrapper's single episode message (one ERROR line at `OTEL_EXPORTER_OTLP_TIMEOUT=1`; the full 3x WARNING + 1x ERROR at the production-default 10 s timeout). Fixed in `telemetry/otel.py`: the state-gated `_ExporterEpisodeFilter` is now armed (attached to exactly the two exporter logger names) at the start of every `export()` attempt and disarmed on a `SUCCESS` return or `shutdown()`; the filter gates on the new `OtlpExporterWrapper.armed` property; the episode state machine, the one-warning-per-episode contract, and the lock discipline are otherwise unchanged (see the Decisions Made entry above). The observable contract is now proven end to end: the re-run orchestrator repro (real `specmgr mcp` stdio session, `SPECMGR_OTEL_ENABLED=true SPECMGR_OTEL_EXPORTER=otlp SPECMGR_OTEL_ENDPOINT=http://127.0.0.1:1 SPECMGR_LOG_ENABLED=true SPECMGR_LOG_FORMAT=json OTEL_EXPORTER_OTLP_TIMEOUT=1`, two tool calls) produces exactly ONE failure-related stderr line -- the wrapper's episode message -- with zero lines from `opentelemetry.exporter.otlp.proto.http.trace_exporter`/`...metric_exporter` including the first cycle's. Tests strengthened: three new `tests/telemetry/test_otel_exporter_wrapper.py::TestFirstCycleAttemptSuppression` scenarios (a fake exporter emits the real per-attempt lines inside `export()`; verified red against the old arming behavior), and `tests/telemetry/test_otel.py::TestStdoutSafetySubprocess::test_unreachable_otlp_endpoint_writes_zero_bytes_beyond_json_rpc_to_stdout` now asserts zero raw exporter-logger records across the whole child session, not just the wrapper-message count. Full standard quality gate re-run: `ruff format --check` (1719 files already formatted), `ruff check` (all checks passed), `vulture src/ whitelist.py --min-confidence 60` (clean; `OtlpExporterWrapper`'s test-consumed state accessors `failing`/`armed`/`exporter` added to the whitelist with the standard justification), `pytest -n auto --cov=src` (3504 passed in ~37 s, baseline 3460 + 44 new tests, no OTel global-state pollution), `specmgr docs` (471 module files rewritten incl. the updated `docs/api/biz.dfch.specmgr.telemetry.otel.md`, a second run changed nothing), `specmgr mcp-docs` (`docs/MCP.md` unchanged), `specmgr coverage-badge` (99%, `docs/coverage.svg` unchanged). The Phase 4 entry below's wrapper description was corrected in place (its "while failing … suppresses the per-cycle noise" wording overstated the first-cycle behavior of the original commit); its gate numbers describe that commit's run and are superseded by this entry's.

#### 2026-09-22 10:49:41.000Z - Phase 4 (Tasks 4.1-4.11) landed: OTel SDK bootstrap, fail-open policy, 41 new unit tests, telemetry VCR extended

Task 4.1 added `src/biz/dfch/specmgr/telemetry/otel.py`: `bootstrap_telemetry(config)` configures the global OpenTelemetry providers only when `SPECMGR_OTEL_ENABLED=true` (the default sets nothing, so the SDK's built-in `OpenTelemetryMiddleware` keeps creating inert non-recording spans through the API's no-provider proxy) -- a `TracerProvider` with a `BatchSpanProcessor` (not `SimpleSpanProcessor`, per the pin) and a `MeterProvider` with a `PeriodicExportingMetricReader` (default 60 s interval), both carrying a `Resource` with the fixed `service.name = "specmgr"` attribute, and the `Meter` fetched once via the global API into the module-level `_meter` slot for Phase 5's increments (no counter/histogram instruments are created -- the Design Notes' division-of-labor pin). The `console` exporters (the default) are constructed exactly `ConsoleSpanExporter(out=sys.stderr)`/`ConsoleMetricExporter(out=sys.stderr)` (`out=` by keyword -- `ConsoleSpanExporter`'s first positional arg is `service_name`); the OTLP/HTTP exporters are wrapped in the Task 4.5 `OtlpExporterWrapper` and built with the `SPECMGR_OTEL_ENDPOINT` base URL extended by the spec-mandated `/v1/traces`/`/v1/metrics` paths (verified against the installed 1.44.0 source: an explicit `endpoint=` argument is POSTed as-is -- the SDK appends those paths only to its own `OTEL_EXPORTER_OTLP_ENDPOINT` env-var fallback -- so the bootstrap appends them itself, with the SDK's trailing-slash semantics). `shutdown_telemetry()` shuts both providers down (performing the `MeterProvider`'s final collection and the `BatchSpanProcessor`'s final span flush) and clears the slots; `server.py`'s `_lifespan` post-`yield` section calls it at process exit -- the only safe place for that final export to land on real stderr, since mcp 2.0.0's stdio transport restores fd 1 once serving ends. Task 4.9 wired `bootstrap_telemetry(telemetry_config)` unconditionally into `server.py`'s module scope at the pinned location (config -> logging -> OTel bootstrap -> `MCPServer(...)` -> the Task 3.2/4.3 guarded middleware append), so `SPECMGR_OTEL_ENABLED` takes effect the moment `specmgr mcp` runs. Task 4.5's `OtlpExporterWrapper` is the thin de-duplication layer for an unreachable OTLP endpoint (Task 1a.4's confirmed behavior: the exporters return `FAILURE` without raising and log 3x WARNING + 1x ERROR per failed cycle under their own module loggers, with no success ever logged): an episode runs from the first `FAILURE` return until the first `SUCCESS` or `shutdown`, exactly one warning (via the `biz.dfch.specmgr.telemetry` logger) is emitted at the first `FAILURE` return of an episode and a further single one at the start of any subsequent episode, and a state-gated `logging.Filter` on exactly the two exporters' own logger names -- armed at the start of every `export()` attempt (the SDK logs its per-attempt failure lines *during* the attempt, before the first `FAILURE` return; arming only on that return leaked the first cycle's raw lines, caught in phase verification and fixed -- see the Decisions Made entry below) and disarmed on a `SUCCESS` return or shutdown -- suppresses the per-attempt noise, so the only failure-attributable stderr output across a whole episode is the single wrapper message -- the wrapper passes `export()`'s result and the `force_flush()`/`shutdown()` signatures through unchanged, and (after this phase's re-evaluation against the installed SDK) subclasses both SDK exporter base classes so it inherits the internal surface `PeriodicExportingMetricReader.__init__` reads off the exporter instance (`_preferred_temporality`/`_preferred_aggregation` -- a plain delegating object crashes the bootstrap with `AttributeError`; see Decisions Made). Task 4.3 implemented the fail-open policy as the upgrade of Task 3.2's minimal guard (same mechanism, not a second independent one): a startup `middleware_contract_compatible(server)` check (the `Server.middleware` list type and the `ServerMiddleware.__call__` signature vs. this middleware's own) around the append -- an incompatible contract or a raising check/append logs exactly one warning and the server continues operating without call observability -- plus a call-time half in the middleware that guards the pre-`ctx`-read and post-call result-processing contract surfaces (never `call_next` itself, so a request is never double-executed and an app exception is never misread as a contract violation): a first `AttributeError`/`TypeError` there logs exactly one warning, disables observability for the rest of the process, and still completes the request. Task 4.2 confirmed the Task 3.1 correlation-ID helper's span-ID branch engages under the real import order (`import server` -> the Task 4.1 bootstrap -> a real call) via a real `specmgr mcp` stdio subprocess session with `SPECMGR_OTEL_ENABLED=true` + `SPECMGR_LOG_ENABLED=true` (the child is a fresh process: pristine OTel globals, the SDK's import-time proxy tracer resolves against the bootstrap's later provider) -- for each of `tools/call`/`resources/read`/`prompts/get`, the exported SDK span's `traceId` equals the middleware log record's `correlation_id` (e.g. measured `6a86d52b370f817929b7627f87afebf7` for `tools/call get_feat_template`). Task 4.10 asserted, over the same real stdio transport, that with `SPECMGR_OTEL_ENABLED=true` and every supported `SPECMGR_OTEL_EXPORTER` value (the default unset-`console`, explicit `console`, and `otlp` at a deliberately unreachable endpoint) stdout carries exactly the JSON-RPC response frames and zero other bytes (the `otlp` case additionally proves the wrapper's one-message-per-episode behavior end to end for the span exporter -- the metric exporter produces no episode in this phase because no metric instrument exists yet, so empty collections are never exported). Task 4.11 confirmed the `service.name = "specmgr"` resource attribute on every exported span and metric (in-process bootstrap tests with the global OTel state isolated, plus the subprocess span assertions). Task 4.4 added the compatibility tests: canaries pinning the installed SDK's `Server.middleware` list type and `ServerMiddleware.__call__` (async, positional `(ctx, call_next)`) shape, and simulated-incompatibility assertions (a renamed protocol param through `server.py`'s real startup path; broken `ctx.method`/`ctx.params`/result shapes at call time) proving the fail-open policy engages. Task 4.7 extended VCR `54fd355f-0978-4dd0-9e1d-da89432e5321` with concrete test references for its AC-001/AC-002/AC-003/AC-004 (coverage stays `partial`: AC-005/AC-006/AC-008 await Phase 5, AC-007 awaits Phase 6). Task 4.8 ran the full standard quality gate: `ruff format --check` (1718 files already formatted), `ruff check` (all checks passed), `vulture src/ whitelist.py --min-confidence 60` (clean), `pytest -n auto --cov=src` (3501 passed in ~38 s, baseline 3460 + 41 new tests, no OTel global-state pollution of unrelated tests), `specmgr docs` (471 module files rewritten incl. the new `docs/api/biz.dfch.specmgr.telemetry.otel.md`, a second run changed nothing), `specmgr mcp-docs` (`docs/MCP.md` unchanged -- no new MCP items), `specmgr coverage-badge` (99%, `docs/coverage.svg` unchanged).

#### 2026-09-21 22:09:17.802Z - Phase 3 (Tasks 3.1-3.7) landed: correlation-ID middleware, 43 unit tests, logging VCR extended

Task 3.1 added `src/biz/dfch/specmgr/telemetry/middleware.py`: the `SpecmgrTelemetryMiddleware` implementation of the SDK's provisional `ServerMiddleware` contract (`async (ctx, call_next) -> HandlerResult`), gated first on enablement -- when neither `SPECMGR_LOG_ENABLED` nor `SPECMGR_OTEL_ENABLED` is `true` (the default), every method, including the three invocations, passes straight to `call_next(ctx)` unmodified (no correlation ID, no log record, no exception conversion, no attachment), so the default config stays a byte-for-byte no-op over the SDK's own behavior (ACC-001/ACC-008, smoke-verified end to end over a real `specmgr mcp` stdio session); otherwise the middleware filters on `ctx.method in {"tools/call", "resources/read", "prompts/get"}` (ACC-013 -- every other method, `initialize`/`*/list`/`ping`/`notifications/*` included, passes through unobserved), extracts the invoked item's identity from the per-method raw params key (`ctx.params["name"]` for `tools/call`/`prompts/get`, `ctx.params["uri"]` for `resources/read`, never a cross-method fallback), generates the per-invocation correlation ID via the shared helper `new_correlation_id` (the current span's trace ID, hex-formatted, when a recording span is active -- which Phase 4's `SPECMGR_OTEL_ENABLED` makes true, since the SDK's built-in `OpenTelemetryMiddleware` runs outside this middleware in the chain; else `uuid4().hex` -- the branch Phase 3 always takes, no `TracerProvider` existing yet), and logs a start record (INFO, before `call_next`) and a completion (INFO) or error (ERROR) record afterwards, the messages carrying id/type-only identity ("tool set_status start" style -- no arguments, no body, no paths, no titles, ACC-004) and the pinned `extra=` fields riding the record (`correlation_id`/`method`/`item_type`/`item_name`, plus `status` -- the new status value, for `set_status` invocations only, per REQ `bc356fc9-964a-4274-93ec-4627c5aeb2e5` -- plus `duration_ms` (measured with `time.monotonic()`, 3-decimal ms, absent on the start record) and, on error records, `exception`). Error detection is per-method: a `tools/call` failure arrives as a post-`call_next` result (`isError: true`, both the wire-dict and the `CallToolResult`-model shapes handled defensively per `HandlerResult`; `InputRequiredResult` is a success, never an error, never an attachment target), while `resources/read`/`prompts/get` failures -- and `tools/call`'s `MCPError` passthrough -- arrive as raised exceptions. Task 3.3 attaches the correlation ID to client-visible error responses only, never to a successful result, via the three per-channel mechanisms: (a) merge `"_meta": {"correlationId": ...}` into a `tools/call` error result, preserving any pre-existing `_meta` (verified: `CallToolResult.meta` dumps as `_meta`; the dispatcher's modern `serverInfo` stamp is preserved); (b) merge `correlationId` into a raised `MCPError`'s `error.data`, preserving the SDK's own `uri` entry where present (a `None`/non-mapping `data` starts a fresh mapping); (c) convert a raised `ValidationError`/raw exception to the wire-identical `MCPError` the dispatcher's own mapping would otherwise produce -- `INVALID_PARAMS` (-32602)/"Invalid request parameters" for a `ValidationError`, `INTERNAL_ERROR` (-32603)/"Internal server error" for any other exception, the exact codes/messages verified against `mcp/server/runner.py`'s `modern_error_data` and `mcp/shared/jsonrpc_dispatcher.py`'s `handler_exception_to_error_data` and asserted in the unit tests against those same functions -- plus the `data` carrying the correlation ID, and re-raise (with `from exc`); the error record's `exception` field is `{type, message, traceback}` for raised exceptions and `{type: "tool_error", message}` (no `traceback` key) for a `tools/call` `isError: true` result -- the SDK's own `tool_error` signal, which Task 5.3's per-channel error-type counter will reuse. The accepted side effect of (c) -- the dispatcher's own server-side `logger.exception` of such a traceback (under `mcp.server.runner`) is replaced by this feature's own error record, which carries the correlation ID anyway -- engages only when the gate is on, so the default config's SDK-side error logging is untouched. The middleware creates and increments no OTel counter/histogram (no `MeterProvider` until Phase 4; the counters are Task 5.2/5.3's). Task 3.2 appended the middleware to `mcp.middleware` in `server.py`'s module scope, right after `mcp = MCPServer(...)` (the point Task 4.9's OTel bootstrap inserts its call above, keeping the config -> logging -> OTel -> middleware order), wrapping the append in the minimal try/except: an incompatible `Server.middleware` contract (the provisional-API risk the ADR flags) logs a single warning on `biz.dfch.specmgr.telemetry` and continues with the middleware unappended rather than crashing `specmgr mcp` at startup (Task 4.3 later upgrades this guard into the complete config-aware fail-open behavior -- the same mechanism, not a second one). Task 3.4/3.7 added `tests/telemetry/test_middleware.py` (43 plain-`unittest` tests driving the middleware directly against fake `call_next`s with real `ServerRequestContext` instances, the `ctx.session` sentinel never touched): the enablement gate (the both-off config passes a raw exception through as the same instance with zero records, a tool error result through byte-for-byte, and a non-invocation method through unobserved; the gate is the disjunction `log_enabled or otel_enabled`); the ACC-013 method filter (`initialize`/`tools/list`/`resources/list`/`prompts/list`/`ping`/`notifications/cancelled` produce no record, no attachment, and no conversion -- a raising non-invocation method passes its exception through unconverted -- while the three invocations each produce start+completion records); the per-method identity extraction (`resources/read`'s logged identity comes from `ctx.params["uri"]`, not a `"name"` key absent for that method, and never falls back to one; tools/prompts from `name`; missing/non-string identities log the record without an `item_name`); each error channel (the `isError: true` result dict/model with `_meta` attachment and pre-existing `_meta` preserved, the raised `MCPError` with `error.data` merged and the SDK's `uri` entry preserved -- re-raised as the same instance -- and the raw-exception/`ValidationError` conversions asserted wire-identical against the dispatcher's own mapping functions, with `__cause__` preserved); success results (incl. `InputRequiredResult`) never receiving the ID; the record shape (start+completion sharing the 32-hex correlation ID, `duration_ms` numeric on completion/error only, `set_status`'s new status value in the extras but never in the id/type-only message, and no `status` extra on any other invocation -- incl. a `set_status` call without a `status` argument, a non-`set_status` tool with one, and a prompt with one); the correlation-ID helper (fresh `uuid4().hex` without a recording span; the active span's trace ID hex with one, tested against a real `TracerProvider`); and the `server.py` wiring (a fresh import appends the middleware innermost -- `[OpenTelemetryMiddleware, RequestStateBoundary, SpecmgrTelemetryMiddleware]` -- enabled or disabled per the env, and a simulated broken `middleware` contract logs exactly one warning and keeps starting with the middleware unappended). Task 3.5 extended the logging VCR `0a1c2f63-9576-4463-bf40-8f771f14fefb` (via the generic `update` tool) with concrete test references for AC-002 (every invocation logged, not tools only) and AC-004 (correlation ID on error responses only); `## Coverage` stays `partial` (AC-003's redaction half waits on Phase 6 -- its id/type-only identity clause, incl. `set_status`'s status value, is already pinned by `TestLogRecordShape`). In addition to the unit tests, a throwaway end-to-end smoke over a real `specmgr mcp` stdio session (both the enabled and the default config) confirmed the wire behavior: the enabled config ties every error response's `correlationId` to its log record's `correlation_id` across all three channels, and the default config preserves the SDK's own error mapping byte-for-byte (incl. the legacy-era `INVALID_PARAMS`/empty-`data` mapping of an unconverted `ValidationError`). Quality gate (Task 3.6), all green: `ruff format --check` (1715 files already formatted), `ruff check` (all checks passed), `vulture src/ whitelist.py --min-confidence 60` (clean, no new whitelist entries), `pytest -n auto --cov=src` (3460 passed in ~40 s, baseline 3417 + 43 new; `telemetry/middleware.py` 99% -- the single miss is a structurally unreachable defensive branch), `specmgr docs` (470 module files + `docs/GENERATED.md` + new `docs/api/biz.dfch.specmgr.telemetry.middleware.md`, idempotent on re-run, staged), `specmgr mcp-docs` (zero change to `docs/MCP.md` -- no new MCP items), `specmgr coverage-badge` (99%, `docs/coverage.svg` byte-identical). The implementation-level pins (channel (c)'s modern-mapping target + the accepted legacy-era divergence, the non-mapping `MCPError.data` replacement, the wire-dict observation point, and `__call__`'s deliberate absence of input asserts) are recorded in the new Decisions Made entry.

#### 2026-09-21 15:13:45.639Z - Phase 2 (Tasks 2.1-2.5) landed: structured logging setup, 29 unit tests, logging VCR extended

Task 2.1 added `src/biz/dfch/specmgr/telemetry/logging.py`: the pinned record shape (base `timestamp` (ISO 8601, UTC)/`level`/`logger`/`message` plus the structured `correlation_id`/`method`/`item_type`/`item_name`/`domain`/`status`/`duration_ms` fields and `exception` (type + message + traceback), emitting only what a record carries) in the JSON `JsonFormatter` (one single physical line per record, so file-sink records are line-delimited JSON) and in `SpecmgrRichHandler`, the rich console format mirroring the SDK's own rich handler construction (`RichHandler` on `Console(stderr=True)`, `rich_tracebacks=True`) and rendering the same fields human-readably as `key=value` pairs appended to the message; and `setup_logging(config)`, the explicit, idempotent root-logger setup gated by `SPECMGR_LOG_ENABLED` -- a no-op when disabled (leaving whatever handlers exist untouched, so the default config keeps the SDK's own `configure_logging()` behavior byte-for-byte, ACC-001) that, when enabled, replaces the root handler set with exactly the intended one(s) at the configured level (the stderr console handler in the `SPECMGR_LOG_FORMAT`-selected format, plus, under `SPECMGR_LOG_FILE_ENABLED`/`SPECMGR_LOG_FILE_PATH`, the always-JSON file sink, ACC-009) -- replacing rather than `logging.basicConfig`-ing, so it is effective in either handler state and never a silent no-op. Task 2.5 wired `setup_logging` unconditionally into `server.py`'s module scope right after the Task 1.6 config validation and before `mcp = MCPServer(...)` (the config is now fetched once and passed to both calls), so the SDK's own `configure_logging()` call inside `MCPServer.__init__` (a `logging.basicConfig`, hence a no-op once the root has handlers) runs afterwards and cannot override the enabled configuration. Task 2.2 added `tests/telemetry/test_logging.py` (29 tests: the pinned JSON shape incl. the `exc_info`-derived `exception` and partial-extra robustness, the rich handler's human-readable field rendering, stdout-never-written in both formats, the file sink's JSON-only/both-switches-gating behavior, the enabled-after-SDK-`configure_logging` replacement, idempotency, level filtering, and fresh-`server`-import wiring under both the enabled and the default configuration). Task 2.3 extended the logging VCR `0a1c2f63-9576-4463-bf40-8f771f14fefb` (via the generic `update` tool) with concrete test references for AC-001/AC-005; `## Coverage` stays `partial` (AC-002/AC-004 wait on Phase 3's middleware, AC-003 on Phase 6's redaction). Quality gate (Task 2.4), all green: `ruff format --check` (1711 files), `ruff check`, `vulture src/ whitelist.py --min-confidence 60` (one new whitelist entry: `JsonFormatter.format`, invoked only through the stdlib logging framework's dynamic `formatter.format(record)` dispatch), `pytest -n auto --cov=src` (3417 passed, baseline 3388 + 29 new; `telemetry/logging.py` 94% -- the misses are unreachable guard branches), `specmgr docs` (469 module files + `docs/GENERATED.md` + new `docs/api/biz.dfch.specmgr.telemetry.logging.md`, idempotent on re-run, staged), `specmgr mcp-docs` (zero change -- no new MCP items), `specmgr coverage-badge` (99%, `docs/coverage.svg` byte-identical). The three implementation-level pins (the rich format's `render_message` seam; the `exc_info`-derived `exception` field; the file sink's append mode) are recorded in the new Decisions Made entry.

#### 2026-09-21 12:33:56.326Z - Phase 1 (Tasks 1.1-1.6) landed: fail-closed config, status resource, server-startup wiring, CLI refusal

Phase 1's own tasks (1.1-1.6) landed everything the Phase 1a spike batch's re-evaluation gate had left for this phase. Task 1.1 added `src/biz/dfch/specmgr/telemetry/config.py` -- the eight `SPECMGR_LOG_*`/`SPECMGR_OTEL_*` env vars (including the two file-sink vars) parsed into a frozen, stdlib-only `TelemetryConfig` dataclass, all defaulting to disabled, with the dedicated `TelemetryConfigError` (a `ValueError` subclass; single-line messages naming the offending env var(s)) raised immediately on every invalid/incomplete combination -- the `SPECMGR_LOG_FILE_ENABLED=true`/blank-`SPECMGR_LOG_FILE_PATH` pairing, the `SPECMGR_OTEL_EXPORTER=otlp`/blank-`SPECMGR_OTEL_ENDPOINT` pairing, `SPECMGR_LOG_FORMAT`/`SPECMGR_OTEL_EXPORTER` outside their closed sets, `SPECMGR_LOG_LEVEL` outside the five named levels, and boolean flags outside case-insensitive `true`/`false` -- unconditionally, regardless of the parent enable switches (ACC-011). Task 1.2 added `tests/telemetry/test_config.py` (defaults, every valid value/normalization, each invalid combination, and the parent-switch-`false` cases). Task 1.3 added the read-only `specmgr://telemetry/status` resource in `general/resources/telemetry_status.py` (registered in `general/resources/__init__.py`'s imports/`__all__` and `server.py`'s docstring), returning the pinned two-line `list[str]` built from the same parsed config, with tests in `tests/general/resources/test_telemetry_status.py`. Task 1.4 extended VCR `f494d230-97f3-4db9-9ef9-ed9c903ee008` via the generic `update` tool with concrete test references for AC-001/AC-002 and moved `## Coverage` from `partial` to `full`. Task 1.6 wired `load_telemetry_config()` unconditionally into `server.py`'s module scope immediately before `mcp = MCPServer(...)` (the point Tasks 2.5/4.9 later append the logging setup and OTel bootstrap to, in config -> logging -> OTel order) and gave the CLI its clean refusal surface: verified end to end, `SPECMGR_LOG_FILE_ENABLED=true` (no path), `SPECMGR_OTEL_EXPORTER=otlp` (no endpoint), and `SPECMGR_LOG_FORMAT=xml` each make `python -m biz.dfch.specmgr mcp` exit 1 with empty stdout and exactly one stderr line, while `specmgr version` and the MCP server (new resource registered, default read `['logging: disabled', 'telemetry: disabled']`) are unchanged under a clean environment. One genuine plan-vs-codebase discrepancy surfaced and is resolved in the new Decisions Made entry below: the CLI's own import chain (`cli.py` -> `commands/__init__` -> ... -> `dec/prompts/create_dec` -> `from ...server import mcp`) executes `server.py`'s module scope at CLI import time -- before any command function runs -- so the clean refusal also had to be added at `cli.py`'s command import, in addition to `commands/mcp.py`'s lazy server import (the plan's assumed first-import point, which stays and is what the in-process test exercises). Task 1.5's quality gate: `ruff format --check` (1706 files already formatted, zero changed), `ruff check` (all checks passed), `vulture src/ whitelist.py --min-confidence 60` (clean, no new whitelist entries needed), `pytest -n auto --cov=src` (3388 passed in ~37 s, up from 3354 -- the 34 new tests), `specmgr docs` (468 module files + `docs/GENERATED.md`, three new API pages, regenerated artifacts staged with zero residual drift), `specmgr mcp-docs` (44 -> 45 resources, `docs/MCP.md` staged), and `specmgr coverage-badge` (99%, `docs/coverage.svg` unchanged).

#### 2026-09-21 07:55:43.000Z - Final pre-implementation review: plan re-verified against the installed SDK, middleware enablement gating pinned

A final review before hand-off to the Orchestrator re-verified the plan's load-bearing claims against the actual repo and the installed `mcp==2.0.0` SDK source (not just prior reading): the ADR `fdbb6d22` is accepted, the three REQs/VCRs and the two `.specmgr/conventions.md` sections are in place, `server.py`'s `_lifespan`/`MCPServer`/import wiring points match the plan, and -- the correlation-ID linchpin -- `lowlevel/server.py` seeds `self.middleware = [OpenTelemetryMiddleware()]` (outermost) with user middleware running inside the built-ins, so the feature's appended (innermost) middleware observes the already-active SDK span and "correlation ID = span trace ID" holds. One genuine gap was found and resolved per stakeholder decision: the plan never pinned that the middleware's correlation-ID generation, error-response attachment, and raw-exception-to-`MCPError` conversion are gated on (logging OR telemetry) enablement, so a literal reading (ACC-008 / Scope / Task 3.3) would suppress the SDK's own default stderr error logging in the both-off config. The middleware is now a pure pass-through when both features are off (new Design Notes "Middleware enablement gating" bullet; Tasks 3.1/3.3/3.4 and ACC-008 updated), and two cosmetic wording nits were fixed (Task 5.7's "add a new AC-008" -> "add the test reference for the already-present AC-008"; ACC-003/Scope "trace/span ID" -> "trace ID"). The plan is implementation-ready; Phase 1 (Tasks 1.1-1.6) is the next work.

#### 2026-09-21 03:45:00.000Z - Post-exploration re-evaluation: plan re-verified against the SDK's error wire model, remaining implementation details filled in

After the Phase 1a spike batch (Tasks 1a.1-1a.7) completed, a fifth review pass re-evaluated the plan against the installed `mcp==2.0.0` SDK's *actual* request/error wire model -- not only the middleware surface Tasks 0.7-0.9 had verified -- and filled in the implementation-level details the exploration enabled. Confirmed against source: (1) `tools/call` failures never reach the middleware as raised exceptions -- `_handle_call_tool` converts every non-`MCPError` tool exception into a `CallToolResult(content=[str(e)], is_error=True)` *result* (the middleware observes the dumped wire dict, `isError: true` camelCase), while `resources/read` wraps SDK errors as `MCPError` (carrying its own `data={"uri": ...}`) and `prompts/get` propagates raw exceptions, which the dispatcher maps to a generic `INTERNAL_ERROR` **without a data field**; (2) `CallToolResult.meta` dumps as `_meta` and `MCPError.error.data` reaches the wire's `error.data` -- the two concrete channels for Task 3.3's correlation-ID attachment; (3) `mcp/shared/_otel.py` fetches its tracer at module-import time, and the OTel API's proxy resolution makes a tracer fetched before `set_tracer_provider` still record into the later provider -- so the prescribed module-scope wiring order works, and Task 4.2's test now uses the real import order; (4) `commands/mcp.py` already documents `SPECMGR_MCP_TRANSPORT` as the codebase's one enum-valued `SPECMGR_*` env var (permissive fallback) -- the env-var Design Note's "no precedent" claim was corrected to name it and record the deliberate fail-closed divergence. Stakeholder decisions recorded in this pass's Decisions Made entries: raw exceptions are converted to wire-identical `MCPError`s so ACC-008 holds universally; Task 6.7's mandatory rewording set is the 13 verified `UcParseError` title sites (the deprecated ADR domain's 9 title sites excluded as an accepted residual gap; the ~25 path sites scrub-covered and optional); the log-scrub application point is formatter-level on the final rendered string (a handler `Filter` cannot see rendered tracebacks). Filled-in details: per-method error channels + per-channel error-type signal (new Design Notes bullet; ACC-003/ACC-008/ACC-012, Task 3.1/3.3/3.4/5.3), the OTel bootstrap pins (`BatchSpanProcessor`, `PeriodicExportingMetricReader`, endpoint base-URL semantics, `_lifespan` shutdown hook, meter slot, histogram buckets), the `DocCache` registry shape (domain parameter + self-registration, 12 one-line edits, OTel-import-free `_doc_cache.py`), the lock-helper meter-slot import safety, the pinned status-resource URI/format (`specmgr://telemetry/status`, new `general/resources/telemetry_status.py`), the CLI fail-closed error surface (Task 1.6), the `SPECMGR_LOG_LEVEL` five-level closed set, the scrub regex scope + token + span event keys + canary test (Task 6.1/6.3), the JSON log-record field shape (Task 2.1), the stdio-specific transport scope note (Task 4.10), the Task 2.1/wiring ordering contradiction fixed, Task 6.7's verified census, and Task 7.1's explicit `<2.0.0` version-range narrowing. VCR `54fd355f`'s AC-007/AC-008, VCR `0a1c2f63`'s AC-003, and VCR `f494d230`'s More Information note were updated to match. No implementation code was written during this pass; the phase/task numbering is unchanged.

#### 2026-09-19 17:40:00.000Z - Task 1a.7 ran: full standard quality gate passed for the Phase 1a batch, completing Tasks 1a.1-1a.7

Task 1a.7, the Phase 1a batch's own quality gate (per ACC-006), ran the full standard pre-commit/CI gate over the tree as left by Tasks 1a.1-1a.6 and every step passed: `ruff format --check` (1700 files already formatted), `ruff check` (all checks passed), `vulture src/ whitelist.py --min-confidence 60` (clean), the full `pytest -n auto` suite with coverage (3354 passed in ~39 s), and the two docs-regen drift checks -- `specmgr docs` (465 module files + `docs/GENERATED.md` rewritten, zero changed files in `git status` afterward) and `specmgr adr-toc` (`docs/adr/README.md` rewritten, zero changed files afterward). The batch's only on-disk changes had been `pyproject.toml`/`uv.lock` (Task 1a.1) and this plan (Tasks 1a.1/1a.6), so this confirms the whole batch left the tree green; the sole open follow-through is Task 7.1 re-confirming the SDK/exporter choice and applying the `opentelemetry-sdk` version-range narrowing recommended by the Task 1a.3 finding.

#### 2026-09-19 17:35:00.000Z - Phase 1a spike batch (Tasks 1a.2-1a.6) ran: all four OpenTelemetry SDK behaviors empirically confirmed, plan revised at the re-evaluation gate

The four technical spikes of Phase 1a (Tasks 1a.2-1a.5) ran against the installed `opentelemetry-sdk`/`opentelemetry-exporter-otlp-proto-http` 1.44.0 and `mcp` 2.0.0 (throwaway scripts, kept outside the repo), and Task 1a.6 recorded their findings in the four new Decisions Made entries below and revised the affected Phase 4/5/6 Design Notes/Task List wording to match confirmed behavior. One-line gist of each finding: (1) both console exporters default to stdout, but constructing them with `out=sys.stderr` yields a real `specmgr mcp` stdio session whose stdout is zero bytes beyond JSON-RPC frames (and mcp 2.0.0's stdio transport diverts fd 1 to stderr only *while serving*, so default-stream exports after serving ends -- e.g. the final collection at `MeterProvider.shutdown()` -- still reach real stdout); (2) `SpanProcessor.on_end()` receives a `ReadableSpan` with no public mutation API and the SDK locks the span's attribute container at `end()`, so Phase 6's redaction must replace the private `_attributes`/`Event._attributes` containers -- with a record-only recommendation to narrow `opentelemetry-sdk` to `<2.0.0` (Task 7.1 applies it); (3) an unreachable OTLP endpoint logs 3x WARNING (one per internal retry attempt) + 1x ERROR **per failed export cycle**, on every cycle, under `opentelemetry.exporter.otlp.proto.http.trace_exporter`/`...metric_exporter`, and the exporter returns `FAILURE` without raising -- so Task 4.5's de-duplication must be a thin exporter wrapper tracking first-failure/recovery transitions (a bare `logging.Filter` cannot observe recovery); (4) observable-instrument callbacks register at creation time (`callbacks=[...]`, no public `add_callback` in 1.44), run only at collection time, must return an iterable of `Observation` (the old `result(...)` protocol is gone), and keep per-domain series distinct under the default CUMULATIVE temporality -- the callback must yield every live registry entry each cycle and iterate defensively.

#### 2026-09-19 17:10:00.000Z - Task 1a.1 landed: `opentelemetry-sdk` + OTLP/HTTP exporter added to the `mcp` extra; OTLP protocol decision recorded

Task 1a.1, the first task of Phase 1's technical-spike batch, landed: `opentelemetry-sdk>=1.44.0` and `opentelemetry-exporter-otlp-proto-http>=1.44.0` were added to the existing `mcp` extra in `pyproject.toml` (the base `dependencies` list stays untouched) and `uv.lock` regenerated -- a purely additive diff of seven new packages, all pure-Python wheels except the standard `protobuf` runtime (`opentelemetry-sdk`/`opentelemetry-proto`/`opentelemetry-exporter-otlp-proto-http`/`opentelemetry-exporter-otlp-proto-common` at 1.44.0, `opentelemetry-semantic-conventions` at 0.65b0, `protobuf` at 7.36.2, `googleapis-common-protos` at 1.75.3), with `opentelemetry-api` remaining a single, consistent 1.44.0 (no bump, no fork) and no `grpcio` anywhere in the lock. The OTLP exporter protocol decision -- OTLP/HTTP+protobuf (collector port 4318), not OTLP/gRPC (port 4317) -- and its full, metadata-verified trade-off rationale are recorded in the new Decisions Made entry below; Phases 4-6 will build their exporter bootstrap and redaction work against this choice, and Task 7.1 (Phase 7) re-confirms it once all implementation phases have landed.

#### 2026-09-19 17:00:00.000Z - Fourth review: diagnosed the repeated-real-gaps pattern and added a Phase 1a technical spike

A fourth pass reviewed this plan from scratch (prompted by an external question: why does every review keep finding new *real* design errors instead of the plan converging?). Diagnosis: Tasks 0.7/0.8/0.9's three prior reviews all verified claims by *reading* source (the installed `mcp==2.0.0` SDK, the current codebase) rather than by *running* anything -- which explains why they caught API-shape mismatches but never a library's runtime defaults or feasibility limits. Two concrete, previously-undetected problems confirmed this diagnosis: (1) the OpenTelemetry Python SDK's `ConsoleSpanExporter`/`ConsoleMetricExporter` default their output stream to stdout, so `SPECMGR_OTEL_EXPORTER=console` -- this plan's own documented default -- would corrupt the `stdio` JSON-RPC channel exactly as ACC-001/ACC-003 exist to prevent, unless the exporter is explicitly constructed with its output redirected to stderr; (2) Phases 4-6 need to import `opentelemetry-sdk` symbols (`TracerProvider`, `MeterProvider`, `SpanProcessor`), but that dependency was not scheduled to be added to `pyproject.toml` until Phase 7 (`Task 7.1`), three phases later -- confirmed empirically against the actual `uv.lock`/`.venv`, where only `opentelemetry-api` (a transitive `mcp` dependency) is currently installed, not the SDK package Phases 4-6 depend on. Resolution: appended a batch of technical-spike-and-packaging tasks (Tasks 1a.1-1a.7) to Phase 1's own checklist, immediately after Task 1.6, so this work runs before Phase 2 without renumbering any later phase and every existing Task-number cross-reference elsewhere in this document (`Task 3.2`, `Task 4.9`, etc.) stays valid; a literal new `#### Phase 1a: ...` heading was attempted first but rejected by `feat/models/v1/body.py`'s `_PHASE_HEADING_PATTERN`, which requires an integer phase number, so the tasks instead live inside Phase 1's existing checklist under their own `1a.N` free-text sub-numbering. This batch (a) relocates the Phase 7 packaging tasks (installing `opentelemetry-sdk` + an OTLP exporter, regenerating `uv.lock`) to before any phase that actually needs them importable, and (b) runs small, throwaway-script spikes against the now-installed SDK to convert several other still-assumption-based design claims into confirmed facts before more implementation code is built on top of them: the console exporter's actual output stream and whether redirecting it to stderr is sufficient; whether a `SpanProcessor.on_end()` hook can actually mutate an already-recorded span's attributes/exception-event text using only the public SDK API (Phase 6's redaction design depends on this); the OTel SDK's actual per-attempt-vs-per-failure-episode export-failure logging behavior (Task 4.5 depends on this); and whether an observable/asynchronous instrument behaves the way Task 5.4's doc-cache design assumes. Phase 7 is repurposed into a final packaging-consistency confirmation now that its original work has moved earlier. A separate set of independent gaps found during the same review -- ones that only needed re-deriving the plan against itself, not running anything -- were fixed directly in this same pass rather than deferred to the Phase 1 spike batch: `SPECMGR_LOG_LEVEL` and the boolean-flag env vars (`SPECMGR_LOG_ENABLED`, `SPECMGR_OTEL_ENABLED`, `SPECMGR_LOG_FILE_ENABLED`) had no specified fail-closed validation behavior (ACC-011, Task 1.1/1.2); Task 4.3's fail-open guard for a broken `Server.middleware` contract landed one full phase after the risk it guards against (Task 3.2's middleware append in Phase 3), so Task 3.2 now wraps its own append in a minimal try/except from the moment it lands, with Task 4.3 upgrading that into the full config-aware disable-with-warning behavior; ACC-004 and ACC-012 restated a near-identical guarantee for spans, so ACC-004 was narrowed to log records only, leaving ACC-012 as the sole span-attribute guarantee; and Tasks 5.4/5.5/5.6 gained explicit notes on the `DocCache`-instance-registry gap (12 independent per-domain singletons, no described discovery mechanism for a single meter callback) and the lock-mutual-exclusion regression-test gap (the 13-file lock-timing refactor changes concurrency-critical control flow, but no task verified release-on-exception behavior survives it) that a refactor of this size otherwise risks glossing over. No implementation code was written during this review; Phase 1 still has not started.

#### 2026-09-19 16:10:00.000Z - Plan-fix review: six further gaps closed against the installed SDK source and current codebase

A third review (Task 0.9), prompted by an external review of the plan, re-verified the plan's technical claims directly against the installed `mcp==2.0.0` SDK source (`mcp/server/context.py`'s `ServerMiddleware` protocol docstring, `mcp/server/_otel.py`'s `OpenTelemetryMiddleware`, `mcp/server/mcpserver/server.py`'s middleware-append/`configure_logging` call sites) and the current codebase (`req/tools/_lock.py`, `general/tools/delete.py`, `uc/models/v1/parser.py`, `commands/mcp.py`), and found six gaps the first two reviews had not caught: (1) `ServerMiddleware` wraps every JSON-RPC method (`initialize`, `*/list`, `ping`, `notifications/*`), not only `tools/call`/`resources/read`/`prompts/get` -- added ACC-013, a method-name filter to Task 3.1, and Task 3.7's test; (2) a `resources/read` invocation identifies its target via `ctx.params["uri"]`, not `["name"]` (the SDK's own `OpenTelemetryMiddleware` has no `resources/read` name-extraction branch) -- reworded Task 3.1 and Task 5.1; (3) no task wired the Phase 1 config validation, Phase 2 logging setup, or Phase 4 OTel bootstrap into `server.py`'s actual startup path -- only the middleware append (Task 3.2) was explicit -- added Tasks 1.6/2.5/4.9; (4) ACC-004/ACC-012's "no log record or span attribute ever contains ... a title" guarantee is not achievable as worded, since a free-text title has no structural signature a generic filter can detect, confirmed against real call sites that already embed one verbatim (`UcParseError`'s `f"...{node.title!r}"`, `DeleteError`'s `f"failed to delete {path}: {ex}"`) -- reworded the acceptance criteria, Scope, and the redaction Design Notes bullet into an achievable two-component guarantee, and added Task 6.7 (reword the enumerated exception messages) with a narrow, message-text-only carve-out in Explicitly Out Of Scope; (5) the correlation ID Phase 3 generates and the span ID Phase 4 is supposed to "tie" it to had no described reconciliation mechanism -- specified a single helper (current span's trace ID when telemetry is enabled, else `uuid4()`) that Phase 3 implements from the start, with Phase 4's Task 4.2 now only confirming the span-ID branch engages; (6) Task 5.5's lock-wait-time timing cannot be obtained by wrapping the existing `with lock:` blocks unchanged (that times acquire-plus-hold, not wait) -- clarified the required `acquire()`/`release()` timing shape. Also softened the inaccurate "`SPECMGR_LOG_ENABLED` follows the existing `SPECMGR_*_DIR` naming convention" claim (no boolean-flag precedent exists) and made Task 7.1's OTLP exporter package choice an explicit, documented decision rather than "most likely." REQ `bc356fc9-964a-4274-93ec-4627c5aeb2e5`, REQ `dacd01f4-ffd8-4363-a20b-5ac1ce11eef2`, VCR `0a1c2f63-9576-4463-bf40-8f771f14fefb`, and VCR `54fd355f-0978-4dd0-9e1d-da89432e5321` were updated in the same pass so their redaction/scope wording matches. No implementation code was written during this review; Phase 1 still has not started.

#### 2026-09-19 15:30:00.000Z - Plan-consistency review: Task List/VCR gaps closed before Phase 1

A follow-up review cross-checked the Task List, Design Notes, and all three VCRs against each other and against the REQs (rather than against the installed SDK, which the prior implementation-readiness review already covered), and found five internal inconsistencies: (1) Phase 3's Task 3.1 claimed the middleware would "increment call/error counters," but no `MeterProvider` exists until Phase 4 and the counters themselves are built in Phase 5 -- reworded Task 3.1 to scope Phase 3 to correlation-ID generation and logging only, and added a Design Notes bullet spelling out the Phase 3/Phase 5 division of labor; (2) the fail-closed static-validation rule (ACC-011/Task 1.1) listed the file-sink "enabled flag without companion value" case but not the structurally identical `SPECMGR_OTEL_EXPORTER=otlp` without `SPECMGR_OTEL_ENDPOINT` case -- added it to ACC-011, the environment-variable Design Notes bullet, and Tasks 1.1/1.2; (3) VCR `54fd355f-0978-4dd0-9e1d-da89432e5321` had no acceptance criterion actually verifying REQ `dacd01f4-ffd8-4363-a20b-5ac1ce11eef2`'s central metrics clause (latency, call/error counts, cache hit/miss, lock wait) -- added AC-008 and a corresponding Task 5.7 extension, mirroring how Phase 6 already extends the same VCR with AC-007 for redaction; (4) the `mcp.tool.*` metric/span naming scheme did not distinguish tool invocations from resource/prompt invocations despite the REQs' explicit "every MCP item, not tools only" scope -- added an `mcp.item.type` attribute (`tool`/`resource`/`prompt`) to the naming scheme in the Design Notes, Scope, and Task 5.2; (5) the logging redaction filter (Phase 6) was described as "scoped to specmgr's own log records," which would not catch un-redacted content in third-party/SDK log records that share the same root-logger handlers once `SPECMGR_LOG_ENABLED=true` -- reworded Task 6.1/6.2 and the Design Notes to attach the filter at the `Handler` level instead, covering every record reaching that handler regardless of source logger. Also moved the OTLP-unreachable de-duplication task (previously Phase 5's Task 5.6) into Phase 4 as the new Task 4.5, since it is about the OTel exporter/SDK bootstrap Phase 4 already owns, not about the domain-level metrics instrumentation Phase 5 covers, renumbering the remaining Phase 4/Phase 5 tasks accordingly; clarified Task 4.4's compatibility test to cover both a canary API-shape assertion and a simulated fail-open assertion; and clarified Task 6.5's wording, since AC-007 was already added to the VCR during the earlier implementation-readiness review rather than still needing to be created. No implementation code was written during this review; REQ `dacd01f4-ffd8-4363-a20b-5ac1ce11eef2` and VCR `54fd355f-0978-4dd0-9e1d-da89432e5321` were also updated directly (item-type attribute clarification; new AC-008).

#### 2026-09-19 14:15:00.000Z - Implementation-readiness review: design gaps resolved before Phase 1

Before starting implementation, ran a codebase research pass (via an explore sub-task) that validated every technical claim in the ADR/plan against the actually-installed `mcp==2.0.0` SDK source and the current `src/`/`tests/`/`general/` structure, and surfaced several design details the REQ/ADR/QA chain had left unspecified: (1) `MCPServer.__init__` already unconditionally calls `configure_logging()` today, confirmed to emit zero stderr output in its default configuration, so ACC-001's baseline holds, but `telemetry/logging.py` must explicitly reconfigure rather than rely on a no-op `basicConfig()`; (2) Phase 4's tracing activates the SDK's own built-in `OpenTelemetryMiddleware` rather than creating a new span, meaning a redaction safeguard scoped only to this feature's own attributes would leave virtually all trace-span content (including raw exception messages) unredacted; (3) no tool/resource/prompt-name -> domain mapping exists yet for the `mcp.domain` metric attribute; (4) the OTel SDK's own exporters likely log export failures per attempt, not once, so the "one message per failure episode" requirement needs explicit de-duplication logic, not just default SDK behavior; (5) no defined behavior existed for a static config error (e.g. missing file-sink path) versus the runtime fail-open policy. Four stakeholder decisions resolved these: static misconfiguration fails closed (raises at startup); the `mcp` dependency keeps its unbounded `>=2.0.0` pin; and, after a follow-up clarification that most trace spans are SDK-owned rather than this feature's own, redaction was widened from "this feature's own middleware attributes only" to a global, `TracerProvider`-level `SpanProcessor` covering every span in the process. A new "Observability Degrades Gracefully, Application Logic Never Does" convention was added to `.specmgr/conventions.md` documenting the fail-open (runtime)/fail-closed (static) split as a deliberate, bounded exception to the existing error-handling convention. The Design Notes, Task List (Phase 1 config validation, Phase 2 root-logger handling, Phase 5 domain mapping, Phase 5/6 OTLP de-duplication and widened redaction plus a new telemetry-VCR-extension task, Phase 8 an incidental `AGENTS.md` fix), and Acceptance Criteria (ACC-011, ACC-012) were all updated accordingly. No implementation code was written during this review.

#### 2026-09-19 13:30:00.000Z - Phase 0 complete: REQs finalized, third REQ added, three initial VCRs authored

A second elicitation round on QA `40a17fb8-c069-4c38-a092-47912e1fa41d` resolved six follow-up questions raised during a review pass (VCR timing, functional-suitability scope, performance-efficiency overhead/sampling, compatibility middleware behavior, interaction-capability status signal, reliability degradation/file-sink, security title redaction/correlation-ID exposure, maintainability naming/switch granularity), including four that required an explanation before the stakeholder could decide (trace sampling, `Server.middleware`, correlation-ID/`ctx` exposure advantage, `rpc.server.duration` naming impact). REQ `bc356fc9...`/`dacd01f4...` were reworded to broaden scope to every MCP item, add id/type-only logging, an opt-in file sink, title redaction, correlation-ID-on-error-only, MCP-specific metric naming, and fail-open behavior on OTLP/middleware failures. A new REQ `41444084-6821-426d-84a2-028a3f4fed0b` (status resource, Interaction Capability) was created. Per the stakeholder's explicit instruction, three initial, partial-coverage VCRs (one per REQ, since a VCR's `## Verifies` is single-valued) were authored immediately rather than deferred until after implementation, and the general "author VCR early, extend phase-by-phase" practice was added to `.specmgr/conventions.md` so it applies to future features too.

#### 2026-09-19 12:30:00.000Z - Planning pipeline completed

Ran the full QA -> REQ -> ADR -> feature-folder pipeline recommended by the GitHub issue #139 planning comment. Elicited stakeholder answers via the `question` tool (logging: stdlib `logging` + MCP SDK rich handler; telemetry: OpenTelemetry API+SDK; dual rich/JSON log format; both features fully opt-in/off by default; redact document bodies and absolute paths; correlation ID tied to OTel span; five telemetry signals of interest). The stakeholder later decided two separate REQ documents (logging, telemetry) were wanted instead of one combined REQ, so REQ `bc356fc9-964a-4274-93ec-4627c5aeb2e5` and REQ `dacd01f4-ffd8-4363-a20b-5ac1ce11eef2` were created and cross-referenced, while a single ADR (`fdbb6d22-278a-4ecf-b2f6-db208bc49fc6`) still covers the combined technology decision. Research during planning (via a dedicated explore sub-task) confirmed the concrete, no-per-tool-edit implementation mechanism: the installed `mcp==2.0.0` SDK's `Server.middleware` list, appendable on the single `mcp = MCPServer(...)` instance in `server.py`.

### Decisions Made

<!-- Newest entry first -- prepend new entries directly below this comment. -->

#### 2026-09-24 09:57:00.000+02:00 - Phase 6 implementation pins: the scrub regex/token as implemented, the span-container replacement shape, the processor order, the two documented residuals, the fail-open, and the Task 6.7 rewordings

The orchestrator's Phase 6 pins landed as stated, plus these implementation-level details settled: (1) **The scrub regex as implemented** (`telemetry/redact.py`, one compiled pattern of three alternatives): the segment class is ``[^\s\"'`()\[\]{}<>|;:,\\]`` (anything but whitespace, quotes, brackets/parens/angle braces, pipes, semicolons, commas, colons, and backslashes -- it excludes the backtick itself, hence the double-backtick code span), the separator run is ``\\+|/`` (a backslash run -- covering both the raw and the JSON-escaped spelling of one logical backslash -- or a slash), and the alternatives (with `(SEG)` denoting the segment class and `(SEP)` the separator run) are POSIX ``(?<![:/])/(?!\d)((SEG)+/)+(SEG+)(?<!\.)`` (two-or-more segments; the leading-`/` lookbehind guards `scheme://`/`specmgr://` shapes and the first-segment-not-a-digit guard version-matrix tokens like `3.11/3.12/3.13`), Windows drive ``(?<![A-Za-z0-9])[A-Za-z]:(SEP+SEG+)(SEP+SEG+)*(?<!\.)`` (covers `C:\a\b` raw, `C:\\a\\b` JSON-escaped, and mixed `C:/a/b`), and UNC ``\\{2,}(SEG+)(\\+SEG+)+(?<!\.)`` (raw `\\host\share` and JSON-escaped `\\\\host\\share` alike). Every match is replaced by exactly `<redacted-path>`. What it catches: the traceback `File ".../abs/path.py", line N` shapes, `file:line` references (the line number is kept -- the colon is not a segment character), and exception messages embedding absolute paths, in both raw and JSON-escaped rendering. What it does not (the documented limits): single-segment POSIX paths (`/tmp`), paths with spaces, POSIX segments containing backslashes (only the prefix up to the backslash matches), and it has one known false positive (a URL path with two or more segments, `https://host/a/b`); it never attempts title detection. A verified, fixed bug from the first draft: the segment class originally admitted `\`, so on the final rendered JSON string the match consumed the escape backslash of a closed `\"` quote (corrupting the JSON -- caught by `TestFormatterScrub`'s `json.loads` assertion); excluding backslash from the segment class (backslashes enter the pattern only as explicit separator runs) makes the scrub structurally safe on rendered JSON, which is now pinned by a dedicated JSON-validity test case. (2) **The span-container replacement shape as implemented**: `RedactionSpanProcessor.on_end` replaces `readable_span._attributes` and each `event._attributes` with `BoundedAttributes(None, scrubbed, immutable=False)` -- chosen over a plain dict (Task 1a.3 proved both work) to keep the container type the SDK's own `dropped_attributes` property `isinstance`-checks; the replacement happens **only when the scrub changed a value** (no needless container swap per span -- pinned by `test_a_span_with_no_path_content_keeps_its_original_container` asserting container identity). Verified against the installed 1.44.0 source: `ReadableSpan.attributes`/`events` re-wrap the live `self._attributes`/`self._events` on every access, and both export paths (the in-memory/console `to_json` and the OTLP protobuf encoder) read those properties at export time, so the replacement is visible end to end. (3) **The processor order, verified**: the `TracerProvider` keeps its processors in a `SynchronousMultiSpanProcessor` that invokes `on_end` in add order (1.44.0 source, `SynchronousMultiSpanProcessor.on_end`), so `bootstrap_telemetry` adds the `RedactionSpanProcessor` *before* the `BatchSpanProcessor` (the span is scrubbed before it is enqueued); pinned by `TestTelemetryWiring::test_the_bootstrap_adds_the_redaction_processor_before_the_batch_processor` and proven end to end by `TestTelemetryWiring::test_a_production_chain_span_with_a_path_and_exception_exports_scrubbed` (real bootstrap -> batch processor -> console exporter to the captured stderr). (4) **Two documented residuals** (scope is exactly the pin's -- attributes plus each event's attributes): the rich-rendered traceback (frame file paths, the exception's own line) is produced by rich's `Traceback` renderable outside the pinned `render_message` seam and is not scrubbed (the JSON path -- production and the file sink -- is fully scrubbed; rich is the local-dev format), and a span's `status` *description* (the `set_status(StatusCode.ERROR, str(e))` payload -- observed unscrubbed in the first production-chain test run) is not an attribute container and is not scrubbed; for the mandatory Task 6.7 set this residual is neutralized at the source (those `str(e)`s no longer carry a title). Both are pinned in `telemetry/redact.py`'s docstrings. (5) **Fail-open**: an `on_end` that hits an unexpected span shape (e.g. a future SDK restructure, before the canary or Task 7.1's version pin catches it) logs exactly one warning per processor on the `biz.dfch.specmgr.telemetry` logger and leaves the span unscrubbed -- never propagating into the ending span's request (the "Observability Degrades Gracefully, Application Logic Never Does" convention; pinned by `TestSpanProcessorFailOpen`). (6) **The Task 6.7 rewordings, per site** (13 sites; old -> new; type/raise condition unchanged): `more than one top-level (H1) heading found; second one is {title!r}` -> `more than one top-level (H1) heading found` (the second H1 is the document's own title); `heading level H{level} is not part of the use case schema: {title!r}` -> `heading level H{level} is not part of the use case schema` (six sites: under H1, under Characteristic Information, under Related Information, under Extensions, under Sub-Variations); `unrecognized H2 heading {title!r}` -> `unrecognized H2 heading`; `unrecognized H3 heading under Characteristic Information: {title!r}` -> `unrecognized H3 heading under Characteristic Information`; `duplicate H3 heading {title!r}` -> `duplicate H3 heading` (two sites: Characteristic Information, Related Information); `unrecognized H3 heading under Related Information: {title!r}` -> `unrecognized H3 heading under Related Information`; `unrecognized Extension heading (expected '{stepRef}. {condition}'): {title!r}` -> `unrecognized Extension heading (expected '{stepRef}. {condition}')`; `unrecognized Sub-Variation heading (expected 'Step {N}: {label}'): {title!r}` -> `unrecognized Sub-Variation heading (expected 'Step {N}: {label}')`. The heading-level/kind (and the already-present expected-shape hint) is what the messages now reference; no line numbers were added (the parser's line indices are body-relative, and adding them would have widened the message change beyond the pin). (7) **Scope adherence**: the deprecated ADR domain's 9 title sites were left untouched (the accepted residual gap -- its code is off-limits in this phase), and the ~25 path-embedding sites were deliberately not reworded (optional per the plan; scrub-covered in logs/spans; minimal diff). The existing `tests/uc/models/v1/test_parser.py` assertions needed no changes (they assert the exception type, never the message text); the only test ripple was the three `test_logging.py` formatter assertions, updated to pin the Task 6.2 wiring (`ScrubbingFormatter` wrapping `JsonFormatter`).

#### 2026-09-23 22:25:00.000Z - Phase 5 verification: the "flaky missing mcp.lock.wait_time" report was an invalid-status repro, not a recording defect -- root-caused and pinned by a production-path regression test

Orchestrator verification of the staged Phase 5 reported `mcp.lock.wait_time` data points missing on production call paths (an in-process `create_req` + `set_status(rid, "req", "review")` repro 3/3, and flaky real stdio sessions). Root cause, established by running the report's own conclusive checks first and then re-running the exact repro: **the repro's `set_status` call never took a domain lock, so the absent series is correct behavior, not a defect in the Task 5.5 recording path.** Evidence: (1) the conclusive identity comparison from the report came back clean in the repro process -- exactly one `biz.dfch.specmgr.req.tools._lock` instance in `sys.modules` (full `__file__` re-scan included), `general.tools.set_status.req_lock is <sys.modules instance>.req_lock` True, `__wrapped__` identity True, `__wrapped__.__globals__` identity True (owned by exactly that sys.modules entry), the production function's resolved `record_lock_wait` is the sys.modules `telemetry.metrics.record_lock_wait` (one `telemetry.metrics` instance), and the `_lock_wait_histogram` slot holds one non-`None` histogram with identical `id()` for the production path and the probe -- ruling out the module-instance split (a), the double-bootstrap/two-histogram (b), the wrong-provider (c), and the swallowed-failure (d) suspects (no fail-open warning ever, and records land when the path runs); (2) the repro used `"review"`, which is **not** in REQ's closed status vocabulary (`accepted`/`deprecated`/`draft`/`implemented`/`proposed`/`rejected`/`superseded`), and the generic `set_status`'s `_check_status_allowed` guard is by design "called before any domain lock is taken or any file is read" (ADR b399f1ce-ed42-4929-b01c-7a57d18e8014's Decision Outcome point 3) -- it returns the structured, non-raising `InvalidStatusResult` as a *successful* JSON-RPC result (no `isError`) whose `status` field echoes the rejected request, which is why the repro read as "a real write; returns status review" while performing no write and taking no lock (confirmed verbatim over stdio: the tool result for `"review"` is `{"valid": false, "type": "req", "status": "review", ...}` and the export carries `mcp.tool.call.count`/`mcp.tool.duration` for the observed invocation but no `mcp.lock.wait_time`); (3) determinism proof with real writes: in-process `create_req` + `set_status(..., "proposed")` (real bootstrap + `_lifespan` shutdown's final collection) exported `mcp.lock.wait_time` with `count: 1`, `mcp.domain="req"`, and the pinned bucket boundaries in 3/3 runs, and real `specmgr mcp` stdio sessions with a valid status exported it in 3/3 runs. Fix/deliverable: **no production-code change** (the 13-file refactor shape, the slot mechanism, and the import-instance hygiene are all sound -- the report's own forensics confirmed it), but the ambiguity is now pinned by `tests/telemetry/test_lock_wait.py::TestLockWaitProductionPath` (2 new tests, going through the real production chain: the real `create_req` tool function, the real generic `set_status` dispatching to `_set_status_req`'s `with req_lock(id_):`, an InMemoryMetricReader-collected production bootstrap): a valid status is a real write and produces the series (count >= 1, `mcp.domain` attribute), while an invalid status returns `InvalidStatusResult`, takes no lock, leaves the series count unchanged, and leaves the document unmodified. Full gate re-run green (3589 passed).

#### 2026-09-22 20:40:00.000Z - Phase 5 implementation pins: the View/bucket mechanism in SDK 1.44.0, the mapping's edge rules, the instrument-creation split as landed, and the checkpoint-recovery fact

The orchestrator's Phase 5 pins landed as stated, plus these implementation-level details settled while repairing/completing the cancelled attempt's tree (which was correct in its core shape -- the `domain_mapping.py` tables, `metrics.py` slot/helper/callbacks, the middleware's lazy instrument creation + `domain` extra, the `_doc_cache.py` counters/registry, the 12 `DocCache("<domain>")` edits, and all 13 `_lock.py` refactors passed review hunk by hunk against the pins; what was missing was the Task 5.6 test coverage, the two test updates the new behavior required (the Phase 3 `domain`-absence assertion, the Phase 4 otlp metric-episode assertion), the VCR, the docs, and the gate). (1) **The View/bucket mechanism, verified in the installed SDK 1.44.0 source**: the bucket advice is expressed as one `opentelemetry.sdk.metrics.view.View(instrument_type=Histogram, instrument_name=<name>, instrument_unit="ms", aggregation=ExplicitBucketHistogramAggregation(boundaries=<pinned tuple>))` per pinned histogram, passed to the `MeterProvider` constructor's `views=` parameter -- i.e. registered *before* any instrument is created, so the middleware's own `mcp.tool.duration` histogram (created lazily on first observation from the same provider's `Meter`) picks its View up automatically; confirmed end to end by `tests/telemetry/test_metrics.py::TestBootstrapInstruments` (an `InMemoryMetricReader` standing in for the `PeriodicExportingMetricReader` via the Phase 4 global-state-reset pattern) asserting `data_point.explicit_bounds == (5.0, 10.0, 25.0, 50.0, 100.0, 250.0, 500.0, 1000.0, 2500.0, 5000.0, 10000.0)` for `mcp.tool.duration` and `(1.0, 5.0, 10.0, 25.0, 50.0, 100.0, 250.0, 500.0)` for `mcp.lock.wait_time`, with `unit == "ms"`, and by the real-stdio sanity session's console export. (2) **The mapping's edge rules** (`telemetry/domain_mapping.py`): resources map by the uri's first path segment after `specmgr://` when that segment is one of the 13 domains (verified against the live registrations: `specmgr://rsk/tara`/`specmgr://rsk/risk-matrix` -> `rsk`, the parameterized `specmgr://adr/{id}` template and every concrete `specmgr://adr/<uuid>` read -> `adr`); the seven cross-cutting resources (`specmgr://version`, `specmgr://config`, `specmgr://iso25010`, `specmgr://dtais`, `specmgr://ears`, `specmgr://rasci`, `specmgr://telemetry/status`) have no domain; prompts map by name via an explicit table (`compact_history` is the one no-domain prompt; the irregular names `refine` -> `qa`, `create_task`/`implement_task`/`update_task` -> `tsk`, `create_risk`/`update_risk` -> `rsk`, the ADR A/B variants `create_adr_test`/`update_adr_test` -> `adr` are the reason the tables are explicit lists rather than heuristics); tools map by name except the five generic dispatch tools, which map via their `type` argument at call time (a `type` value outside the 13 domains, an absent `type`, or a non-string `type` yields no domain); the deprecated ADR domain counts as a domain for metrics; the live-registration drift test (`tests/telemetry/test_domain_mapping.py::TestMappingAgainstLiveRegistrations`) pins both directions (no registered tool/prompt goes unmapped, no table entry is stale) and asserts every registered resource is consistent with the first-segment rule, so a future rename/add trips CI instead of silently losing attribution. (3) **The instrument-creation split as landed**: the bootstrap (`telemetry/otel.py`) creates `mcp.lock.wait_time` (histogram) + `mcp.cache.hit`/`mcp.cache.miss` (observable counters, callbacks registered at creation per Task 1a.5's confirmed 1.44.0 API) and stores the `Meter`/histogram in the `telemetry/metrics.py` slots (this module's own `_meter` keeps mirroring the `Meter` for the Phase 4 tests); the middleware lazily creates `mcp.tool.duration`/`mcp.tool.call.count`/`mcp.tool.error.count` on first observation (create-once, cached per instance, double-checked under its own lock); `shutdown_telemetry` clears the `telemetry/metrics.py` slots after shutting down the providers (the final collection still sees them). `telemetry/__init__.py`'s own eager `from . import config, logging, middleware, otel` was removed (it is documentation-only now): the base-library-safe `_lock.py` modules import `telemetry.metrics` through the package `__init__`, and the eager import would have dragged `mcp`/`opentelemetry.*` (via `middleware`/`otel`) onto the base library -- every existing importer uses direct submodule paths (`from .telemetry.config import ...`), so nothing depends on the re-exports. (4) **DocCache details**: the hit/miss counters are incremented at `DocCache.read`'s two real decision points (a hash-matched entry -- cached success *or* cached failure -- counts a hit under the instance lock before the stored result is returned/re-raised; the fall-through to `parse_fn` counts a miss), are plain ints, and are *not* reset by `reset()` (the cumulative counters match the CUMULATIVE reader temporality; `reset()` clears entries only); `DOC_CACHE_REGISTRY` get/set is guarded by a module-level `_registry_lock` (the same discipline as the instance's own `_lock`); `reset_doc_cache_registry()` is test-only and stays vulture-clean through `__all__` (vulture's export-pattern handling), no whitelist entry needed. (5) **SDK 1.44.0 test-side finding** (no production impact): `InMemoryMetricReader.get_metrics_data()` returns `None` (not an empty `MetricsData`) when no instrument has recorded any data yet, and the console export's per-scope metric ordering is first-recorded-first (`test.counter` before the zero-valued `mcp.cache.*` observables in `TestBootstrapConsole`), so the Phase 4 `metrics[0]` assertion survived Phase 5 unchanged. (6) **The 13 `_lock.py` refactors** kept each file's public API byte-identical (`_lock_for`, the `<domain>_lock` context manager, feat's `_create_lock`/`feat_create_lock`), per-id locks stay per-id, and the shape is `started = time.monotonic(); lock.acquire(); record_lock_wait(_LOCK_DOMAIN, (time.monotonic() - started) * 1000.0); try: yield finally: lock.release()` (wait = acquire duration only, never hold); `record_lock_wait` never propagates (a raising histogram would leak the just-acquired lock) -- it fail-opens with one warning and process-level silence thereafter.

#### 2026-09-22 18:18:50.000Z - Phase 4 correction: the Task 4.5 suppression filter is armed at the start of each export attempt, not on the first FAILURE return

Phase verification reproduced an ACC-010 contract violation in the Task 4.5 wrapper: over a real `specmgr mcp` stdio session with `SPECMGR_OTEL_EXPORTER=otlp` at an unreachable endpoint, the first cycle's raw SDK line (`ERROR opentelemetry.exporter.otlp.proto.http.trace_exporter "Failed to export span batch ..."`) reached stderr *before* the wrapper's single episode message (at the production-default 10 s exporter timeout the whole first cycle -- 3x WARNING + 1x ERROR -- leaks). Root cause: the installed SDK's OTLP HTTP exporters log their per-attempt failure lines *during* the `export()` call (each retry's WARNING and the final ERROR are emitted inside the attempt's own retry loop), i.e. **before** `export()` returns the `FAILURE` the wrapper keys its episode state on -- so a filter attached only at first-FAILURE-return could never suppress the first cycle. Fix (pinned into `telemetry/otel.py`): the state-gated `_ExporterEpisodeFilter` is now **armed at the start of every `export()` attempt** (attached idempotently to exactly the two exporter logger names before delegating to the wrapped exporter) and **disarmed on a `SUCCESS` return or `shutdown()`**; the filter gates on the armed state (a new `OtlpExporterWrapper.armed` property), the episode state machine is otherwise unchanged (first `FAILURE` return of an episode emits the single warning, further failures are silent, `shutdown` ends any episode and starts none new), and all transitions stay under the existing lock (one message per episode even under the SDK's batch/periodic thread interleaving). The observable contract is exactly ACC-010's: across a whole unreachable-endpoint episode the only failure-attributable stderr output is the single wrapper message -- zero lines from `opentelemetry.exporter.otlp.proto.http.trace_exporter`/`...metric_exporter`, including the first cycle's. Suppression during a healthy attempt loses nothing (the SDK logs no successful export), and a `SUCCESS`-then-`FAILURE` sequence still starts a fresh episode with its own single message. Proven by three new unit scenarios driving a fake exporter that emits the real per-attempt lines *inside* `export()` (`tests/telemetry/test_otel_exporter_wrapper.py::TestFirstCycleAttemptSuppression` -- first cycle, recovery + second episode, and a healthy attempt's arm/disarm window; verified red against the old arming behavior), by the strengthened `tests/telemetry/test_otel.py::TestStdoutSafetySubprocess::test_unreachable_otlp_endpoint_writes_zero_bytes_beyond_json_rpc_to_stdout` (zero raw exporter-logger records across the whole child session, not just the wrapper-message count), and by the re-run orchestrator repro (2 tool calls, `OTEL_EXPORTER_OTLP_TIMEOUT=1`: exactly one failure-related stderr line, the wrapper's). Full standard quality gate re-run green (see the Updates entry).

#### 2026-09-22 10:49:41.000Z - Phase 4 implementation pins: the wrapper subclasses the SDK exporter base classes, the OTLP endpoint paths are appended by the bootstrap, the fail-open guards only the contract surfaces, and the tests prove it with real stdio subprocesses

The orchestrator's Phase 4 pins landed as stated (the meter slot in `telemetry/otel.py` with no counter/histogram creation -- the histogram bucket advice stays Phase 5's spec; the `server.py` module-scope order as a reordering, `bootstrap_telemetry` between `setup_logging` and `MCPServer(...)`; `_lifespan`'s post-`yield` calling the module-level `shutdown_telemetry()`; global-API provider registration, nothing set when disabled). Six implementation-level details the plan left open were settled, two of them by re-verifying against the installed `opentelemetry-sdk` 1.44.0 source rather than the plan's assumption-based text: (1) **The Task 4.5 wrapper subclasses the SDK's exporter base classes** (`OtlpExporterWrapper(SpanExporter, MetricExporter)`): the first draft was a plain delegating object, but `PeriodicExportingMetricReader.__init__` reads `exporter._preferred_temporality`/`exporter._preferred_aggregation` off the exporter instance (set by `MetricExporter.__init__`), so the plain wrapper crashed the otlp bootstrap with an `AttributeError` -- reproduced and fixed; the wrapper now calls `MetricExporter.__init__` with the wrapped exporter's own values (span exporters carry no such attributes -> `None`), satisfies the ABC's abstract surface with its pass-through `export`/`force_flush`/`shutdown`, and additionally falls back to the wrapped exporter via `__getattr__` for any attribute it does not define itself (a future SDK-internal read keeps working). The pin's "one shared wrapper class, two instances" is unchanged. (2) **The bootstrap appends the spec-mandated OTLP paths itself**: verified against the 1.44.0 source that an explicit `OTLPSpanExporter(endpoint=...)`/`OTLPMetricExporter(endpoint=...)` argument is POSTed as-is -- the SDK's `_append_trace_path`/`_append_metrics_path` apply only to the `OTEL_EXPORTER_OTLP_ENDPOINT` env-var fallback -- so "the `SPECMGR_OTEL_ENDPOINT` base URL as-is" (the pin's wording, based on the Design Notes' "the exporter ... appends the spec-mandated ... paths itself" belief) would POST to the bare base URL and never reach a collector; the bootstrap extends the base URL with `/v1/traces`/`/v1/metrics` (the SDK's own trailing-slash semantics) to make the plan's stated intent hold, and pins it in `test_otel.py::TestOtlpExporterConstruction`. (3) **The Task 4.3 fail-open mechanism is both halves of the pin's latitude**: a startup `middleware_contract_compatible(server)` check (the `Server.middleware` list type + the `ServerMiddleware.__call__` signature vs. this middleware's own, both async positional `(ctx, call_next)`) around the Task 3.2 append -- same mechanism, upgraded, not a second guard -- plus a call-time guard in the middleware that wraps ONLY the contract surfaces (the `ctx.method` read, the pre-call identity extraction, the post-call result processing) in `except (AttributeError, TypeError)`, never `call_next` itself: a first violation logs exactly one warning (via the `biz.dfch.specmgr.telemetry` logger), flips `enabled` to `False` for the process, and still completes the request (pre-call: `call_next(ctx)` fresh; post-call: the result the chain already produced, unannotated -- `call_next` is never executed twice), and an app-level exception (e.g. a tool raising `TypeError`) is handled by Phase 3's own per-channel branches and never misread as a contract violation (unit-proven). (4) **Task 4.2's test mechanism is the most-faithful option**: a real `specmgr mcp` stdio subprocess session (a fresh child process -- pristine OTel set-once globals, the true import order in `server.py`'s module scope) with `SPECMGR_OTEL_ENABLED=true` + `SPECMGR_LOG_ENABLED=true` (JSON format) and the default `console` exporter: the parent feeds `initialize`/`tools/call`/`resources/read`/`prompts/get`, waits for the four JSON-RPC responses, closes stdin, and asserts on the child's captured stderr that each invocation's exported SDK span `traceId` (parsed from the console span JSON exported at the `_lifespan` shutdown flush) equals the middleware completion record's `correlation_id` -- measured e.g. `6a86d52b370f817929b7627f87afebf7` for `tools/call get_feat_template` (and the same match for the resource and prompt invocations, all spans carrying `service.name = "specmgr"`). (5) **In-process bootstrap tests isolate the OTel API's set-once global state** (`test_otel.py::_isolated_otel_globals`): the bootstrap must use the public `set_tracer_provider`/`set_meter_provider` (the production path), but those are once-per-process, so the test installs pristine `Once` guards + `None` providers around itself and restores the worker process's previous provider objects and the guards' used/unused state on exit (plus the metrics proxy's `_real_meter_provider`), so no other test in the xdist worker sees this test's providers; every in-process provider is shut down before restoration. (6) **Task 4.10's otlp case uses the SDK's own `OTEL_EXPORTER_OTLP_TIMEOUT=1`** to shorten the 10 s production export timeout (the test asserts the stdout guarantee and the wrapper's one-span-episode message, not the timeout), and asserts the metric exporter emits NO episode message in this phase: no metric instrument exists until Phase 5, so the reader's final collection is empty (`MetricReaderStorage.collect` returns `None` -> `_receive_metrics`/`export` never run) -- the metric wrapper's episode de-duplication is unit-covered by `test_otel_exporter_wrapper.py` instead. The wrapper's episode messages name the condition and state the repeat policy: "OTLP span/metric export is failing (the configured endpoint is unreachable or not accepting exports); spans/metrics will not reach the collector until export recovers -- this message repeats only for a subsequent failure episode".

#### 2026-09-21 22:09:17.802Z - Phase 3 implementation pins: channel (c) targets the dispatcher's modern mapping, non-mapping `MCPError.data` is replaced, the middleware observes the serialized wire dict, and `__call__` carries no input asserts

The orchestrator's Phase 3 pins landed as stated -- the `domain` extra omitted from Phase 3 log records (the name -> domain mapping is Task 5.1's new design work; the formatters emit only what is present); the per-channel `exception` field shapes (`{type, message, traceback}` for raised `MCPError`/raw exceptions, `{type: "tool_error", message}` without a `traceback` key for a `tools/call` `isError: true` result); the span-first correlation-ID helper implemented fully now (degrading to the `uuid4()` branch until Phase 4 sets a provider, both branches unit-tested incl. against a real `TracerProvider`); INFO for start/completion and ERROR for failed on the module logger `biz.dfch.specmgr.telemetry`; `duration_ms` measured with `time.monotonic()`, rounded to 3 decimals, present on completion/error records only; and the middleware taking the parsed `TelemetryConfig` at construction (`server.py` passes its own module-scope `telemetry_config`). Five implementation-level details the plan left open were settled: (1) **Channel (c) replicates the dispatcher's MODERN error mapping, and the legacy-era divergence is accepted**: verified against `mcp/server/runner.py`'s `modern_error_data` and `mcp/shared/jsonrpc_dispatcher.py`'s `handler_exception_to_error_data`, a raw exception converts to `MCPError(INTERNAL_ERROR, "Internal server error", {"correlationId": ...})` and a `ValidationError` to `MCPError(INVALID_PARAMS, "Invalid request parameters", {"correlationId": ...})` -- the plan's pinned wire-identical target, with the `data` field intentionally carrying the correlation ID where the ladder's own `ValidationError` mapping uses the empty string (a scalar cannot hold the ID; the plan's channel (c) explicitly allows the `data` addition). The legacy (2025-handshake) stdio era's catch-all for *unconverted* raw exceptions maps to `code=0`/`str(e)` instead (an SDK v1-compat pin in `JSONRPCDispatcher`; `runner.py`'s modern path is what the plan verified) -- unreachable for the errors this middleware produces, since an `MCPError` always keeps its own `ErrorData` in every era, and for the three invocation methods when the middleware is enabled (they are converted before the dispatcher); it is what the both-off default config preserves byte-for-byte (smoke-verified). (2) **A non-mapping `MCPError.data` is replaced, not preserved**: channel (b) merges `correlationId` into `e.error.data` when it is a mapping (the SDK's `resources/read` failures carry `{"uri": ...}`; elicitation data is a dict) and starts a fresh `{"correlationId": ...}` mapping when it is `None` or an unobserved non-mapping scalar -- a scalar cannot hold an extra key, and the three invocation methods have no SDK-produced scalar-`data` `MCPError` shape. (3) **The middleware observes the dispatcher's already-serialized wire dict, not the handler's model**: verified against `mcp/server/runner.py`, `_inner` returns `self._serialize(...)` -- the dumped wire dict (`isError` camelCase, `_meta` incl. the modern `serverInfo` stamp) -- into the middleware chain, so production traffic exercises the dict branch of the defensive dict/model handling; the model branch stays, mirroring the SDK's own `match CallToolResult(is_error=True) | {"isError": True}` template in `mcp/server/_otel.py`. (4) **`__call__` deliberately carries no input asserts**: the repo's assert-on-inputs convention is honored in `__init__` (the `TelemetryConfig` type check), where Task 3.2's startup guard already wraps the call; `ctx`/`call_next` come from the provisional SDK contract, so a future signature/semantics change must degrade (the Task 3.2 guard at append time, Task 4.3's runtime fail-open) rather than raise an `AssertionError` on every request -- per the "Observability Degrades Gracefully, Application Logic Never Does" convention. (5) **Log-record messages are `"{item_type} {item_name} {phase}"`** (the item name omitted when it cannot be extracted from the raw params) -- id/type-only identity on one greppable line, the structured fields in `extra=`.

#### 2026-09-21 15:13:45.639Z - Phase 2 implementation pins: the rich format's `render_message` seam, the `exc_info`-derived `exception` field, and the file sink's append mode

Three implementation-level decisions from Task 2.1, each serving (not contradicting) the plan's pinned record shape: (1) **The rich format appends its structured fields in a `SpecmgrRichHandler.render_message` override, not a `format()` override**: rich's `RichHandler.emit` funnels both the plain-message path and the `rich_tracebacks` exception path through `render_message(record, message)` as the single message-text point, whereas `format()`'s output is discarded for exception records when `rich_tracebacks` is on (the SDK's own construction enables it) -- a `format()`-only override would silently drop the structured fields from exactly the error records that matter most; `render_message` is therefore the rich-format seam the Task 6.2 scrub hooks onto, next to `JsonFormatter.format` for the JSON path. (2) **The `exception` field is derived from the record's standard `exc_info` when no explicit `exception` extra is present**: the pinned shape says `exception` arrives as an `extra=` kwarg (Phase 3's middleware), but the SDK's own `logger.exception` records use the standard mechanism -- deriving `{type, message, traceback}` from `exc_info` keeps the pinned shape for those records instead of silently dropping their traceback; an explicit `exception` extra always wins when present, and a record carrying neither renders the field absent (the "emit only what is present" rule). The rich format renders the field as `Type: message` only, since `rich_tracebacks` already displays the `exc_info` traceback separately. (3) **The file sink opens in append mode (`mode="a"`) so a server restart does not truncate the previous session's records** -- the plan pins the sink's on/off gating and JSON-only output (ACC-009) but not its open mode.

#### 2026-09-21 12:33:56.326Z - CLI import chain executes server.py before any command function: clean refusal added at cli.py too; per-variable case sensitivity and status-resource read semantics pinned

Two plan-consistency findings from Task 1.6's implementation, plus the per-variable case-sensitivity reading Task 1.1 pinned. (1) **The CLI's import chain executes `server.py`'s module scope before any command function runs**: `cli.py`'s `from .commands import ...` pulls `commands/__init__` -> `adr_toc` -> `adr/__init__` -> `adr/prompts/create_adr` -> `general.tools._packaged_data` -> `general/__init__` -> `general/tools/__init__` -> `delete` -> `dec.tools._cache` -> `dec/__init__` -> `dec/prompts/create_dec` -> `from ...server import mcp`, so the plan's assumed first-import point (`commands/mcp.py`'s lazy `from ..server import mcp as mcp_server` inside the command function) is never the first import under `specmgr mcp` -- `server.py`'s module scope (and therefore Task 1.6's `load_telemetry_config()` call) already ran at CLI import time, and the first observed behavior for a broken env was a raw traceback out of the `cli.py` import. ACC-011's clean refusal (single stderr line naming the offending env var(s), exit code 1, empty stdout) therefore had to be added at `cli.py`'s command import as well, mirroring `commands/mcp.py`'s own `except TelemetryConfigError` branch, which stays -- it is the correct handler whenever the command function's import is the first (as the in-process test exercises), and the plan pins it. Consequence worth noting: under a static telemetry misconfiguration, every `specmgr` CLI command (not only `specmgr mcp`) now refuses to start, since they all share that import chain; under a valid environment no command's behavior changes. (2) **Case sensitivity is pinned per variable, not globally**: the Design Notes' "Environment variables" bullet marks `SPECMGR_LOG_LEVEL` and the boolean flags as case-insensitive and names `SPECMGR_LOG_FORMAT`/`SPECMGR_OTEL_EXPORTER` only as "closed sets" -- implemented as exact, case-sensitive sets (`rich`/`json`, `console`/`otlp`), so `SPECMGR_LOG_FORMAT=RICH` fails closed as an unrecognized value (ACC-011); that is the strictest reading of the bullet's deliberate divergence from `SPECMGR_MCP_TRANSPORT`'s case-insensitive permissiveness. Related implementation-level pins: the status resource re-parses via `load_telemetry_config()` on every read (no cached process-wide singleton, no test-only reset hook), so it always reports the environment the process started with, per the VCR's AC-002 restart semantics; `SPECMGR_LOG_FILE_PATH`/`SPECMGR_OTEL_ENDPOINT` are stored as given (whitespace-only counts as unset for the non-blank pairing rule only); and the pinned `file=<on|off>` entry format from the Design Notes' "Status resource" bullet is what `telemetry_status.py` implements (the VCR's 2026-09-21 05:45 update entry's `file=disabled` example was superseded by that bullet, which the entry itself cites as the fixing source).

#### 2026-09-21 07:55:43.000Z - Middleware is a pure pass-through when both features are off (enablement gating pinned)

The final pre-implementation review found that the plan gated metrics (Task 5.2, "no-op when disabled") and log records (`SPECMGR_LOG_ENABLED`) but never stated that the middleware's correlation-ID generation, error-response attachment, and Task 3.3's raw-exception-to-`MCPError` conversion are gated on enablement. Read literally (ACC-008 / Scope / Task 3.3), those would run for the three invocation methods even in the default both-off config, which (a) attaches a correlation ID to error responses with no logs/spans behind it, and, more importantly, (b) converts a raw exception to an `MCPError` before the SDK dispatcher -- suppressing the dispatcher's own stderr `logger.exception` (under `mcp.server.runner`) that a raw exception would otherwise produce. Because the feature's own error record is not emitted when logging is off, Task 3.3's stated "accepted side effect" (replacing that traceback log) becomes a pure loss of the SDK's default error logging in the default config -- contradicting the ADR's "fully opt-in, off by default" driver and ACC-001's zero-change baseline. Per stakeholder decision, pinned into a new Design Notes bullet ("Middleware enablement gating") and into Tasks 3.1/3.3/3.4 and ACC-008: when neither `SPECMGR_LOG_ENABLED` nor `SPECMGR_OTEL_ENABLED` is `true`, the middleware is a pure pass-through for every method (no correlation ID, no conversion, no attachment), leaving the default config's error handling byte-for-byte the SDK's own.

#### 2026-09-21 03:45:00.000Z - Per-method error channels pinned against the installed mcp 2.0.0 wire model; raw exceptions converted for universal correlation-ID attachment

The post-exploration re-evaluation verified against `mcp/server/mcpserver/server.py` and `mcp/server/runner.py` that the three invocation methods carry errors to a middleware in three different shapes: `tools/call` converts every non-`MCPError` tool exception into an `isError: true` *result* before the middleware sees it (no exception object available at that layer), `resources/read` wraps SDK errors as `MCPError` (with the SDK's own `data={"uri": ...}`), and `prompts/get` propagates raw exceptions that the dispatcher would otherwise map to a generic `INTERNAL_ERROR` without a data field. Consequence pinned into the Design Notes and Tasks 3.1/3.3/3.4/5.3: the middleware detects tool errors from the post-`call_next` result dict, the correlation ID is attached via `_meta` (tool error results) or `error.data` (JSON-RPC errors), and -- per stakeholder decision -- raw exceptions/`ValidationError`s on the three invocation methods are converted to wire-identical `MCPError`s (same code/message the dispatcher would produce, plus the `data`) so ACC-008 holds universally; the accepted side effect is that the SDK's own server-side `logger.exception` of such a traceback is replaced by this feature's error log record (which carries the correlation ID anyway). The error-type counter uses the per-channel signal `tool_error`/JSON-RPC-code/`type(exc).__qualname__`, satisfying REQ `dacd01f4-ffd8-4363-a20b-5ac1ce11eef2`'s "error counts by exception type" clause without a REQ-text change.

#### 2026-09-21 03:45:00.000Z - Task 6.7 rewording scope: uc titles mandatory, ADR titles excluded as accepted residual gap, paths scrub-covered

The re-evaluation censused every title- and path-embedding exception call site: 22 title sites (13 `UcParseError` in `uc/models/v1/parser.py`, 8 `AdrParseError` in `models/adr/v1/parser.py`, 1 `AdrOptionNotFoundError` in `models/adr/v1/mutations.py`) and ~25 path sites (12 domains' `<domain>/tools/_io.py` `*NotFoundError` messages, 10 `DeleteError` in `general/tools/delete.py`, 3 in `feat/tools/_paths.py`). Per stakeholder decision, Task 6.7's *mandatory* rewording set is the title sites in non-deprecated domains (today: the 13 uc sites); the deprecated ADR domain's 9 title sites are *explicitly excluded* as an accepted residual gap (the domain is planned for phase-out, mirroring its `feat-107` cache exclusion -- ADR parse-error messages can still carry a title into free-text log/span content, and the path scrub still applies to them), and the path-embedding sites are scrub-covered and only optionally reworded (absolute paths are already disclosed client-side by the existing `specmgr://config` resource). ACC-004/ACC-012, the Scope bullets, the redaction Design Note, and VCRs `0a1c2f63`/`54fd355f`'s rewording clauses were aligned to this scope.

#### 2026-09-21 03:45:00.000Z - Log scrub runs at formatter level on the final rendered string; OTel bootstrap pins recorded

Two implementation-shape decisions from the same pass. (1) A handler-attached `logging.Filter` sees `LogRecord.msg`/`args`/`exc_info` but not the `exc_text` the formatter renders afterwards -- and tracebacks embed absolute file paths -- so the path scrub is pinned to formatter level on the final rendered string (the JSON `Formatter` and a `RichHandler` subclass each pass their output through the shared scrub function), applied to every specmgr-installed handler; the scrub pattern covers POSIX/Windows/UNC absolute paths replaced by a fixed `<redacted-path>` token, and Task 6.3 gains a Task 4.4-style canary test for the span side's private-container reliance. (2) The OTel bootstrap (Task 4.1) pins: `BatchSpanProcessor` (a `SimpleSpanProcessor` would block each span `end()` synchronously for the exporter timeout on an unreachable OTLP endpoint -- stalling tool calls outside the fail-open design), `PeriodicExportingMetricReader`, `endpoint=` base-URL semantics for the HTTP exporters, provider shutdown in `server.py`'s existing `_lifespan` placeholder (closing the Task 1a.2 after-serving final-collection finding), the meter slot + `ms`-unit histograms with explicit bucket advice, and the verified proxy-tracer ordering (Task 4.2's test uses the real import order).

#### 2026-09-19 17:30:00.000Z - Observable-instrument behavior confirmed for the DocCache counters: creation-time callbacks, collection-time values, CUMULATIVE default (Task 1a.5)

Tested by prototyping an observable counter against a throwaway `FakeCache` stand-in for `DocCache.stats()` (per-domain hit/miss counters + `stats()` accessor; a 4-instance registry mimicking the real 12 per-domain `DocCache` singletons), using `meter.create_observable_counter(...)` with an `InMemoryMetricReader` (default temporality), a second `InMemoryMetricReader` with DELTA temporality, and a `PeriodicExportingMetricReader` + `ConsoleMetricExporter(out=sys.stderr)` for the export-time check. Confirmed: (1) the callback is invoked **only at collection/export time, never at registration** (zero invocations immediately after instrument creation; callback invocations line up exactly with `collect()`/export cycles); (2) observed values reflect the stand-in's state **at collection time** -- the pre-increment collection returned all zeros, the next collection returned exactly the incremented values, and in the periodic run the stderr-exported `mcp.domain="req"` series tracked 3 -> 4 across an increment made mid-run; (3) per-attribute (per-domain) series are kept distinct (4 domains -> 4 separate data points per instrument per collection); (4) unchanged collections do not double-count or drift: the default reader temporality is **CUMULATIVE** (an unchanged collection re-reports the same cumulative value; DELTA -- only when explicitly requested via `preferred_temporality` -- reports 0 for unchanged intervals). Confirmed SDK-1.44-specific API shape Task 5.4 must code against: callbacks are registered **at instrument creation time** (`create_observable_counter(name, callbacks=[...])` -- the SDK instrument class has no public `add_callback`), and the callback (called with a `CallbackOptions` carrying `timeout_millis`) must **return an iterable of `Observation`** -- the older `result(Observation)` callable protocol is gone in this version; also, the `preferred_temporality` dict must be keyed by the SDK's **internal** instrument classes (`opentelemetry.sdk.metrics._internal.instrument.*`), not the API ones (API classes raise `Invalid instrument class found`). Implication for Task 5.4 (Phase 5) and the "Doc-cache hit/miss counters" Design Note (both revised): code against the confirmed API shape, and respect the two confirmed caveats -- the callback must yield an `Observation` for **every** live registry entry every cycle (a series the callback does not yield is **absent** from that collection -- verified by dropping one domain for a cycle and watching its series disappear), and its registry iteration must be defensive, because a raising callback makes the SDK log `Callback failed for instrument ...` (fail-open) and drop the instrument's entire collection cycle; no ACC contradiction.

#### 2026-09-19 17:25:00.000Z - OTLP-unreachable logging cadence confirmed: 4 lines per failed cycle, exporter's own loggers, no raise -- de-dup must be an exporter wrapper (Task 1a.4)

Tested by pointing the installed `OTLPSpanExporter`/`OTLPMetricExporter` at a deliberately closed `127.0.0.1` port (bind + close, then a connect probe proving `ConnectionRefusedError`), with a capture handler on the **root** logger installed **before** any exporter existed (SDK loggers propagate to root): (A) a continuous ~9 s episode (one span ended per second, `BatchSpanProcessor` with `schedule_delay_millis=500`), (B) two discrete `force_flush` "episodes" of 2 spans each, (C) a ~6 s metrics episode (`PeriodicExportingMetricReader`, `export_interval_millis=1000`, real counter incremented each cycle) -- all with `timeout=5` to bound spike runtime -- plus a dedicated single-cycle run with the **production-default** timeout (10 s, no `timeout=` arg). Confirmed: the SDK logs **neither once per episode nor once per cycle, but once per failed attempt within a cycle plus one final error per cycle**, and repeats this **every cycle for as long as telemetry data keeps flowing** (a cycle only skips when its queue is empty): with the production-default 10 s timeout one failed cycle emits exactly **3x WARNING** `Transient error HTTPConnectionPool(...): Max retries exceeded with url: / (Caused by NewConnectionError(...)) encountered while exporting span batch, retrying in N.NNs.` (one per internal retry attempt; backoff `2^n * uniform(0.8, 1.2)` s bounded by the exporter deadline) + **1x ERROR** `Failed to export span batch due to timeout, max retries or shutdown.` -- **4 lines per failed cycle**, measured cycle duration ~7.1 s (at `timeout=5`: 2x WARNING + 1x ERROR per cycle; measured totals: 12 trace lines over 4 cycles in the continuous phase, 6 lines over the two force_flush episodes, 9 metric lines over 3 cycles). All lines are under the **exporters' own module loggers** `opentelemetry.exporter.otlp.proto.http.trace_exporter` (spans) and `opentelemetry.exporter.otlp.proto.http.metric_exporter` (metrics; same messages with "metrics batch"); the only additional noise is `urllib3.connectionpool` DEBUG lines (`Starting new HTTP connection (N): ...`), invisible at the default INFO root level. Crucially, the exporter **returns** `SpanExportResult.FAILURE`/`MetricExportResult.FAILURE` **without raising**, so the SDK's own exception-logging paths never fire for an unreachable endpoint: `opentelemetry.sdk._shared_internal`'s `BatchProcessor` (which already carries the SDK's own 20-second-window `DuplicateFilter`) and `opentelemetry.sdk.metrics.export`'s periodic reader emitted **zero** records in the entire capture -- there is no SDK-provided episode de-duplication to lean on. Implication for Task 4.5 (Phase 4) and the "Fail-open policy (runtime failures only)" Design Note (both revised): the Design Notes' previous "likely ... on every failed export cycle" assumption is confirmed in direction but imprecise in shape (it is per-attempt-within-cycle + final error, 4 lines/cycle at production defaults, repeating every cycle while data flows); Task 4.5 must implement a **thin wrapper around the configured OTLP exporter that tracks first-failure/recovery transitions itself** (suppressing the two exporter module loggers while active, emitting at most one stderr message per episode), since a bare `logging.Filter` with episode state on those logger names cannot satisfy ACC-010's "never repeating while the condition persists" on its own -- the SDK logs no success at all, so recovery is not observable from the log stream; no ACC contradiction (ACC-010 stays achievable as worded).

#### 2026-09-19 17:20:00.000Z - SpanProcessor.on_end() redaction: no public mutation API exists; private container replacement is the confirmed mechanism (Task 1a.3)

Tested with a minimal custom `SpanProcessor` placed **first** in the chain (`[mutator, SimpleSpanProcessor(InMemorySpanExporter)]`, and `[mutator, BatchSpanProcessor(InMemorySpanExporter, schedule_delay_millis=100)]`) against the installed `opentelemetry-sdk` 1.44.0, with a span carrying a sensitive attribute plus a `record_exception` event whose message embeds a sentinel path/title; each of 10 mechanisms was run in its own fresh provider, then the in-memory exporter's received span was inspected (and, for the winning mechanisms, the real OTLP protobuf via `opentelemetry.exporter.otlp.proto.common._internal.trace_encoder._encode_span`). Confirmed: `on_end()` receives a **`ReadableSpan`** (MRO `[ReadableSpan, object]` -- not the SDK's live `Span`), so there is **no public mutation path at all**: `ReadableSpan` has no `set_attribute` (AttributeError), its `attributes` property is a `MappingProxyType` (item assignment -> TypeError), and the span's own `BoundedAttributes` container is locked by `Span.end()` setting `_attributes._immutable = True` *before* processors are notified (in-place `rs._attributes[k] = v` -> bare TypeError); event attributes are equally locked (`BoundedAttributes` defaults to `immutable=True` and `add_event` builds event attributes with that default), so in-place mutation through the public `event.attributes` property also raises TypeError. What **works** -- and is what Phase 6 must use -- is **replacing the private containers inside `on_end`**, before any later exporter reads the span: `rs._attributes = BoundedAttributes(None, scrubbed, immutable=False)` (a plain `dict` replacement works identically), and for the exception event either `ev._attributes = BoundedAttributes(None, scrubbed, immutable=False)` (per event) or rebuilding `rs._events` with new `Event` objects; both mechanisms were proven visible end-to-end -- the InMemorySpanExporter (simple **and** batch chains) and the OTLP protobuf both received `sensitive = 'REDACTED'` and `exception.message = 'REDACTED-MSG'` / `exception.stacktrace = 'REDACTED-STACK'` -- because both export paths read the *current* `self._attributes`/`self._events` containers at export time (`ReadableSpan.to_json()`, and `trace_encoder._encode_span` via the public `span.attributes`/`span.events`/`event.attributes` properties). Fragility: the mechanism depends on the private field names `ReadableSpan._attributes`, `ReadableSpan._events`, `Event._attributes` and on the SDK continuing to hand exporters the live containers (rather than end-time snapshots); if a future release restructures any of these, redaction silently stops applying (no exception is raised -- spans simply leak again). Implication for Task 6.1/6.3 (Phase 6) and the "Redaction scope and limits" Design Note (both revised): Task 6.1 implements the confirmed private-container-replacement mechanism (with the fragility carried into Task 6.3's tests); record-only recommendation for Task 7.1 (Phase 7 -- which is the task that applies `pyproject.toml` changes): **narrow `opentelemetry-sdk>=1.44.0` (and `opentelemetry-exporter-otlp-proto-http>=1.44.0`) to `<2.0.0`**, so a future major upgrade that restructures these internals trips at install/lock time rather than silently breaking Phase 6's redaction at runtime; no ACC contradiction -- ACC-012's guarantee is achievable via this mechanism.

#### 2026-09-19 17:15:00.000Z - Console exporter streams confirmed: default stdout, `out=sys.stderr` gives zero non-JSON-RPC stdout in a real stdio session (Task 1a.2)

Tested: (a) direct instantiation of `opentelemetry.sdk.trace.export.ConsoleSpanExporter` and `opentelemetry.sdk.metrics.export.ConsoleMetricExporter` with no args and with `out=sys.stderr`, each emitting one real span (`TracerProvider` + `SimpleSpanProcessor` + `force_flush`) / one real metric (`MeterProvider` + `PeriodicExportingMetricReader` + counter add), run as a child process whose fd 1/fd 2 are captured separately by the parent; (b) a full `specmgr mcp` stdio smoke test: a throwaway driver installed a global SDK `TracerProvider` (ConsoleSpanExporter, `SimpleSpanProcessor`) + `MeterProvider` (ConsoleMetricExporter via `PeriodicExportingMetricReader`, 500 ms interval) **before** importing the real `biz.dfch.specmgr.server` module, then ran the real `mcp = MCPServer(...)` on `transport="stdio"` while a parent script fed valid JSON-RPC frames (`initialize`, `notifications/initialized`, `tools/list`, `tools/call get_feat_template`, `resources/read specmgr://version`, `prompts/get compact_history`, `ping`) and asserted on the byte streams. Confirmed: both console exporters' **default output stream is stdout** (constructor defaults `out: IO = sys.stdout` / `out: IO = stdout`, bound at module-import time) -- measured 874 B span payload / 5676 B metric payload on stdout with no args, and 0 B on stdout with `out=sys.stderr`; the exact constructor arguments Task 4.1 must use are `ConsoleSpanExporter(out=sys.stderr)` and `ConsoleMetricExporter(out=sys.stderr)`, passed **by keyword** (note `ConsoleSpanExporter`'s first positional arg is `service_name`, so a bare positional `sys.stderr` would land in the wrong slot). In the smoke test, the redirected mode's child stdout contained **exactly 6 JSON-RPC frames and zero other bytes** (all 6 request ids answered), while the span JSON (including the SDK's built-in `OpenTelemetryMiddleware` spans for `initialize`, `tools/call get_feat_template`, and `resources/read`) and the periodic **and** final metric exports all landed on stderr; the control (default) mode's stdout was contaminated -- 6 JSON-RPC frames plus 150 non-JSON lines (a full `resource_metrics` console blob). Additional confirmed mcp-2.0.0-specific behavior: the SDK's `stdio_server()` transport **claims fd 0/1 while serving** -- fd 1 is diverted to fd 2 (`os.dup2(os.dup(2), 1)`) and the JSON-RPC wire is served through a private duplicate fd -- so a default-stream console export *during* serving silently lands on stderr, and the real stdout contamination comes from exports **after serving ends** (in the control run, the `PeriodicExportingMetricReader`'s final collection on `shutdown()`, after fd 1 is restored). Implication for Task 4.1/4.10 (Phase 4) (both revised): the redirection requirement is real and is now concrete -- `out=sys.stderr` on both console exporters is what makes ACC-001/ACC-003 hold once telemetry is enabled, and Task 4.10's stdout-safety test mirrors the redirected-mode assertion (every stdout byte part of a JSON-RPC frame); no ACC contradiction.

#### 2026-09-19 17:10:00.000Z - OTLP exporter protocol: OTLP/HTTP+protobuf (`opentelemetry-exporter-otlp-proto-http`), not OTLP/gRPC

Task 1a.1 added `opentelemetry-sdk>=1.44.0` and one OTLP exporter to the existing `mcp` extra (not a new extra -- telemetry is meaningless without the MCP server, and the ADR's "minimize new required runtime dependencies" driver concerns the base `dependencies` list, which stays untouched). The governing ADR `fdbb6d22-278a-4ecf-b2f6-db208bc49fc6` anticipates that "only `opentelemetry-sdk` (+ exporter) is genuinely new" but deliberately does not pre-select the exporter protocol, delegating that choice to this task. Chosen: `opentelemetry-exporter-otlp-proto-http` (OTLP/HTTP+protobuf, collector port 4318); not chosen: `opentelemetry-exporter-otlp-proto-grpc` (OTLP/gRPC, collector port 4317). Trade-off analysis, verified against the actual 1.44.0 package metadata on PyPI rather than assumption: both exporters in this release share the same core dependency set -- `opentelemetry-exporter-otlp-proto-common==1.44.0`, `opentelemetry-proto==1.44.0` (which itself requires `protobuf`), `googleapis-common-protos`, and `opentelemetry-sdk~=1.44.0` -- so their dependency difference is exactly one package each: the gRPC exporter additionally requires `grpcio` (a multi-MB, platform-specific native C-extension wheel, with per-Python-minor lower bounds -- `>=1.63.2` on 3.11/3.12 and `>=1.66.2` on 3.13 -- spanning this repo's full CI matrix), while the HTTP exporter additionally requires `requests~=2.7`, already present in `uv.lock` transitively (2.34.2, constraint satisfied), adding nothing new. The resulting lock delta for the chosen option is seven new packages, all pure-Python wheels except the standard `protobuf` runtime, with `opentelemetry-api` remaining a single, consistent 1.44.0 (no bump, no fork) and no `grpcio`. Why HTTP wins here: (1) dependency footprint -- the ADR's "minimize new required runtime dependencies" driver; native `grpcio` is the single largest new supply-chain item either option could add (harder to audit, platform wheels across the 3.11/3.12/3.13 CI matrix, historical grpcio pinning/version-transition churn), and the gRPC-specific transport benefits (HTTP/2 multiplexing, stream compression) never engage at this feature's telemetry volume (a handful of spans/metric points per MCP tool/resource/prompt invocation); (2) endpoint reachability -- OTLP/HTTP on 4318 is a first-class, spec-mandated OTLP transport (not a fallback) and the de-facto default in common collector deployments (the standard `otelcol` image listens on both 4317 and 4318 out of the box, and cloud-vendor OTLP ingestion endpoints are overwhelmingly documented as plain HTTPS URLs), and being plain HTTP it routes through ordinary proxies/tunnels/load balancers without gRPC-specific (HTTP/2, direct-TCP) infrastructure -- the right fit for a stdio MCP server whose production operators may use any standard OTLP collector (this feature builds no specific backend, per its Explicitly Out Of Scope); (3) configuration shape -- the plan's `SPECMGR_OTEL_ENDPOINT` is specified as an "OTLP endpoint URL" (a full URL), which the HTTP exporter consumes natively (appending the spec-mandated `/v1/traces`/`/v1/metrics` paths), while the gRPC exporter takes a bare `host:port`; (4) the Phase 4 fail-open OTLP-unreachable design (Task 4.5, spiked by Task 1a.4) applies identically to either transport, so nothing in the feature's degradation semantics is lost. Phases 4-6 build their exporter bootstrap and redaction work against this choice; Task 7.1 (Phase 7) re-confirms it once all implementation phases have landed.

#### 2026-09-19 17:05:00.000Z - Independent gaps fixed directly instead of deferred to Phase 1a

A handful of gaps found during the fourth review did not need empirical confirmation, only re-deriving the plan against itself, so they were fixed directly in this same pass rather than added to Phase 1's spike-task batch (Tasks 1a.1-1a.7): `SPECMGR_LOG_LEVEL` and the three boolean-flag env vars (`SPECMGR_LOG_ENABLED`, `SPECMGR_LOG_FILE_ENABLED`, `SPECMGR_OTEL_ENABLED`) had no specified fail-closed validation behavior, unlike `SPECMGR_LOG_FORMAT`/`SPECMGR_OTEL_EXPORTER` -- added to ACC-011 and Tasks 1.1/1.2. ACC-004 and ACC-012 restated a near-identical "no body/path/title as a structured attribute" guarantee once for logs+spans and once for spans alone -- narrowed ACC-004 to log records only, leaving ACC-012 as the sole span-attribute guarantee. Task 4.3's fail-open guard for a broken `Server.middleware` contract landed a full phase after the risk it guards against (Task 3.2's append in Phase 3) -- Task 3.2 now wraps its own append in a minimal try/except from the moment it lands, with Task 4.3 upgrading that into the complete config-aware behavior rather than being an independent, later-arriving guard. Tasks 5.4/5.5/5.6 gained explicit notes on two gaps a large refactor otherwise risks glossing over: the `DocCache`-instance-registry gap (12 independent per-domain singletons, no described discovery mechanism for a single meter callback to attribute metrics per domain) and the lock-mutual-exclusion regression-test gap (the 13-file lock-timing refactor changes concurrency-critical acquire/release control flow, but no task verified release-on-exception behavior survives it, beyond the new timing value itself).

#### 2026-09-19 17:00:00.000Z - Phase 1a technical spike added ahead of Phase 4-6, absorbing Phase 7's packaging tasks

Rather than write more design prose that risks encoding further unverified assumptions about the OpenTelemetry Python SDK, a batch of technical-spike-and-packaging tasks (Tasks 1a.1-1a.7) was appended to Phase 1's own checklist, immediately after Task 1.6, deliberately without renumbering any later phase -- every existing Task-number cross-reference elsewhere in this document stays valid. (Not a new `#### Phase 1a: ...` heading: the `Phase N: title` heading requires an integer `N`, per `feat/models/v1/body.py`'s `_PHASE_HEADING_PATTERN`, so a fractional "Phase 1a" heading is not schema-valid; the `1a.N` sub-numbering lives entirely in the task items' own free-text description instead.) These tasks relocate Phase 7's packaging work (installing `opentelemetry-sdk` + an OTLP exporter, regenerating `uv.lock`) to before any phase that needs to import SDK symbols, and run small, throwaway-script spikes converting several other still-assumption-based Phase 4/5/6 design claims into confirmed facts (console-exporter output stream, `SpanProcessor` attribute-mutation feasibility, the OTel SDK's actual export-failure logging cadence, and observable-instrument behavior) before more implementation code is built on top of them. Phase 7 is repurposed into a final packaging-consistency confirmation now that its original work has moved earlier.

#### 2026-09-19 16:12:00.000Z - Title redaction is a best-effort, two-component guarantee, not an unconditional universal one

After confirming that real, existing exception messages in the codebase already embed a document/artifact title (`UcParseError`) or an absolute path (`DeleteError`) verbatim, and that a generic post-hoc filter cannot reliably detect free-form title text (unlike a path, which at least has a regex-matchable shape), the redaction guarantee was split into two components instead of left as a single unconditional claim: (1) this feature's own code never attaches a body/path/title as a *structured* attribute -- fully achievable, since this feature controls what it names/sets; (2) a regex-based path scrub plus a narrow, message-text-only rewording of the specific enumerated exception classes (Task 6.7) serves as the free-text backstop. This is the more honest contract to test against and ship, rather than an "under any configuration" wording the implementation could never fully satisfy.

#### 2026-09-19 16:11:00.000Z - ServerMiddleware's method scope must be filtered explicitly, not assumed to already match "item invocation"

Confirmed against the installed SDK source that `Server.middleware`/`ServerMiddleware` -- this feature's single instrumentation point -- wraps every inbound JSON-RPC request and notification, not only `tools/call`/`resources/read`/`prompts/get`. Rather than let logging, correlation-ID assignment, and metric counters silently fire on `initialize`/`*/list`/`ping`/`notifications/*` too, the middleware now explicitly filters on `ctx.method`, and this scope is called out as its own acceptance criterion (ACC-013) so it is independently testable and cannot regress unnoticed.

#### 2026-09-19 15:32:00.000Z - Metrics-correctness VCR gap closed with a new AC instead of folded into an existing one

Rather than stretching an existing telemetry-VCR criterion (e.g. AC-002's span coverage) to also imply the metrics are correct, a dedicated AC-008 was added, mirroring how AC-007 was earlier added for span redaction: each REQ clause with independently verifiable behavior gets its own acceptance criterion, so a future failure of just the metrics (with spans/tracing otherwise healthy) is diagnosable from the VCR alone.

#### 2026-09-19 15:31:00.000Z - OTLP-degradation task moved from Phase 5 to Phase 4; Phase 3 no longer claims counter work

Two structural Task List decisions made during the plan-consistency review: (1) the OTLP-unreachable de-duplication task (previously Phase 5's Task 5.6) is moved into Phase 4 as the new Task 4.5, since it concerns the OTel exporter/SDK bootstrap Phase 4 already configures, not the domain-level metrics counters Phase 5 adds; (2) Phase 3's Task 3.1 no longer claims to "increment call/error counters," since no `MeterProvider` exists until Phase 4 and the counters themselves are Task 5.2/5.3's responsibility -- Phase 3 is now scoped to correlation-ID generation and logging only.

#### 2026-09-19 14:20:00.000Z - Redaction widened to a global SpanProcessor after a follow-up clarification

Initially decided to scope the redaction safeguard to only this feature's own middleware/meter attributes. A follow-up clarification established that Phase 4's tracing activates the SDK's own built-in `OpenTelemetryMiddleware` rather than creating a new span of this feature's own -- meaning essentially all trace spans, including raw exception messages via `record_exception`/`set_status(str(e))`, are SDK-owned. Given that framing, the stakeholder reversed the initial answer: Phase 6 now implements a global, `TracerProvider`-level `SpanProcessor` scrubbing every span in the process (including the SDK's built-in middleware's spans), the only way to actually satisfy REQ `dacd01f4-ffd8-4363-a20b-5ac1ce11eef2`'s "never attaching ... to any span" wording. The logging-side `Filter` stays scoped to specmgr's own log records, since those are entirely this feature's own creation.

#### 2026-09-19 14:18:00.000Z - Static misconfiguration fails closed; mcp dependency keeps its unbounded pin

Two narrower decisions made during the same pre-implementation review: (1) a static logging/telemetry misconfiguration (e.g. `SPECMGR_LOG_FILE_ENABLED=true` without `SPECMGR_LOG_FILE_PATH`) fails closed -- `specmgr mcp` refuses to start -- rather than falling under the runtime fail-open policy, consistent with every other `SPECMGR_*` environment variable already raising on a bad value; (2) `pyproject.toml`'s `mcp>=2.0.0` pin keeps no upper bound, relying instead on the Task 4.4 compatibility test and the runtime fail-open behavior to catch a breaking SDK change.

#### 2026-09-19 14:16:00.000Z - New conventions.md section: observability fails open, application logic never does

Added an "Observability Degrades Gracefully, Application Logic Never Does" section to `.specmgr/conventions.md`, generalizing this feature's fail-open (runtime observability failures)/fail-closed (static misconfiguration) split into a reusable, explicitly-scoped exception to the existing error-handling convention, mirroring how this same feature's Task 0.6 earlier generalized the VCR-authored-early practice.

#### 2026-09-19 13:29:00.000Z - Author VCR early, extend phase-by-phase; not after implementation

Per the stakeholder's explicit instruction, VCRs for this feature's REQs are authored as soon as the REQs are finalized (Phase 0), scoped to whatever is genuinely verifiable from each REQ's specified contract alone (`## Coverage: partial`), and are then extended -- not rewritten -- at the end of each implementation phase that makes further criteria concretely verifiable. This avoids both a purely ceremonial post-implementation VCR (describing whatever got built) and a purely aspirational pre-implementation one (never updated once code exists); it also surfaces a design/implementation deviation immediately, at the cheapest point to fix it. This practice is generalized into `.specmgr/conventions.md` for future features, not left as a one-off decision scoped to this feature alone.

#### 2026-09-19 13:28:00.000Z - Third REQ split out for the status resource

The stakeholder's Interaction Capability answer (a status resource returning `list[str]`) does not fit either existing REQ's characteristics (Maintainability/Security/Compatibility for logging; Performance Efficiency/Reliability/Compatibility for telemetry), so it was split into its own REQ (`41444084-6821-426d-84a2-028a3f4fed0b`, Interaction Capability), mirroring the earlier "two REQs, one ADR" split decision below.

#### 2026-09-19 12:29:00.000Z - Two REQs, one ADR

The stakeholder chose to split the formal requirement into two REQ documents (logging vs. telemetry) to reflect that these are two distinct requirements, while keeping a single ADR since only one technology decision (the combined stdlib-logging + OpenTelemetry stack) is being made.

#### 2026-09-19 12:28:00.000Z - stdlib logging + OpenTelemetry, both opt-in/off by default

See ADR `fdbb6d22-278a-4ecf-b2f6-db208bc49fc6` for the full rationale: stdlib `logging` (dual rich/JSON format) plus OpenTelemetry API/SDK for metrics and tracing, wired through a single `ServerMiddleware`, both features fully opt-in and off by default.

### Related PRs / Commits

- `0b2f178` -- build(feat-139): add opentelemetry-sdk and OTLP/HTTP exporter to mcp extra (Task 1a.1).
- `66d3182` -- docs(feat-139): record Phase 1a spike findings, revise plan at re-evaluation gate (Tasks 1a.2-1a.6).
- `358e416` -- docs(feat-139): pass Phase 1a batch quality gate, complete Tasks 1a.1-1a.7 (Task 1a.7).
- `dc46844` -- feat(feat-139): add fail-closed telemetry config, status resource, and startup wiring (Tasks 1.1-1.6).
- `f673697` -- feat(feat-139): add structured logging setup with JSON/rich formatters and opt-in file sink (Tasks 2.1-2.5).
- `14c4aac` -- feat(feat-139): add correlation-ID call middleware with per-channel error handling (Tasks 3.1-3.7).
- `981d60e` -- feat(feat-139): add OpenTelemetry SDK bootstrap with fail-open policy and OTLP episode de-dup (Tasks 4.1-4.11).
- `5471987` -- feat(feat-139): add MCP metrics instrumentation with domain mapping, cache counters, and lock-wait timing (Tasks 5.1-5.8).
- `e1d4692` -- feat(feat-139): add redaction safeguards: formatter-level path scrub, global span scrubber, uc title rewording (Tasks 6.1-6.8).

### More Information

Traceability chain: QA `40a17fb8-c069-4c38-a092-47912e1fa41d` -> REQ `bc356fc9-964a-4274-93ec-4627c5aeb2e5` / REQ `dacd01f4-ffd8-4363-a20b-5ac1ce11eef2` / REQ `41444084-6821-426d-84a2-028a3f4fed0b` -> ADR `fdbb6d22-278a-4ecf-b2f6-db208bc49fc6` -> VCR `0a1c2f63-9576-4463-bf40-8f771f14fefb` / VCR `54fd355f-0978-4dd0-9e1d-da89432e5321` / VCR `f494d230-97f3-4db9-9ef9-ed9c903ee008` -> this feature folder. GitHub issue: https://github.com/dfch/biz.dfch.SpecMgr/issues/139.
